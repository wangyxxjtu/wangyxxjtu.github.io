<?xml version="1.0" encoding="utf-8"?>
<search>
  <entry>
    <title>Hello World</title>
    <url>/2019/03/19/hello-world/</url>
    <content><![CDATA[<p>Welcome to <a href="https://hexo.io/">Hexo</a>! This is your very first post. Check <a href="https://hexo.io/docs/">documentation</a> for more info. If you get any problems when using Hexo, you can find the answer in <a href="https://hexo.io/docs/troubleshooting.html">troubleshooting</a> or you can ask me on <a href="https://github.com/hexojs/hexo/issues">GitHub</a>.</p>
<h2 id="Quick-Start"><a href="#Quick-Start" class="headerlink" title="Quick Start"></a>Quick Start</h2><h3 id="Create-a-new-post"><a href="#Create-a-new-post" class="headerlink" title="Create a new post"></a>Create a new post</h3><figure class="highlight bash"><table><tr><td class="code"><pre><span class="line">$ hexo new <span class="string">&quot;My New Post&quot;</span></span><br></pre></td></tr></table></figure>

<p>More info: <a href="https://hexo.io/docs/writing.html">Writing</a></p>
<h3 id="Run-server"><a href="#Run-server" class="headerlink" title="Run server"></a>Run server</h3><figure class="highlight bash"><table><tr><td class="code"><pre><span class="line">$ hexo server</span><br></pre></td></tr></table></figure>

<p>More info: <a href="https://hexo.io/docs/server.html">Server</a></p>
<h3 id="Generate-static-files"><a href="#Generate-static-files" class="headerlink" title="Generate static files"></a>Generate static files</h3><figure class="highlight bash"><table><tr><td class="code"><pre><span class="line">$ hexo generate</span><br></pre></td></tr></table></figure>

<p>More info: <a href="https://hexo.io/docs/generating.html">Generating</a></p>
<h3 id="Deploy-to-remote-sites"><a href="#Deploy-to-remote-sites" class="headerlink" title="Deploy to remote sites"></a>Deploy to remote sites</h3><figure class="highlight bash"><table><tr><td class="code"><pre><span class="line">$ hexo deploy</span><br></pre></td></tr></table></figure>

<p>More info: <a href="https://hexo.io/docs/deployment.html">Deployment</a></p>
]]></content>
  </entry>
  <entry>
    <title>hexo-command</title>
    <url>/2018/10/24/hexo-command/</url>
    <content><![CDATA[<h1 id="this-file-record-some-usually-used-hexo-command"><a href="#this-file-record-some-usually-used-hexo-command" class="headerlink" title="this file record some usually used hexo command"></a>this file record some usually used hexo command</h1><p><strong>all of the comands run in git bash under blog directory: D:/blog</strong></p>
<figure class="highlight bash"><table><tr><td class="code"><pre><span class="line">hexo new name</span><br></pre></td></tr></table></figure>
<p>create a new blog file <em>name.md</em> in your blog dir, for example (D:/blog/source/_post/)</p>
<figure class="highlight bash"><table><tr><td class="code"><pre><span class="line">hexo g -d</span><br></pre></td></tr></table></figure>
<p>update the blog, the web page will update by a few minutes, therefore, you should wait for a while to see the changes</p>
]]></content>
  </entry>
  <entry>
    <title>sheel_prog.md</title>
    <url>/2019/04/20/shell-prog/</url>
    <content><![CDATA[<ul>
<li><p>shell 编程基础</p>
</li>
<li><p>** 1 入门简单例子 **</p>
<figure class="highlight bash"><table><tr><td class="code"><pre><span class="line"><span class="comment">#！ /bin/sh</span></span><br><span class="line"></span><br><span class="line">Cd ~</span><br><span class="line"></span><br><span class="line">Mkdir shell_tut</span><br><span class="line"></span><br><span class="line">Cd sheel_tut</span><br><span class="line"></span><br><span class="line">For ((I=0;i&lt;10;i++)): <span class="keyword">do</span></span><br><span class="line">	touch test_<span class="variable">$I</span>.txt</span><br><span class="line"><span class="keyword">done</span></span><br></pre></td></tr></table></figure>
<p>上述例子中，第一行制定脚本解释器，创建循环体， 循环10次，创建10个文件， 循环体结束，其中for do done都是sh关键字。#开头为注释，无多行注释方法<br>要运行脚本两种方法，./test.sh 或者sh test.sh</p>
</li>
</ul>
<ul>
<li><p>**2 变量定义 **<br>在shell中变量定义直接声明赋值即可，例如</p>
<figure class="highlight bash"><table><tr><td class="code"><pre><span class="line">name=<span class="string">&#x27;yaxiwang&#x27;</span> <span class="comment">#注意等号不能有空格</span></span><br><span class="line">name=‘yaxiongwang‘ <span class="comment">#重定义变量</span></span><br></pre></td></tr></table></figure>
<p>使用变量的时候：$+变量名</p>
<figure class="highlight bash"><table><tr><td class="code"><pre><span class="line">Name=<span class="string">&#x27;yaxionwang&#x27;</span></span><br><span class="line"><span class="built_in">echo</span> <span class="variable">$Name</span></span><br></pre></td></tr></table></figure>
<p>既可以输出name</p>
</li>
<li><p>**3 字符串类型 **<br>在shell中，字符串类型可以使用单引号和双引号，其中单引号会按原样输出，其中不可以包含变量，单引号中不能出现单引号，使用转义字符也不行<br>而双引号中可以有变量，也可使用转义字符，如：</p>
<figure class="highlight bash"><table><tr><td class="code"><pre><span class="line">name==<span class="string">&#x27;yaxiongwang&#x27;</span></span><br><span class="line">str=<span class="string">&quot;Hello. I know you are \&quot;#name\&quot;! \n&quot;</span></span><br></pre></td></tr></table></figure></li>
</ul>
<p> 1)拼接字符串<br> <figure class="highlight bash"><table><tr><td class="code"><pre><span class="line">name==<span class="string">&#x27;yaxiongwang&#x27;</span></span><br><span class="line">greeting=<span class="string">&quot;hello, &quot;</span><span class="variable">$name</span><span class="string">&quot; !&quot;</span></span><br><span class="line">greeting=<span class="string">&quot;hello, <span class="variable">$&#123;name&#125;</span> !&quot;</span></span><br></pre></td></tr></table></figure><br> 2)获得字符串长度<br> <figure class="highlight bash"><table><tr><td class="code"><pre><span class="line">String = <span class="string">&#x27;abdc&#x27;</span></span><br><span class="line">Echo <span class="variable">$&#123;#string&#125;</span></span><br><span class="line"><span class="comment">#提取子串</span></span><br><span class="line"><span class="built_in">echo</span> <span class="variable">$&#123;string:1:4&#125;</span></span><br><span class="line"><span class="comment">#查找字符串</span></span><br><span class="line"><span class="built_in">echo</span> ‘expr index <span class="string">&quot;<span class="variable">$string</span>&quot;</span> bd’ <span class="comment">#输出2，找出单词bd在单词string中的位置</span></span><br></pre></td></tr></table></figure></p>
<ul>
<li>**4流程控制 **<br>主要是if语句与循环，在sheel中if语句不能为空，if else块中必须有内容可以执行，否则报错，如果没有内容可以不写。<br>If语句基本语法：<figure class="highlight bash"><table><tr><td class="code"><pre><span class="line"><span class="keyword">if</span> condition</span><br><span class="line"><span class="keyword">then</span> </span><br><span class="line">	<span class="built_in">command</span> 1</span><br><span class="line">        Command 2</span><br><span class="line"><span class="keyword">else</span></span><br><span class="line">	<span class="built_in">command</span> 1</span><br><span class="line">	<span class="built_in">command</span> 2</span><br><span class="line">	<span class="built_in">command</span> 3</span><br><span class="line"><span class="keyword">fi</span></span><br><span class="line"></span><br><span class="line"><span class="comment">#另一种形式是</span></span><br><span class="line"><span class="keyword">for</span> var <span class="keyword">in</span> item1, item2, item3....itemn</span><br><span class="line"><span class="keyword">do</span></span><br><span class="line">	cmd 1</span><br><span class="line">	cmd 2</span><br><span class="line"><span class="keyword">done</span></span><br><span class="line"></span><br><span class="line"><span class="comment">#也可以是c风格的for</span></span><br><span class="line"><span class="keyword">for</span> ((exp1; exp2; exp3))</span><br><span class="line"><span class="keyword">do</span></span><br><span class="line">	cmds</span><br><span class="line"><span class="keyword">done</span></span><br></pre></td></tr></table></figure></li>
</ul>
<p>while循环的使用方法</p>
<figure class="highlight bash"><table><tr><td class="code"><pre><span class="line"><span class="keyword">while</span> condition</span><br><span class="line"><span class="keyword">do</span></span><br><span class="line">	cmds</span><br><span class="line"><span class="keyword">done</span></span><br><span class="line"></span><br><span class="line"><span class="comment">#无限循环可以写作</span></span><br><span class="line"><span class="keyword">while</span> <span class="literal">true</span>或者<span class="keyword">while</span> ：</span><br></pre></td></tr></table></figure>

<ul>
<li><p>**5 基本逻辑运算符 **</p>
<figure class="highlight bash"><table><tr><td class="code"><pre><span class="line">-ne 不等于</span><br><span class="line">-eq 等于</span><br><span class="line">-le 小于等于</span><br><span class="line">-l 小于</span><br><span class="line">-ge 大于等于</span><br><span class="line">-g 大于</span><br><span class="line">== 字符串比较</span><br></pre></td></tr></table></figure>
<p>逻辑联结符：<br>&amp;&amp;, ||, &lt;和&gt;必须写在双中括号中[[a&gt;b &amp;&amp; c&lt;d]],如果写在一个单中括号中报错</p>
</li>
<li><p>**6 $符与中括号[]使用 **</p>
<figure class="highlight bash"><table><tr><td class="code"><pre><span class="line">count =1</span><br><span class="line"><span class="keyword">while</span> [ <span class="variable">$count</span> -le 5]</span><br><span class="line"><span class="keyword">do</span></span><br><span class="line">	cmd 1</span><br><span class="line">	count=$[ <span class="variable">$count</span>+1 ]</span><br><span class="line"><span class="keyword">done</span></span><br></pre></td></tr></table></figure>
<p>中括号中是可执行的程序（运算），count=$[ $count+1 ]第一个$调用变量，而第二个$获得可执行程序的结果，或者说的到命令输出</p>
</li>
<li><p>**7 数组使用 **<br>初始化数组：</p>
<figure class="highlight bash"><table><tr><td class="code"><pre><span class="line">array=(a b c d)</span><br><span class="line">array[0]<span class="comment">#索引数组</span></span><br></pre></td></tr></table></figure></li>
<li><p>**8 括号的使用技巧 **<br>字符串比较用双中括号[[ ]]；算数比较用单中括号[ ]——左右留空格<br>算数运算用双小括号(( )) ；shell命令及输出用小括号( )——左右不留空格<br>快速替换用花括号{ }——左右留空格<br>反单引号起着命令替换的作用<code> </code></p>
</li>
<li><p>**9 scp（其他命令）错误：argument list too long **</p>
<figure class="highlight bash"><table><tr><td class="code"><pre><span class="line">find dir <span class="string">&#x27;*&#x27;</span> -<span class="built_in">exec</span> scp -r &#123;&#125; yaxiwang@cluster.eng.uts.edu.au:~/Data/ \;</span><br></pre></td></tr></table></figure>
<p>注意后面的分号一定要有</p>
</li>
<li><p>** 利用shell拷贝文件到远程 **<br>主程序</p>
<figure class="highlight bash"><table><tr><td class="code"><pre><span class="line">word_vec_pth=<span class="string">&quot;/Volumes/Untitled/TBIRDataSet/NUS-WIDE/word_vec_stanford/&quot;</span></span><br><span class="line"> tags_pth=<span class="string">&quot;./tags_filtered.txt&quot;</span></span><br><span class="line"></span><br><span class="line"> cat <span class="variable">$tags_pth</span> | <span class="keyword">while</span> <span class="built_in">read</span> line <span class="comment">#一行一行读取文件</span></span><br><span class="line"> <span class="keyword">do</span></span><br><span class="line">     tmp_line=<span class="variable">$&#123;word_vec_pth&#125;</span><span class="variable">$&#123;line&#125;</span>.txt</span><br><span class="line">     <span class="keyword">if</span> [ ! -f <span class="variable">$tmp_line</span> ];<span class="keyword">then</span> <span class="comment">#判断文件是否存在</span></span><br><span class="line">            <span class="built_in">echo</span> 1</span><br><span class="line">    <span class="keyword">else</span></span><br><span class="line">     <span class="comment">#传入参数和后面的对应</span></span><br><span class="line">            ./scp_expect.sh <span class="string">&quot;<span class="variable">$tmp_line</span>&quot;</span> <span class="string">&quot;yaxiwang@cluster.eng.uts.edu.au:~/Data/word_vec_s    tanford/&quot;</span></span><br><span class="line">1            <span class="built_in">echo</span> successfully transfer</span><br><span class="line">     <span class="keyword">fi</span></span><br><span class="line"><span class="keyword">done</span></span><br></pre></td></tr></table></figure>
<p>scp_expect.sh主要是为了避免每次输入密码</p>
<figure class="highlight bash"><table><tr><td class="code"><pre><span class="line"></span><br><span class="line"><span class="comment">#!/usr/bin/expect</span></span><br><span class="line"><span class="built_in">set</span> content [lindex <span class="variable">$argv</span> 0] <span class="comment">#将传入的第一个参数命名为content</span></span><br><span class="line"><span class="built_in">set</span> dest [lindex <span class="variable">$argv</span> 1] </span><br><span class="line">spawn scp <span class="variable">$content</span> <span class="variable">$dest</span></span><br><span class="line">expect <span class="string">&quot;*passward:&quot;</span></span><br><span class="line">send <span class="string">&quot;wang\$764113779\r&quot;</span></span><br><span class="line">expect eof</span><br></pre></td></tr></table></figure>
<p>注意要将scp_except.sh授权为可执行文件chmod +x scp_except.sh</p>
</li>
<li><p><strong>expect命令</strong><br>参考博客：<a href="https://blog.csdn.net/jxdl6655/article/details/78626749">https://blog.csdn.net/jxdl6655/article/details/78626749</a><br>关键字：</p>
<figure class="highlight bash"><table><tr><td class="code"><pre><span class="line">［<span class="comment">#!/usr/bin/expect］ 这一行告诉操作系统脚本里的代码使用那一个shell来执行。这里的expect其实和linux下的bash、windows下的cmd是一类东西。 注意：这一行需要在脚本的第一行。</span></span><br><span class="line"></span><br><span class="line">spawn              调用要执行的命令,相当于开启一个线程</span><br><span class="line">expect               只有spawn执行的命令的执行结果才被expect捕捉到，主要包括标准输入的提示信息、eof和timeout。等待命令提示信息的出现，也就是捕捉用户输入的提示：</span><br><span class="line">send                 发送需要交互的值，替代了用户手动输入内容</span><br><span class="line"><span class="built_in">set</span>                    设置变量值</span><br><span class="line">interact             执行完成后保持交互状态，把控制权交给控制台，这个时候就可以手工操作了。如果没有这一句登录完成后会退出，而不是留在远程终端上。</span><br><span class="line">expect eof        这个一定要加，与spawn对应表示捕获终端输出信息终止，类似于if....endif</span><br></pre></td></tr></table></figure>
<p>expect脚本必须以interact或expect eof结束，执行自动化任务通常expect eof就够了</p>
</li>
</ul>
]]></content>
  </entry>
  <entry>
    <title></title>
    <url>/2019/04/23/program_notes/</url>
    <content><![CDATA[<ul>
<li><p>**利用nltk记性词干提取 **<br>可利用WOrdNetLemmatizer实现，该实现的时候需要手动设置处理词的词性，否则会出现错误的提取，例如working，如果不指定词性，输出将会为working。所以具体做法为，现对词进行词性标注，将词性转换为wordnet的词性，传入WordNetLemmatizer中<br>词性函数定义：</p>
<figure class="highlight bash"><table><tr><td class="code"><pre><span class="line">from nltk.corpus import workout</span><br><span class="line">def get_wordnet_pos(treebank_tag):</span><br><span class="line">    <span class="keyword">if</span> treebank_tag.startswith(<span class="string">&#x27;J&#x27;</span>):</span><br><span class="line">        <span class="built_in">return</span> wordnet.ADJ</span><br><span class="line">    <span class="keyword">elif</span> treebank_tag.startswith(<span class="string">&#x27;V&#x27;</span>):</span><br><span class="line">        <span class="built_in">return</span> wordnet.VERB</span><br><span class="line">    <span class="keyword">elif</span> treebank_tag.startswith(<span class="string">&#x27;N&#x27;</span>):</span><br><span class="line">        <span class="built_in">return</span> wordnet.NOUN</span><br><span class="line">    <span class="keyword">elif</span> treebank_tag.startswith(<span class="string">&#x27;R&#x27;</span>):</span><br><span class="line">        <span class="built_in">return</span> wordnet.ADV</span><br><span class="line">    <span class="keyword">else</span>:</span><br><span class="line">        <span class="built_in">return</span> None</span><br></pre></td></tr></table></figure>
<p>传入词性进行单词还原：</p>
<figure class="highlight bash"><table><tr><td class="code"><pre><span class="line">import nltk</span><br><span class="line">from nltk.stem import WordNetLemmatizer</span><br><span class="line">test_word = <span class="string">&#x27;working&#x27;</span></span><br><span class="line">word, word_pos = nltk.pos_tag([test_word])</span><br><span class="line">wordnet_pos = get_wordnet_pos(word_pos) or word net.NOUN</span><br><span class="line">lemmatizer = WordNetLemmatizer()</span><br><span class="line">lemmatize.lemmatize(test_word, pos = wordnet_pos)<span class="comment">#the output will be working</span></span><br></pre></td></tr></table></figure></li>
<li><p>** numpy中拼接两个数组**</p>
<figure class="highlight bash"><table><tr><td class="code"><pre><span class="line">Np.vstack([a_vec, b_vec])<span class="comment">#纵向</span></span><br><span class="line">Np。hstack([a_vec, b_vec]<span class="comment">#横向</span></span><br></pre></td></tr></table></figure></li>
<li><p>**python多线程 **<br>参考网址：<a href="https://www.cnblogs.com/bethansy/p/7965820.html">https://www.cnblogs.com/bethansy/p/7965820.html</a><br>一个例子：</p>
<figure class="highlight bash"><table><tr><td class="code"><pre><span class="line">import threading</span><br><span class="line"></span><br><span class="line"><span class="comment">#申明类</span></span><br><span class="line">Class fea_read_load(threading.Thread):</span><br><span class="line">	def __init__(self,func, para):</span><br><span class="line">		threading.Thread.__init__(self)</span><br><span class="line">		self.para = para</span><br><span class="line">		self.func = func</span><br><span class="line">		self.result = self.func(self.para)</span><br><span class="line">	def get_result(self):</span><br><span class="line">		<span class="built_in">return</span> self.result</span><br><span class="line"></span><br><span class="line"><span class="comment">#调用过程</span></span><br><span class="line">def main():</span><br><span class="line">	<span class="keyword">for</span> query <span class="keyword">in</span> querys</span><br><span class="line">		fea_t = fea_read_load(load_vis_fea, query)<span class="comment">#其中laod_vis_fea是一个函数，query是参数</span></span><br><span class="line">		fea_t.start()<span class="comment">#开启线程</span></span><br><span class="line">		fea_t.join()<span class="comment">#等待线程结束</span></span><br><span class="line">		<span class="comment">#结束之后才能获取结果</span></span><br><span class="line">		fea_dict = fea_t.get_result()<span class="comment">#获得读区额特征字典</span></span><br></pre></td></tr></table></figure></li>
</ul>
]]></content>
  </entry>
  <entry>
    <title>bash_command.md</title>
    <url>/2019/04/18/bash-command/</url>
    <content><![CDATA[<ul>
<li><p>** 1 在当前文件夹及其子文件夹下查找文件 **</p>
<figure class="highlight bash"><table><tr><td class="code"><pre><span class="line">find . -<span class="built_in">type</span> f -name *vocab* </span><br></pre></td></tr></table></figure>
<p>上述的命令在当前文件夹及其子文件夹下查找包含vocab的文件<br>find 后面可以加目录，不写默认为当前目录<br>type换成d为查找目录</p>
</li>
<li><p>** 2 关于SCP命令 **<br>使用方式为：</p>
<figure class="highlight bash"><table><tr><td class="code"><pre><span class="line">scp local_file_dir usename@ip_address:remote_dir</span><br></pre></td></tr></table></figure>
<p>第一个参数为本地的文件名，第二个参数为 先输入用户名，@止呕输入IP地址或者url，远端的文件名<br>例如从本地mac往UTS机器上船文件</p>
<figure class="highlight bash"><table><tr><td class="code"><pre><span class="line">Scp vocabulary.json yaxiwang@cluster.eng.uts.edu.au:~/Data/</span><br></pre></td></tr></table></figure>
<p>如果传输的是文件夹，scp 后面加上-r选项</p>
</li>
</ul>
<p>从远端传输文件到本地也可以使用scp命令<br>例如：</p>
<figure class="highlight bash"><table><tr><td class="code"><pre><span class="line">scp yaxiwang@cluster.eng.uts.edu.au:~/Data/Flickr30k/data/f30k_precomp_vocab.json ./   </span><br></pre></td></tr></table></figure>
<p>如果路径没错还是报 no such file错误，尝试将～换成绝对路径如 /home/yaxiwang</p>
<ul>
<li><p>** 3。间隔指定时间将命令的执行结果写入到文件中 **<br>例如每隔2秒将内存的使用信息写入指定的文件</p>
<figure class="highlight bash"><table><tr><td class="code"><pre><span class="line">While <span class="literal">true</span></span><br><span class="line"><span class="keyword">do</span></span><br><span class="line">	free -h &gt; memory.out</span><br><span class="line">	sleep 1</span><br><span class="line"><span class="keyword">done</span>:</span><br></pre></td></tr></table></figure></li>
<li><p>** 查看当前用户的内存限制使用量 **</p>
<figure class="highlight bash"><table><tr><td class="code"><pre><span class="line"></span><br><span class="line"><span class="built_in">ulimit</span> -a</span><br></pre></td></tr></table></figure></li>
<li><p><strong>4 列出文件夹下的所有文件并按照从大到小排序</strong></p>
<figure class="highlight bash"><table><tr><td class="code"><pre><span class="line">du -sh * | sort -nr</span><br></pre></td></tr></table></figure>
<p>其中n表示按照数值大小进行排序，r表示反序即由大到小排序</p>
</li>
<li><p>** 5 xargs 命令相关 **<br>Xargs是给命令传递参数的过滤器，也是组合多个命令的一个工具<br>1）当你尝试删除太多文件，会得到一个错读：Argument list too long，可以使用xargs命令解决：</p>
<figure class="highlight bash"><table><tr><td class="code"><pre><span class="line">Find ./ -naem <span class="string">&#x27;*.log&#x27;</span> -print0 | xargs -0 rm -f</span><br></pre></td></tr></table></figure>
<p>这里print0是有必要的，xargs以空白字符来分割记录（空格，tag，换行等），如果文件名为‘file 1.log’，那么。xargs会解析为两个文件file 和1.log，但是这两个文件不存在，就会报错。print0在find命令打印文件名之后输出一个NULL，然后-0 告诉xargs用NULL来分割就不会报错了.<br>2)查找所有的jpg文件并且压缩</p>
<figure class="highlight bash"><table><tr><td class="code"><pre><span class="line">Find / -name *.jpg -<span class="built_in">type</span> f -<span class="built_in">print</span> | xargs tar -cvzf images.tar.gz</span><br></pre></td></tr></table></figure>

<p>3)拷贝所有图片到一个外部驱动</p>
<figure class="highlight bash"><table><tr><td class="code"><pre><span class="line">ls . *.jpg | xargs -n1 -i cp &#123;&#125; /external-hard_drive/dir</span><br></pre></td></tr></table></figure>
<p>这里n1表示每次传入的argument数量是1，i 参数表示使用前面的输出替换 {},<br>或者</p>
<figure class="highlight bash"><table><tr><td class="code"><pre><span class="line">ls . *.jpg | xargs -n1 -I &#123;&#125; cp &#123;&#125; /external-hard_driver/dir</span><br></pre></td></tr></table></figure>

<p>4)查找文件中是否按右某个单词test-word</p>
<figure class="highlight bash"><table><tr><td class="code"><pre><span class="line">Find . -<span class="built_in">type</span> f -<span class="built_in">print</span> | xargs grep <span class="string">&#x27;test-word&#x27;</span></span><br></pre></td></tr></table></figure></li>
<li><p>**tmux 常用命令 **<br>1）新建与杀死会话</p>
<figure class="highlight bash"><table><tr><td class="code"><pre><span class="line">tmux new -s session</span><br><span class="line">Thus kill-session -t 会话名称</span><br></pre></td></tr></table></figure>
<p>2）会话相关操作</p>
<figure class="highlight bash"><table><tr><td class="code"><pre><span class="line">tmux ls <span class="comment">#列出所有会话 第一列名称，第二列窗口编号</span></span><br><span class="line">tmux a <span class="comment">#进入最近的会话</span></span><br><span class="line">Tmux a -t 会话名 <span class="comment">#进入指定的会话</span></span><br><span class="line">Windows/<span class="built_in">command</span> 键 + 上/下箭头 <span class="comment">#上/下翻</span></span><br></pre></td></tr></table></figure>

<p>3）tmux命令模式<br>利用control + b进入tmux会话模式，此模式的响应输入命令有：</p>
<figure class="highlight bash"><table><tr><td class="code"><pre><span class="line">c <span class="comment">#创建新窗口,</span></span><br><span class="line">p <span class="comment">#切换到上一个window</span></span><br><span class="line">n <span class="comment">#切换到下一个window</span></span><br><span class="line">w <span class="comment">#查看所有窗口</span></span><br><span class="line">“ <span class="comment">#横分当前面板</span></span><br><span class="line">% <span class="comment">#竖分当前面板</span></span><br><span class="line">方向键/o <span class="comment">#在分割的面板之间切换, 安住ctrl + b o，切换面板</span></span><br><span class="line">, <span class="comment">#修改窗口名称</span></span><br><span class="line">？<span class="comment">#查看所有命令</span></span><br><span class="line">d <span class="comment">#刮起当前会话</span></span><br><span class="line">s <span class="comment">#查看所有会话</span></span><br><span class="line">$ <span class="comment">#修改会话名称</span></span><br><span class="line">q <span class="comment">#显示面板号，快速输入面板号可以选择相应面板</span></span><br><span class="line">&amp; <span class="comment">#关闭当前窗口</span></span><br><span class="line">x <span class="comment">#关闭当前面板</span></span><br><span class="line">f <span class="comment">#在当前窗口打开的所有文件中进行搜索</span></span><br><span class="line">d <span class="comment">#暂时离开当前session</span></span><br><span class="line">; <span class="comment">#切换到最后一个使用的面板</span></span><br><span class="line">l <span class="comment">#切换到最后一个使用的窗口</span></span><br><span class="line">z <span class="comment">#在最大化和初始之前切换  </span></span><br></pre></td></tr></table></figure>
<p> bash界面中上下翻页：ctrl + a pageUp pageDown<br> 有时候，外接键盘的fn+方向键上下翻页不管用，解决方案：<br> 先ctrl+q, 按【进入拷贝模式，之后按page up/down去翻页</p>
</li>
</ul>
<p> 具体在使用时，创建一个session，记住名称，在退出退出执行时，ctrl +b -》d提交到后台，重新连接时，tmux a -t session名称/tmux a进入最近的会话</p>
<ul>
<li>** vim中统计字符串出现的次数**<figure class="highlight bash"><table><tr><td class="code"><pre><span class="line">:m，ns/\&lt;string\&gt;//gn<span class="comment">#统计m-n行中字符串string出现的次数</span></span><br><span class="line">:%s/string//gn<span class="comment">#统计当前编辑的文件中string出现的次数</span></span><br></pre></td></tr></table></figure>
字符串替换<figure class="highlight bash"><table><tr><td class="code"><pre><span class="line">:%s/old/new/g <span class="comment">#直接将旧字符串替换为新字符串</span></span><br><span class="line">:%s/old/new/gc <span class="comment">#替换每次需要你确认</span></span><br></pre></td></tr></table></figure>
统一缩进多行：<figure class="highlight bash"><table><tr><td class="code"><pre><span class="line">:line_num1, line_num2 &lt;/&gt; <span class="comment">#按下enter即可向左/右缩进一个tab</span></span><br><span class="line"><span class="comment">#第二中方法</span></span><br><span class="line">进入visual模式，选中要缩进的行<span class="built_in">shift</span> + &lt;/&gt; 向左/右缩进一个tab</span><br><span class="line"></span><br><span class="line"><span class="comment">#多窗口编辑</span></span><br><span class="line">Ctrl w + | <span class="comment">#水平最大化当前窗口</span></span><br><span class="line">Ctrl w + _ <span class="comment">#垂直最大化当前窗口</span></span><br><span class="line">Ctrl w + = <span class="comment">#将所有窗口设置为同等大小</span></span><br></pre></td></tr></table></figure></li>
</ul>
<p> 或者cat命令也可<br>  <figure class="highlight bash"><table><tr><td class="code"><pre><span class="line">Cat file | grep -I 字符串 | wc -l</span><br><span class="line">grep (-rinc) 字符串（可以是正则表达式） filename <span class="comment">#查找包含字符串的行</span></span><br></pre></td></tr></table></figure><br>  参数n表示输出结果并显示行号，c表示统计结果的出现次数， r表示在目录下递归查找，使用此参数，filename需要换成目录， i表示忽略大小写</p>
<ul>
<li><p>**是用grep查找文件夹下包含指定字符串的文件 **</p>
<figure class="highlight bash"><table><tr><td class="code"><pre><span class="line">grep <span class="string">&#x27;字符串&#x27;</span> ./ -Rnl <span class="comment">#参数R表示便利所有子目录，n显示匹配的行号，l表示显示文件名称，加会覆盖n的效果不会显示行，只会显示文件名称，所以有l参数时可以去掉n</span></span><br><span class="line">grep -v <span class="string">&#x27;字符串&#x27;</span> ./ -Rl <span class="comment"># 表示不包含字符串的文件</span></span><br><span class="line"><span class="comment">#查找多个字符串</span></span><br><span class="line">grep <span class="string">&#x27;字符串1&#x27;</span> ./ -R | grep <span class="string">&#x27;字符串2&#x27;</span> ./ -Rl</span><br></pre></td></tr></table></figure>
<p>参数也可以放在grep命令后边</p>
</li>
<li><p>**vim使用indent代码折叠 **<br>一下快捷键在缩进处使用</p>
<figure class="highlight bash"><table><tr><td class="code"><pre><span class="line">zc <span class="comment">#折叠</span></span><br><span class="line">zC <span class="comment">#对所在范围内的嵌套的折叠点进行折叠</span></span><br><span class="line">zo <span class="comment">#展开折叠</span></span><br><span class="line">zO <span class="comment">#对所在范围内的嵌套的折叠点进行展开</span></span><br><span class="line">[z <span class="comment">#到当前折叠点的开始出</span></span><br><span class="line"> z] <span class="comment">#到当前折叠点的开始出</span></span><br><span class="line">zj <span class="comment">#向下移动到下一个折叠处的开始</span></span><br><span class="line">zk <span class="comment">#向上移动前一折叠的结束</span></span><br><span class="line">za <span class="comment">#折叠打开和关闭之间切换</span></span><br><span class="line">zM <span class="comment">#全文折叠</span></span><br><span class="line">zR <span class="comment">#全部打开折叠</span></span><br><span class="line">zd <span class="comment">#删除光标下的折叠</span></span><br><span class="line">zD <span class="comment">#删除光标下的折叠以及嵌套的折叠</span></span><br></pre></td></tr></table></figure></li>
<li><p>**文件查看命令 **<br>查看有多少行数据：</p>
<figure class="highlight bash"><table><tr><td class="code"><pre><span class="line">wc -lw filename <span class="comment">#查看文件有多少行（l）和字数（w）</span></span><br></pre></td></tr></table></figure>
<p>查看文件前几行/后几行</p>
<figure class="highlight bash"><table><tr><td class="code"><pre><span class="line"> head -n line_num file_name <span class="comment">#查看文件的前几行</span></span><br><span class="line"> tail -n line_num file_name <span class="comment">#查看文件的后几行</span></span><br><span class="line"> Ls -Slh | head -n 5 <span class="comment">#只显示结果的前5个</span></span><br><span class="line">Grep <span class="string">&#x27;Image to text&#x27;</span> train.log | head -n 4 <span class="comment">#显示前4个带有‘Image to text’字符串的行</span></span><br></pre></td></tr></table></figure></li>
<li><p>**远程登录脚本 **<br>在ssh脚本登录的时候，避免每次输入密码</p>
<figure class="highlight bash"><table><tr><td class="code"><pre><span class="line"><span class="meta">#!/usr/bin/expect</span></span><br><span class="line"> <span class="built_in">set</span> timeout 30</span><br><span class="line"> spawn ssh -l username ip_adress</span><br><span class="line"> expect <span class="string">&quot;*password:&quot;</span></span><br><span class="line"> send <span class="string">&quot;your password&quot;</span></span><br><span class="line"> interact</span><br></pre></td></tr></table></figure>
<p>写完之后保存为文件，注意第一行。将改文件授可执行权限</p>
<figure class="highlight bash"><table><tr><td class="code"><pre><span class="line">chmod +x filename.sh</span><br></pre></td></tr></table></figure>
<p>之后执行</p>
<figure class="highlight bash"><table><tr><td class="code"><pre><span class="line">./filename.sh</span><br></pre></td></tr></table></figure>
<p>注意不要使用sh filename.sh进行运行，否则会报错</p>
</li>
</ul>
<ul>
<li>**下载数据好用多线程工具 **<br>工具1：aria2<br>安装：<figure class="highlight plaintext"><table><tr><td class="code"><pre><span class="line">brew install aria2 #for Mac</span><br><span class="line">sudo apt-get install aria2 #for ubuntu</span><br></pre></td></tr></table></figure>
断点续传下载数据：<figure class="highlight bash"><table><tr><td class="code"><pre><span class="line">aria2c -c url <span class="comment">#-c表示断点续传, 注意是aria2c</span></span><br></pre></td></tr></table></figure></li>
</ul>
<p>工具2: axel<br>Linux下多线程下载工具</p>
<figure class="highlight bash"><table><tr><td class="code"><pre><span class="line">sudo apt-get install axel</span><br></pre></td></tr></table></figure>
<p>使用方法：<br>axel [options] url1 url2 …<br>常用的选项：</p>
<figure class="highlight bash"><table><tr><td class="code"><pre><span class="line">--max-speed=2 / -s x <span class="comment">#指定最大的下载速度，单位为bite</span></span><br><span class="line">--num-connections=x / -n x <span class="comment">#指定最大的链接线程数为axel</span></span><br><span class="line">--output=x / -o x <span class="comment">#下载下来的文件重新命名为x</span></span><br><span class="line">--search=x / -s=x <span class="comment">#使用文件搜索引擎来找找文件镜像 （不太懂）</span></span><br><span class="line">--no-proxy / -N <span class="comment">#不使用代理服务器下载文件</span></span><br><span class="line">--verbose  <span class="comment">#输出更多的状态信息</span></span><br><span class="line">--quiet -q <span class="comment">#不想标准输出输输出信息</span></span><br><span class="line">--header=x / -H x <span class="comment">#添加HTTP的头域</span></span><br><span class="line">--alternate -a <span class="comment">#显示进度条</span></span><br><span class="line">--user-agent=x / -U x <span class="comment">#指定user agent</span></span><br><span class="line">--<span class="built_in">help</span> -h <span class="comment">#显示一些帮助信息</span></span><br><span class="line">--version / -V <span class="comment">#显示版本信息</span></span><br></pre></td></tr></table></figure>

<ul>
<li>**wget 下载文件断点续传 **<figure class="highlight bash"><table><tr><td class="code"><pre><span class="line">wget -c -t 0 -O new_name.tar.gz http://www.cnscn.org/old_name.tar.gz </span><br></pre></td></tr></table></figure></li>
</ul>
<p> -c 断点续传<br> -t 链接断开重试次数， 0为不限次数<br> -O 重命名文件<br>Wget 命令的其他参数</p>
<figure class="highlight bash"><table><tr><td class="code"><pre><span class="line">wget [参数列表] [目标软件、网页的网址]</span><br><span class="line"></span><br><span class="line">-V,–version 显示软件版本号然后退出；</span><br><span class="line">-h,–<span class="built_in">help</span>显示软件帮助信息；</span><br><span class="line">-e,–execute=COMMAND 执行一个 “.wgetrc”命令</span><br><span class="line"></span><br><span class="line">-o,–output-file=FILE 将软件输出信息保存到文件；</span><br><span class="line">-a,–append-output=FILE将软件输出信息追加到文件；</span><br><span class="line">-d,–debug显示输出信息；</span><br><span class="line">-q,–quiet 不显示输出信息；</span><br><span class="line">-i,–input-file=FILE 从文件中取得URL；</span><br><span class="line"></span><br><span class="line">-t,–tries=NUMBER 是否下载次数（0表示无穷次）</span><br><span class="line">-O –output-document=FILE下载文件保存为别的文件名</span><br><span class="line">-nc, –no-clobber 不要覆盖已经存在的文件</span><br><span class="line">-N,–timestamping只下载比本地新的文件</span><br><span class="line">-T,–timeout=SECONDS 设置超时时间</span><br><span class="line">-Y,–proxy=on/off 关闭代理</span><br><span class="line"></span><br><span class="line">-nd,–no-directories 不建立目录</span><br><span class="line">-x,–force-directories 强制建立目录</span><br><span class="line"></span><br><span class="line">–http-user=USER设置HTTP用户</span><br><span class="line">–http-passwd=PASS设置HTTP密码</span><br><span class="line">–proxy-user=USER设置代理用户</span><br><span class="line">–proxy-passwd=PASS设置代理密码</span><br><span class="line"></span><br><span class="line">-r,–recursive 下载整个网站、目录（小心使用）</span><br><span class="line">-l,–level=NUMBER 下载层次</span><br><span class="line"></span><br><span class="line">-A,–accept=LIST 可以接受的文件类型</span><br><span class="line">-R,–reject=LIST拒绝接受的文件类型</span><br><span class="line">-D,–domains=LIST可以接受的域名</span><br><span class="line">–exclude-domains=LIST拒绝的域名</span><br><span class="line">-L,–relative 下载关联链接</span><br><span class="line">–follow-ftp 只下载FTP链接</span><br><span class="line">-H,–span-hosts 可以下载外面的主机</span><br><span class="line">-I,–include-directories=LIST允许的目录</span><br><span class="line">-X,–exclude-directories=LIST 拒绝的目录</span><br><span class="line"></span><br><span class="line">原文：https://blog.csdn.net/richardysteven/article/details/4565931 </span><br></pre></td></tr></table></figure>

<ul>
<li><strong>docker相关命令</strong><figure class="highlight bash"><table><tr><td class="code"><pre><span class="line">pytorch数据交互准备-挂载</span><br><span class="line">sudo nvidia-docker run --ipc=host -it -v /media/tensorflow/1412C30112C2E738:/home/wangyx --name wangyx_docker ufoym/deepo bash</span><br><span class="line">or</span><br><span class="line">sudo nvidia-docker run -it -v --shm-size /home/smiles/jhy:/home/jhy -name jhy_deepo ufoym/deepo bash</span><br><span class="line"></span><br><span class="line">挂载</span><br><span class="line">sudo nvidia-docker run -it -v /media/smiles/OS1/jhy/:/home/jhy --name jhy_deepo d35aa33f77cb bash</span><br><span class="line">启动</span><br><span class="line">sudo nvidia-docker start -i jhy_deepo</span><br><span class="line">删除</span><br><span class="line">sudo docker container rm jhy_deepo / docker rm 容器id（docker ps -a查看容器id，如果docker正在运行，加上-f选项参数，强制移除）</span><br><span class="line">查看容器</span><br><span class="line">sudo docker ps -a</span><br><span class="line"></span><br><span class="line">查看本地有哪些可用的镜像</span><br><span class="line">docker images</span><br><span class="line"></span><br><span class="line">在HUB中搜索可用的镜像</span><br><span class="line">docker search image_name (caffe <span class="keyword">for</span> example)</span><br><span class="line"></span><br><span class="line">拉取HUB中的镜像到本地</span><br><span class="line">docker pull image_name (mtngld/caffe-gpu, <span class="keyword">for</span> example)</span><br><span class="line">Docker查看要拉取的镜像版本：https://hub.docker.com/r/mapler/caffe-py3/tags?page=1&amp;ordering=last_updated</span><br><span class="line"></span><br><span class="line">重新开启一个容器，与原先不同步：</span><br><span class="line">nvidia-docker <span class="built_in">exec</span> -it wangyx /bin/bash/ </span><br></pre></td></tr></table></figure>
cp resnet50-19c8e357.pth /root/.torch/models<br>/media/tensorflow/1412C30112C2E738</li>
</ul>
<p>my environment name: wangyx_docker (sudo nvidia-docker start -i wangyx_docker) </p>
<ul>
<li><strong>docker运行设置设置共享内存</strong><br>如果共享内存不足，将会出现错误：dataloder worker exited unexpectedly，或者调小batchsize，或者重新进入docker使用上面的参数<br>解决办法只能是重新新建容器，因为默认容器的共享内存是64M<figure class="highlight bash"><table><tr><td class="code"><pre><span class="line">nvidia-docker run -itd --shm-size 64G --name wangyx -v /data1/wangyx:/home/wangyx -p 3316:22 pytorch/pytorch(本地镜像：docker images查看本地存在哪些镜像)</span><br></pre></td></tr></table></figure></li>
</ul>
<p>八卡服务器连接：<br>ssh <a href="mailto:&#x77;&#97;&#x6e;&#x67;&#x79;&#x78;&#64;&#49;&#57;&#50;&#x2e;&#x31;&#54;&#56;&#46;&#x31;&#48;&#48;&#46;&#50;&#50;">&#x77;&#97;&#x6e;&#x67;&#x79;&#x78;&#64;&#49;&#57;&#50;&#x2e;&#x31;&#54;&#56;&#46;&#x31;&#48;&#48;&#46;&#50;&#50;</a><br>psw:gansu320</p>
<p>容器名称：wangyx<br>启动：nvidia-docker start -i wangyx<br>退出：ctrl+P+Q 退出但不结束 ctrl+D / exit 退出并关闭</p>
<ul>
<li><p>**zip命令解压文件到指定文件 **</p>
<figure class="highlight bash"><table><tr><td class="code"><pre><span class="line">unzip -d dir file.zip</span><br></pre></td></tr></table></figure>
<p>tar文件解压到指定文件夹</p>
<figure class="highlight bash"><table><tr><td class="code"><pre><span class="line">tar -zxvf ./tarfile.tar -C adir</span><br><span class="line">tar -zcvf ./tarfilename.tar ./adir <span class="comment"># 压缩</span></span><br><span class="line"></span><br><span class="line"><span class="comment">#-----------------------------------</span></span><br><span class="line">tar 解压缩命令 </span><br><span class="line">tar </span><br><span class="line">-c: 建立压缩档案 </span><br><span class="line">-x：解压 </span><br><span class="line">-t：查看内容 </span><br><span class="line">-r：向压缩归档文件末尾追加文件 </span><br><span class="line">-u：更新原压缩包中的文件 </span><br><span class="line">这五个是独立的命令，压缩解压都要用到其中一个，可以和别的命令连用但只能用其中一个。下面的参数是根据需要在压缩或解压档案时可选的。 </span><br><span class="line">-z：有gzip属性的 </span><br><span class="line">-j：有bz2属性的 </span><br><span class="line">-Z：有compress属性的 </span><br><span class="line">-v：显示所有过程 </span><br><span class="line">-f 指定压缩文件文件名称</span><br><span class="line">-O：将文件解开到标准输</span><br></pre></td></tr></table></figure></li>
<li><p>**python调试 **<br>倒入pdb之后去调试可能更加方便</p>
<figure class="highlight bash"><table><tr><td class="code"><pre><span class="line">import pub</span><br><span class="line"></span><br><span class="line"><span class="comment">#在任何想调试的地方</span></span><br><span class="line">pdb.set_trace()</span><br></pre></td></tr></table></figure>
<p>代码运行到trace处自动进入调试状态，pdb调试命令即可使用,所以不用从程序入口开始调试</p>
</li>
<li><p>**tmux设置快捷键为ctrl+a **</p>
<figure class="highlight bash"><table><tr><td class="code"><pre><span class="line">vim ~/.tmux.conf</span><br><span class="line"><span class="comment">#输入</span></span><br><span class="line">unbind C-b</span><br><span class="line"><span class="built_in">set</span> -g prefix C-a</span><br><span class="line"> </span><br><span class="line"><span class="built_in">bind</span> C-a send-prefix</span><br><span class="line"> </span><br><span class="line"><span class="built_in">bind</span> r source-file ~/.tmux.conf</span><br></pre></td></tr></table></figure>
<p>保存退出即可生效</p>
</li>
<li><p>**vim常用设置 **</p>
<figure class="highlight bash"><table><tr><td class="code"><pre><span class="line"><span class="built_in">set</span> nocompatible</span><br><span class="line"><span class="built_in">set</span> number</span><br><span class="line">syntax on</span><br><span class="line"><span class="built_in">set</span> autoindent</span><br><span class="line"><span class="built_in">set</span> smartindent</span><br><span class="line"><span class="built_in">set</span> tabstop=4</span><br><span class="line"><span class="built_in">set</span> shiftwidth=4</span><br><span class="line"><span class="built_in">set</span> hlsearch</span><br><span class="line"></span><br><span class="line"><span class="built_in">set</span> showmatch</span><br><span class="line">filetype on</span><br><span class="line"><span class="built_in">set</span> ruler</span><br><span class="line"></span><br><span class="line"><span class="built_in">set</span> incsearch</span><br><span class="line"><span class="built_in">set</span> cursorcolumn</span><br><span class="line">hi cursorcolumn cterm=none ctermbg=black ctermfg=green</span><br><span class="line">autocmd WinEnter * setlocal cursorline</span><br><span class="line">autocmd WinLeave * setlocal nocursorline</span><br><span class="line">highlight CursorColumn guibg=<span class="comment">#303000 ctermbg=10</span></span><br><span class="line"><span class="built_in">set</span> cursorline</span><br><span class="line"><span class="built_in">set</span> scrolloff=5</span><br><span class="line">hi cursorline cterm=none term=none</span><br><span class="line">autocmd WinEnter * setlocal cursorline</span><br><span class="line">autocmd WinLeave * setlocal nocursorline</span><br><span class="line">highlight CursorLine guibg=<span class="comment">#303000 ctermfg=White ctermbg=Black</span></span><br><span class="line">hi Search term=standout ctermfg=0 ctermbg=3</span><br><span class="line"></span><br><span class="line"><span class="built_in">set</span> nocompatible       <span class="string">&quot; be iMproved, required</span></span><br><span class="line"><span class="string">filetype off         &quot;</span> required</span><br><span class="line"></span><br><span class="line"><span class="string">&quot; set the runtime path to include Vundle and initialize</span></span><br><span class="line"><span class="string">set rtp+=~/.vim/bundle/Vundle.vim</span></span><br><span class="line"><span class="string">call vundle#begin()</span></span><br><span class="line"><span class="string">&quot;</span> <span class="built_in">let</span> Vundle manage Vundle, required</span><br><span class="line">Plugin <span class="string">&#x27;VundleVim/Vundle.vim&#x27;</span></span><br><span class="line">Plugin <span class="string">&#x27;Valloric/YCM&#x27;</span></span><br><span class="line">Plugin <span class="string">&#x27;skywind3000/asyncrun&#x27;</span></span><br><span class="line">Plugin <span class="string">&#x27;vim-scripts/zoomwin&#x27;</span></span><br><span class="line"><span class="string">&quot; Keep Plugin commands between vundle#begin/end.</span></span><br><span class="line"><span class="string">&quot;</span> 安装其它插件</span><br><span class="line"><span class="string">&quot; All of your Plugins must be added before the following line</span></span><br><span class="line"><span class="string">Plugin &#x27;preservim/nerdtree&#x27;</span></span><br><span class="line"><span class="string">call vundle#end()      &quot;</span> required</span><br><span class="line">filetype plugin indent on  <span class="string">&quot; required</span></span><br><span class="line"><span class="string"></span></span><br><span class="line"><span class="string">let g:ycm_global_ycm_extra_conf = &#x27;/root/.vim/bundle/YCM/.ycm_extra_conf.py&#x27;</span></span><br><span class="line"><span class="string">&quot;</span>开启基于tag的补全，可以在这之后添加需要的标签路径</span><br><span class="line"><span class="string">&quot;&quot;</span><span class="built_in">let</span> g:ycm_collect_identifiers_from_tags_files = 1</span><br><span class="line"><span class="string">&quot;&quot;</span>开启语义补全</span><br><span class="line"><span class="built_in">let</span> g:ycm_seed_identifiers_with_syntax = 1</span><br><span class="line"><span class="string">&quot;在接受补全后不分裂出一个窗口显示接受的项</span></span><br><span class="line"><span class="string">&quot;</span><span class="string">&quot;set completeopt-=preview</span></span><br><span class="line"><span class="string">&quot;</span><span class="string">&quot;不显示开启vim时检查ycm_extra_conf文件的信息</span></span><br><span class="line"><span class="string">let g:ycm_confirm_extra_conf=0</span></span><br><span class="line"><span class="string">&quot;</span>每次重新生成匹配项，禁止缓存匹配项</span><br><span class="line"><span class="string">&quot;&quot;</span><span class="built_in">let</span> g:ycm_cache_omnifunc=0</span><br><span class="line"><span class="string">&quot;&quot;</span>在注释中也可以补全</span><br><span class="line"><span class="built_in">let</span> g:ycm_complete_in_comments=1</span><br><span class="line"><span class="string">&quot;输入第一个字符就开始补全</span></span><br><span class="line"><span class="string">&quot;</span><span class="string">&quot;let g:ycm_min_num_of_chars_for_completion=1</span></span><br><span class="line"><span class="string">&quot;</span><span class="string">&quot;不查询ultisnips提供的代码模板补全，如果需要，设置成1即可</span></span><br><span class="line"><span class="string">let g:ycm_use_ultisnips_completer=0</span></span><br><span class="line"><span class="string"></span></span><br><span class="line"><span class="string">let g:ycm_show_diagnostics_ui = 0</span></span><br><span class="line"><span class="string">let g:ycm_server_log_level = &#x27;info&#x27;</span></span><br><span class="line"><span class="string">let g:ycm_min_num_identifier_candidate_chars = 2</span></span><br><span class="line"><span class="string">let g:ycm_collect_identifiers_from_comments_and_strings = 1</span></span><br><span class="line"><span class="string">let g:ycm_complete_in_strings=1</span></span><br><span class="line"><span class="string">let g:ycm_key_invoke_completion = &#x27;&lt;c-z&gt;&#x27;</span></span><br><span class="line"><span class="string"></span></span><br><span class="line"><span class="string">noremap &lt;c-z&gt; &lt;NOP&gt;</span></span><br><span class="line"><span class="string"></span></span><br><span class="line"><span class="string">&quot;</span><span class="built_in">let</span> g:ycm_semantic_triggers =  &#123;</span><br><span class="line"><span class="string">&quot;                   \ &#x27;c,cpp,python,java,go,erlang,perl&#x27;: [&#x27;re!\w&#123;2&#125;&#x27;],</span></span><br><span class="line"><span class="string">&quot;</span>            \ <span class="string">&#x27;cs,lua,javascript&#x27;</span>: [<span class="string">&#x27;re!\w&#123;2&#125;&#x27;</span>],</span><br><span class="line"><span class="string">&quot;            \ &#125;</span></span><br><span class="line"><span class="string"></span></span><br><span class="line"><span class="string">map &lt;F3&gt; :NERDTree&lt;cr&gt;</span></span><br><span class="line"><span class="string">map &lt;F2&gt; :AsyncRun</span></span><br><span class="line"><span class="string">map &lt;F1&gt; :copen&lt;cr&gt;</span></span><br><span class="line"><span class="string">map &lt;F4&gt; :ZoomWin&lt;cr&gt;</span></span><br><span class="line"><span class="string"></span></span><br><span class="line"><span class="string">##与YCM等的安装配合使用</span></span><br><span class="line"><span class="string">#---------------------------------------------------------</span></span><br><span class="line"><span class="string">#使用map设置快捷键</span></span><br><span class="line"><span class="string">map &lt;F8&gt; gt #f8映射到gt， 打开的多标签中进入下一个标签</span></span><br><span class="line"><span class="string">map &lt;F7&gt; gT #f7映射为gT，打开的多标签汇总进入上一个标签</span></span><br><span class="line"><span class="string">map n :bn&lt;cr&gt; #n映射为normal模式下的&quot;</span>:bn+enter<span class="string">&quot;，多文件中操作红移动到下一个</span></span><br><span class="line"><span class="string">Map p :bp&lt;cr&gt; #p映射为normal模式下的 “:bp+enter”,多文件操作中的移动到上一个文件</span></span><br><span class="line"><span class="string">Unman gt #取消对gt的绑定</span></span><br><span class="line"><span class="string"></span></span><br><span class="line"><span class="string">&lt;k0&gt; - &lt;k9&gt; 小键盘 0 到 9 </span></span><br><span class="line"><span class="string">&lt;S-...&gt; Shift＋键 </span></span><br><span class="line"><span class="string">&lt;C-...&gt; Control＋键 </span></span><br><span class="line"><span class="string">&lt;M-...&gt; Alt＋键 或 meta＋键 </span></span><br><span class="line"><span class="string">&lt;A-...&gt; 同 &lt;M-...&gt; </span></span><br><span class="line"><span class="string">&lt;Esc&gt; Escape 键 </span></span><br><span class="line"><span class="string">&lt;Up&gt; 光标上移键 </span></span><br><span class="line"><span class="string">&lt;Space&gt; 插入空格 </span></span><br><span class="line"><span class="string">&lt;Tab&gt; 插入Tab </span></span><br><span class="line"><span class="string">&lt;CR&gt; 等于&lt;Enter&gt;</span></span><br></pre></td></tr></table></figure></li>
<li><p><strong>from scipy.misc import imead错误No module named imread</strong><br>如果是python2 安装PIL python3 安装pillow，如果都做了还是不行，可能是pillow 和scipy的版本不一致导致<br>解决方案：卸载当前的scipy和pillow，重新安装</p>
<figure class="highlight bash"><table><tr><td class="code"><pre><span class="line">Pip install scipy==1.1.0</span><br><span class="line">Pip install pillow==6.0.0</span><br></pre></td></tr></table></figure></li>
<li><p>**bash中的日常使用的快捷键 **</p>
<figure class="highlight bash"><table><tr><td class="code"><pre><span class="line">ctrl+w 删除最后一个词</span><br><span class="line">Ctrl + xx 在行首和光标之间快速切换</span><br><span class="line">ctrl + y 粘贴之间的删除的东西</span><br><span class="line">ctrl+u 删除从光标处到行开头的内容</span><br><span class="line">ctrl+k 删除光标到行末尾的内容</span><br><span class="line">ctrl+e 定位光标到末尾</span><br><span class="line">ctrl+a 定位光标到开头</span><br><span class="line"></span><br><span class="line">Alt + 方向左右键 向/前后移动一个字</span><br><span class="line">ctrl + _ 撤销之间的操作</span><br><span class="line">alt+. 环形遍历之前的命令</span><br><span class="line">alt+<span class="comment"># 另当前的行失效，将跳过键入的命令，让你重新输入</span></span><br><span class="line"></span><br><span class="line"></span><br><span class="line"><span class="built_in">history</span> 查看历史命令</span><br><span class="line">!! 运行上一个命令</span><br><span class="line"><span class="built_in">history</span> | grep your_prefix</span><br><span class="line">会出来历史命令序号</span><br><span class="line">!!序号  <span class="comment">#可以重新运行代码</span></span><br><span class="line"></span><br><span class="line">w/uptime  查看机器u已经运行了多少天</span><br><span class="line">～/.bash_profile 其中可以写当你登录机器的时候要运行的命令，如希望登录的时候链接某个docker，则写入Nvidia-docker attach wangyfian2</span><br><span class="line"><span class="built_in">alias</span> 使用其创建快捷键，例如list ll=<span class="string">&#x27;ls -latr&#x27;</span></span><br><span class="line"><span class="comment">#自定义传入参数</span></span><br><span class="line"><span class="built_in">alias</span> hist=<span class="string">&#x27;history | grep $1&#x27;</span> <span class="comment">#~/.bash_profile, $1表示第一个参数</span></span><br></pre></td></tr></table></figure></li>
<li><p><strong>tmate神器 告别vpn</strong><br>使用tmate可以基于密匙认证，链接内网vpn，以mac和在内网中的服务器为例<br>step 1:在Mac上安装tmate</p>
<figure class="highlight bash"><table><tr><td class="code"><pre><span class="line">Brew install tmate</span><br><span class="line"></span><br></pre></td></tr></table></figure>
<p>Step 2:生成密匙文件</p>
<figure class="highlight bash"><table><tr><td class="code"><pre><span class="line">Ssh-keygen -t rsa <span class="comment">#一路按enter 生成文件id_rsa （～/.ssh/id_rsa）</span></span><br></pre></td></tr></table></figure>
<p>Step 3:在Linux服务器上记录step2密匙</p>
<figure class="highlight bash"><table><tr><td class="code"><pre><span class="line"><span class="comment">#新建 /root/.ssh/authorized_keys</span></span><br><span class="line">将step2中的id-rsa文件中的所有内容粘贴到 authorized_keys中并保存</span><br></pre></td></tr></table></figure>
<p>Step4:服务器授权<br>在Linux服务器上为文件夹./ssh和文件authorized_keys改变权限</p>
<figure class="highlight bash"><table><tr><td class="code"><pre><span class="line">Chmod 600 /root/.ssh/authorized_keys</span><br><span class="line">Chmod 700 /root/.ssh</span><br></pre></td></tr></table></figure>
<p>step 5:链接<br>在Linux服务器上运行tmate：</p>
<figure class="highlight bash"><table><tr><td class="code"><pre><span class="line">tmate</span><br></pre></td></tr></table></figure>
<p>开出回话，将首先链接tmate服务器，之后会在屏幕下方显示tmate id （形如ssh adfadfdfasf），复制改id，在Mac的bash中输入ssh + id即可链接(如果太快没有复制到，用tmate show-messages即可出现负值@tmate对应的命令)</p>
</li>
</ul>
<p>可能的错误</p>
<figure class="highlight bash"><table><tr><td class="code"><pre><span class="line">Public key (permission denied)</span><br></pre></td></tr></table></figure>
<p>将上述机器位置对调再做同样的过程（成功案例是将服务器和mac上都进行了操作）</p>
<ul>
<li><p>**在vim中空格与tab键的替换 **<br>某些时候vim中空格和tab并不匹配，比如文件中是四个空格表示缩进，而你想要是一个tab为四空格缩进，这种情况下需要相互的转化<br>四个空格转换为一个tab,在需要转换的文件中依次输入：</p>
<figure class="highlight bash"><table><tr><td class="code"><pre><span class="line">:<span class="built_in">set</span> ts=4</span><br><span class="line">:<span class="built_in">set</span> noexpandtab</span><br><span class="line">:%retab!</span><br></pre></td></tr></table></figure>
<p>一个tab转换为四个空格类似</p>
<figure class="highlight bash"><table><tr><td class="code"><pre><span class="line">:<span class="built_in">set</span> ts=4</span><br><span class="line">:<span class="built_in">set</span> expand tab</span><br><span class="line">:%retab!</span><br></pre></td></tr></table></figure></li>
<li><p>**python设置输出提示信息 **</p>
<figure class="highlight bash"><table><tr><td class="code"><pre><span class="line">Import os</span><br><span class="line"></span><br><span class="line">Os.environ[<span class="string">&#x27;TF_CPP_MIN_LOG_LEVEL&#x27;</span>] = <span class="string">&#x27;3&#x27;</span></span><br><span class="line">0---输出所有信息</span><br><span class="line">1---屏蔽通知信息</span><br><span class="line">2---屏蔽通知信息和警告信息</span><br><span class="line">3---屏蔽通知信息，警告信息和报错信息</span><br></pre></td></tr></table></figure></li>
<li><p>**调用numpy函数出现错误：segmentation error （core dump） **<br>检查numpy版本，例如升级numpy版本</p>
<figure class="highlight bash"><table><tr><td class="code"><pre><span class="line">pip install numpy --upgrade</span><br></pre></td></tr></table></figure></li>
<li><p>**nvidia-smi not found, 但是程序可以使用gpu **<br>路径未配置好,安装的驱动在/usr/local/nvidia下，配置path变量</p>
<figure class="highlight bash"><table><tr><td class="code"><pre><span class="line">Export PATH=/usr/<span class="built_in">local</span>/nvidia/bin:<span class="variable">$PATH</span></span><br></pre></td></tr></table></figure></li>
<li><p><strong>利用zmail发送电子邮件</strong><br>环境需要package：email， smtplib和zmail均可通过pip install 安装，其中email和smtplib可能是python自带的（注意执行程序的目录下不要email.py 文件，否则导入email回出错）<br>准备需要：打开发送邮箱的pop3/smtp服务</p>
<figure class="highlight bash"><table><tr><td class="code"><pre><span class="line">import zmail</span><br><span class="line"><span class="comment">#发送带附件的邮件</span></span><br><span class="line">def send_zmail(path):</span><br><span class="line">  <span class="comment">#path is the attachment path</span></span><br><span class="line">  mail_content = &#123;<span class="string">&#x27;subject&#x27;</span>:<span class="string">&#x27;a subject string&#x27;</span>,</span><br><span class="line">                 <span class="string">&#x27;content_text&#x27;</span>: <span class="string">&#x27;a content string&#x27;</span>,</span><br><span class="line">                  <span class="string">&#x27;attachments&#x27;</span>:path&#125;</span><br><span class="line">  server = zmail.server(<span class="string">&#x27;senderxxx@qq.com&#x27;</span>, <span class="string">&#x27;授权码&#x27;</span>)</span><br><span class="line"><span class="comment">#这里的授权吗不是邮箱密码，而是打开发送邮箱pop3/SMTP服务时给的授权码,小号授权码是：rtztpbqrnyqmcjjd</span></span><br><span class="line"><span class="comment">#牛小号：授权码：fmzpsgtqoajhbbcg</span></span><br><span class="line">server.seed_mail([<span class="string">&#x27;targetxxx@qq.com&#x27;</span>], mail_content)</span><br></pre></td></tr></table></figure>
<p>#接受邮件的附件</p>
<figure class="highlight bash"><table><tr><td class="code"><pre><span class="line">Import zmail</span><br><span class="line">server = zmail.server(<span class="string">&#x27;xxxxxxx@qq.com&#x27;</span>, <span class="string">&#x27;授权码&#x27;</span>)</span><br><span class="line">mail = server.get_latest()<span class="comment">#得到最新的邮件</span></span><br><span class="line">attachment = mail[<span class="string">&#x27;attachments&#x27;</span>]<span class="comment">#得到邮件的附件</span></span><br><span class="line">name, content = attachment[0]<span class="comment"># 分别得到邮件的名称和内容</span></span><br><span class="line">with open(name, <span class="string">&#x27;wb&#x27;</span>) as f:</span><br><span class="line">     f.write(content)</span><br></pre></td></tr></table></figure></li>
<li><p>**mac的一些常用操作命令 **</p>
<figure class="highlight bash"><table><tr><td class="code"><pre><span class="line">1,open:打开文件、目录或者执行程序，相当于图形界面下的双击操作</span><br><span class="line">open my.docx<span class="comment">#将会使用默认关联程序打开</span></span><br><span class="line">选项-e: 强制在TextEdit中编辑次文件</span><br><span class="line"></span><br><span class="line">2，pbcopy和pbpaste：命令行与剪切板，等同于鼠标的复制粘贴操作</span><br><span class="line">ls . | pdcopy <span class="comment">#复制当前文件夹下的所有文件</span></span><br><span class="line">Pbpaste &lt; test.file <span class="comment">#将文件的内容复制到剪切板</span></span><br><span class="line">pbpaste &gt; test.file1 <span class="comment">#将剪切板上的内容粘贴进文件里</span></span><br><span class="line"></span><br><span class="line">3，say命令：让Mac说话</span><br><span class="line">say <span class="string">&#x27;All works is done!&#x27;</span></span><br><span class="line"></span><br><span class="line">4, mdfind命令：spotlight的命令行模式</span><br><span class="line">mdfind -onlyin ~/Document essay <span class="comment">#在document中寻找essay文件</span></span><br></pre></td></tr></table></figure></li>
<li><p><strong>linux的convert命令</strong><br>作用转换图像，缩放，旋转等</p>
<figure class="highlight bash"><table><tr><td class="code"><pre><span class="line">convert 1.jpg 2.png <span class="comment">#转换图像格式</span></span><br><span class="line">Convert 1.gif 2.bmp <span class="comment">#</span></span><br><span class="line"><span class="comment">#改变图像尺寸</span></span><br><span class="line">convert -resize 1024x768 1.jpg 2.jpg</span><br><span class="line"><span class="comment">#比例放缩 </span></span><br><span class="line">Convert -resize %50 1.jpg 2.jpg </span><br><span class="line"><span class="comment">#旋转图像</span></span><br><span class="line">Convert -rotate 1.jpg 2.jpg</span><br><span class="line"><span class="comment">#批量处理-重新转换</span></span><br><span class="line"><span class="keyword">for</span> %f <span class="keyword">in</span> (*.jpg) <span class="keyword">do</span> convert “%f” “%~nf.png”</span><br><span class="line"><span class="comment">#全部进行缩放</span></span><br><span class="line"><span class="keyword">for</span> %f <span class="keyword">in</span> (*.png) <span class="keyword">do</span> convert “%f” -sample 80×40 “%~nf_sample.png”</span><br><span class="line"></span><br></pre></td></tr></table></figure></li>
<li><p><strong>universal-ctags安装使用</strong> 1, 安装</p>
<figure class="highlight bash"><table><tr><td class="code"><pre><span class="line">sudo apt-get install autoconf</span><br><span class="line">git <span class="built_in">clone</span> https://github.com/universal-ctags/ctags.git</span><br><span class="line"><span class="built_in">cd</span> ctats</span><br><span class="line">./autogen.sh</span><br><span class="line">./configure</span><br><span class="line">make</span><br><span class="line">sudo make install</span><br></pre></td></tr></table></figure></li>
</ul>
<p>2, 使用</p>
<figure class="highlight bash"><table><tr><td class="code"><pre><span class="line">1)配置vim</span><br><span class="line">打开～/.vimrc键入</span><br><span class="line">Set tags=tags;</span><br><span class="line">Set autochdir</span><br><span class="line"></span><br><span class="line">2)使用在需要使用使用ctags的文件夹下，首先使用ctags -R .生成tags文件，之后进入vim</span><br><span class="line">即可使用，常用快捷键</span><br><span class="line">Ctrl + ] 找到光标出变量/函数定义的地方</span><br><span class="line">Ctrl + T 回到跳转之前的地方</span><br><span class="line">Ctrl + O 退回原来的地方</span><br><span class="line">[I 全局查找光标出的变量，全局指在当前文件夹中的所有文件爱你中国呢查找</span><br><span class="line">[i 局部查找变量，当前文件中查找</span><br><span class="line">]i 类似上条命令，从光标出向下开始查找</span><br><span class="line">[&#123; / [( 转到匹配的上一个中括号/圆括号</span><br><span class="line">[&#125; / [) 转到匹配的下一个中括号/圆括号</span><br><span class="line">&#123;/&#125; 转到上/下一个空行</span><br><span class="line">u 更新taglist，用于更改了代码之后更新</span><br><span class="line">[[ 跳到前一个文件</span><br><span class="line">]] 跳到后一个文件</span><br><span class="line">*/<span class="comment"># 转到光标出单词下一次/上一次出现的地方</span></span><br><span class="line">gd 查找光标出的局部变量</span><br></pre></td></tr></table></figure>

<ul>
<li><p><strong>命令行中查看图片尺寸信息的命令</strong></p>
<figure class="highlight bash"><table><tr><td class="code"><pre><span class="line"><span class="comment">#第一种是用file命令</span></span><br><span class="line">file im.png</span><br><span class="line"><span class="comment">#第二种是安装imagemagic</span></span><br><span class="line">Sudo apt-get install imagemagic</span><br><span class="line">identify im.jpg <span class="comment">#使用identify查看信息</span></span><br></pre></td></tr></table></figure></li>
<li><p><strong>查看文件/文件夹的读取路径</strong></p>
<figure class="highlight bash"><table><tr><td class="code"><pre><span class="line">readlink -f dir/file_path</span><br></pre></td></tr></table></figure></li>
<li><p>**superpixel-benchmark安装指南（docker） **</p>
<figure class="highlight plaintext"><table><tr><td class="code"><pre><span class="line">1, 按照项目指引安装glog</span><br><span class="line">可能出现的错误： src/demangle.cc:170:16: error: ISO C++ forbids comparison between pointer and integer [-fpermissive]</span><br><span class="line">解决办法是去对应的行在指针前面加*</span><br><span class="line"></span><br><span class="line">2，安装Gflags（https://blog.csdn.net/weixin_43767344/article/details/103799983）：</span><br><span class="line">git clone https://github.com/gflags/gflags.git</span><br><span class="line"></span><br><span class="line">cd gflags</span><br><span class="line"></span><br><span class="line">mkdir build &amp;&amp; cd build</span><br><span class="line"></span><br><span class="line">cmake … -DBUILD_SHARED_LIBS=ON</span><br><span class="line"></span><br><span class="line">make -j4</span><br><span class="line"></span><br><span class="line">make install</span><br><span class="line"></span><br><span class="line">3，安装opencv，参考https://docs.opencv.org/4.5.2/d7/d9f/tutorial_linux_install.html，（build with opencv_contrib）</span><br><span class="line"></span><br><span class="line">4, 然后make的时候，可能出现的错误： undefined reference to ‘google::FlagRegisterer::FlagRegisterer</span><br><span class="line">解决方案，在对应的sub_dir下，以eval_summary_cli为例，在superpixel-benchamrk/eval_summary_slic/CMakelist.txt中加入：</span><br><span class="line">find_package(gflags REQUIRED)</span><br><span class="line">find_package(Threads)</span><br><span class="line"></span><br><span class="line">target_link_libraries(eval_summary_cli libgflags.a libglog.a $&#123;CMAKE_THREAD_LIBS_INIT&#125;)</span><br><span class="line"></span><br><span class="line">该解决方案只能解决，eval相关的编译，对于core algorithm以及example的编译不起作用，目前对于对于算法模块的编译尚未找到解决方案</span><br><span class="line"></span><br><span class="line">5，退而求其次，只能先编译用于eval的程序，将cmakelist.txt中的涉及算法的行，全都注释掉</span><br><span class="line"></span><br><span class="line">6, 对于其他的错误如CV_BGR2Lab等未发现，为opencv版本问题，搜索即可找到解决方案</span><br><span class="line">可能用到的参考网址：https://github.com/google/glog/issues/52</span><br></pre></td></tr></table></figure></li>
<li><p>**ln命令与软，硬连接 **<br>一个文件的硬连接相当于多个名称，删除一个不会影响另一个<br>一个文件的软连接相当于快捷方式<br>ln默认是硬连接，命令的使用方式为：</p>
<figure class="highlight bash"><table><tr><td class="code"><pre><span class="line">ln 源文件路径 硬连接文件路径 <span class="comment">#建立硬连接</span></span><br><span class="line">ln -s 源文件路径 硬文件路径 <span class="comment">#建立软连接</span></span><br><span class="line"></span><br><span class="line"><span class="comment">#例子，假设安装了g++-7与g++-8, 配置默认的g++通过建立软连接</span></span><br><span class="line">ln -s /usr/bin/g++7 /usr/bin/g++ <span class="comment">#配置默认为g++-7,g++-8同理</span></span><br></pre></td></tr></table></figure>
<p>普通用户针对目录不能建立硬连接，可以建立软连接，原因是目录硬连接可能陷入死循环</p>
</li>
<li><p>** 强制删除坏包 **<br>问题描述: apt remove xxx 出现 dependency problems prevent removal of xxx</p>
<figure class="highlight plaintext"><table><tr><td class="code"><pre><span class="line">dpkg --force-all -P package_name</span><br><span class="line">#例如</span><br><span class="line">dpkg --force-all -P Nvidia-driver-470</span><br></pre></td></tr></table></figure></li>
</ul>
]]></content>
  </entry>
  <entry>
    <title>miscellanea</title>
    <url>/2018/10/24/miscellanea/</url>
    <content><![CDATA[<h3 id="this-file-record-some-miscellanea-information"><a href="#this-file-record-some-miscellanea-information" class="headerlink" title="this file record some miscellanea information"></a>this file record some miscellanea information</h3><p><strong>myblog localtion</strong>: <a href="https://wangyxxjtu.github.io/">https://wangyxxjtu.github.io/</a></p>
<p><strong>tencent log in the workstation</strong><br>first connect the server<br>then input ssh order: ssh <a href="mailto:&#x77;&#x65;&#x62;&#x64;&#x65;&#118;&#64;&#49;&#x30;&#46;&#49;&#x36;&#53;&#46;&#50;&#x38;&#46;&#55;&#x33;">&#x77;&#x65;&#x62;&#x64;&#x65;&#118;&#64;&#49;&#x30;&#46;&#49;&#x36;&#53;&#46;&#50;&#x38;&#46;&#55;&#x33;</a><br>passward:5qn5$japO</p>
<p>** anaconda deletes environment**:</p>
<figure class="highlight bash"><table><tr><td class="code"><pre><span class="line">conda remove -n tensorflow --all</span><br><span class="line">Conda activate name<span class="comment">#激活环境</span></span><br><span class="line">conda deactivate <span class="comment">#退出环境</span></span><br><span class="line">conda info --env <span class="comment">#列出所有的环境</span></span><br></pre></td></tr></table></figure>

<p>** about the traspose convoluation output size **<br>the output size for transpose convolution is: </p>
<ul>
<li>output_size=(input_size-1)xstride- 2 x input_padding + kernel_size  + output_padding</li>
</ul>
<p>** vim command: **<br>when vim file1 file2, and use comand “vertical all” to split the file1 and file into 2 vertical windows, use ‘ctrl + w’ to switch the file<br>多窗口操作： </p>
<figure class="highlight plaintext"><table><tr><td class="code"><pre><span class="line">:new/sp filename 横向切割窗口</span><br><span class="line">:vsp filename  纵向切割窗口</span><br><span class="line">:tabc 关闭当前窗口</span><br><span class="line">:tabo 关闭所有窗口</span><br><span class="line">窗口大小调整</span><br><span class="line">:ctrl+w +/- 纵向扩大/缩小</span><br><span class="line">:res +/- num 纵向扩大/缩小多少行</span><br><span class="line">:vertical res +/- num 横向增加/减少多少列</span><br><span class="line"></span><br><span class="line">:!ls 运行bash命令</span><br><span class="line">:n 跳到下一个文件 n c 跳到c文件</span><br><span class="line">:e# 回到刚才编辑的文件</span><br><span class="line">:ls 现实当前打开的文件，还有为难序号</span><br><span class="line">:shell 不关闭vim回到shell</span><br><span class="line">:exit / ctrl+D 从shell回到vim  </span><br></pre></td></tr></table></figure>

<p>** 工蜂git upload the file: **<br>it is under ‘volumn’ bar</p>
<ul>
<li>**scp 命令远程拷贝文件 **</li>
</ul>
<figure class="highlight bash"><table><tr><td class="code"><pre><span class="line">scp ./yalexwang_workstation/  webdev@100.115.156.103:haoyang/title_image_matching/yalexwang_workstation/ (如果拷贝是文件夹，scp之后加 -r即可)</span><br></pre></td></tr></table></figure>
<p>过滤当前目录下的文件进行拷贝：<br> <figure class="highlight bash"><table><tr><td class="code"><pre><span class="line">Scp !(*.new) yaxiwang@cluster.eng.uts.edu.au:~/Data/</span><br></pre></td></tr></table></figure></p>
<ul>
<li><p><strong>远程拷贝，递归地遍历目录，过滤文件进行拷贝</strong></p>
<figure class="highlight bash"><table><tr><td class="code"><pre><span class="line"> rsync -av -e ssh --include=<span class="string">&#x27;*.new&#x27;</span> ./vgg16/ yaxiwang@cluster.eng.uts.edu.au:~/Data/TBIRDataSet/NUS-Wide/vgg16/</span><br><span class="line"><span class="comment">#与sshpass结合使用可以避免输入密码,-p选项输入密码 -f指定文件读取密码</span></span><br><span class="line">sshpass -p wangyifan123456 rsync -avP ./* wangyifan@10.20.108.16:/home/wangyifan/   </span><br></pre></td></tr></table></figure>
<p>其中a：递归忙碌<br>v：详细输出<br>e ssh：指定ssh作为远程shell，文件会被加密<br>exclue/include： 排除文件/包含文件</p>
</li>
<li><p>** nltk安装和使用停用词 **<br>在已经安装nltk的基础上，进入python，导入nltk，输入命令：</p>
<figure class="highlight bash"><table><tr><td class="code"><pre><span class="line">nltk.download(<span class="string">&#x27;stopwords&#x27;</span>)</span><br><span class="line">之后就可以导入停用词（列表）</span><br><span class="line">from nltk.corpus import stopwords</span><br><span class="line"></span><br><span class="line">stop_word = stopswords.words(<span class="string">&#x27;english&#x27;</span>)<span class="comment">#列表，里面是一些引文单词如‘me’, &#x27;againse&#x27;等</span></span><br></pre></td></tr></table></figure></li>
<li><p>** python在声明一个类之后，对象调用**<br>如果你想在定义一个类，通过对象调用传参调用，就得在类中实现一个函数 <strong>call</strong>(),定义传参如何调用，就像pytorch中的那样调用一样（liear = nn.linaer(3,4), liner(matrix)）,在nn.Linear中实现__call__函数</p>
</li>
<li><p>** sheel输出重定向与后台运行**<br>输出重定向命令：</p>
<figure class="highlight bash"><table><tr><td class="code"><pre><span class="line"><span class="built_in">command</span> 1&gt; out.file 2&gt;&amp;1 （简写 <span class="built_in">command</span>&gt;out.file 2&gt;&amp;1）</span><br></pre></td></tr></table></figure>
<p>其中1表示标准输出（打印到屏幕上的内容输出到了out.file中），2表示标准错误，将错误信息也输出到out.file中， &amp;表示引用，不用重复打开文件覆盖内容, 0表示标准输入</p>
</li>
</ul>
<p>对于将文件不断的挂起到后台运行，可以使用命令nohup（no hang up）:</p>
<figure class="highlight bash"><table><tr><td class="code"><pre><span class="line">nohup <span class="built_in">command</span> &gt; &amp;(nohup <span class="built_in">command</span> 2&gt;&amp;1 &amp;)</span><br><span class="line">nohup python train.py &gt;train.log 2&gt;&amp;1 &amp;</span><br></pre></td></tr></table></figure>
<p>默认将输出保存到当卡目录下的nohup.out文件中，如果已经存在，则保存到$HOME/nohup.out中, 其中最后一个&amp;符表示提交到后台运行，第一个&gt;表示将打印屏幕的输出弄到nohup.out文件中。</p>
<ul>
<li><p>**nohup输出的文件，中途不再输出 **<br>猜测可能是缓冲的问题，解决是加 -u参数</p>
<figure class="highlight bash"><table><tr><td class="code"><pre><span class="line">nohup python -u train.py&gt;nohup.log 2&gt;&amp;1 &amp; <span class="comment">#-u表示不启用缓冲</span></span><br></pre></td></tr></table></figure></li>
<li><p>** 杀死nohup提交到后台的程序**<br>首先使用命令：</p>
<figure class="highlight bash"><table><tr><td class="code"><pre><span class="line">ps ux | grep 运行的程序名称（如 train.py）/ps ef | grep 运行的程序名称</span><br></pre></td></tr></table></figure>
<p>按照运行时间查看要杀死的那次的运行对应的进程id，之后使用命令:</p>
<figure class="highlight bash"><table><tr><td class="code"><pre><span class="line"><span class="built_in">kill</span> -9 进程id</span><br></pre></td></tr></table></figure>
<p>Nohup 命令执行后会输出一个id，这个id+1，一般就是提交到的后台命令程序的id。</p>
</li>
<li><p><strong>从远程linux拷贝文件到本地</strong></p>
<figure class="highlight bash"><table><tr><td class="code"><pre><span class="line">sz file-name</span><br></pre></td></tr></table></figure>
<p>输入上面的命令之后，会选择保存的位置，选择好之后就可以传送了</p>
</li>
<li><p><strong>linux 查看文件夹中有多少个文件</strong></p>
<figure class="highlight bash"><table><tr><td class="code"><pre><span class="line">ls | wc -w</span><br></pre></td></tr></table></figure>
<p>列出文件夹下的所有文件并且按照内存的大小从大到小排序</p>
<figure class="highlight bash"><table><tr><td class="code"><pre><span class="line">ls -Slh(r) </span><br></pre></td></tr></table></figure>
<p>R参数表示反过来排序</p>
</li>
</ul>
<ul>
<li><p><strong>linux查看磁盘使用情况</strong></p>
</li>
<li><p>查看磁盘总体占用情况</p>
<figure class="highlight bash"><table><tr><td class="code"><pre><span class="line">df -h</span><br><span class="line">Df -lh <span class="comment">#查看整个磁盘的占用情况</span></span><br></pre></td></tr></table></figure></li>
<li><p>查看当前用户磁盘占用情况</p>
<figure class="highlight bash"><table><tr><td class="code"><pre><span class="line">du -sh ~</span><br></pre></td></tr></table></figure></li>
<li><p>** 以最高权限kill程序**</p>
<figure class="highlight bash"><table><tr><td class="code"><pre><span class="line">pkill -9 program_id (pkill -9 python 杀死所有python进程)</span><br></pre></td></tr></table></figure></li>
<li><p><strong>查看文件或者文件夹占用的磁盘空间大小</strong></p>
<figure class="highlight bash"><table><tr><td class="code"><pre><span class="line">du -h flick30k_image/</span><br></pre></td></tr></table></figure></li>
<li><p><strong>pickle.load问题</strong><br>使用pickle.load读入文件时报错：</p>
<figure class="highlight bash"><table><tr><td class="code"><pre><span class="line"><span class="string">&#x27;ascii&#x27;</span> codec can<span class="string">&#x27;t decode byte 0xe4 in position 24: ordinal not in range(128)</span></span><br></pre></td></tr></table></figure>
<p>解决方案：</p>
<figure class="highlight bash"><table><tr><td class="code"><pre><span class="line">pickle.load(open(filename, <span class="string">&#x27;rb&#x27;</span>), encoding = <span class="string">&#x27;bytes&#x27;</span>)</span><br></pre></td></tr></table></figure></li>
<li><p>** python 3 与python<em>2中的转换为utf-8</em><br>Python 2中： str(your_string).decode(‘utf-8’)<br>Python 3中： str(your_string, ‘utf-8’)<br>在python3中使用decode会报错‘no attribute’</p>
</li>
<li><p>** python 3错误：’collections.OrderedDict’ object has no attribute ‘iteritems’ **<br>Python 3中没有OrderedDict.iteritems,该用OrderedDict.items()</p>
</li>
<li><p><strong>python对字典按照值进行排序</strong></p>
<figure class="highlight bash"><table><tr><td class="code"><pre><span class="line">sorted_list = sorted(dict_name.items(), key = lambda x:x[1], reverse  = True)<span class="comment">#由大到小排序</span></span><br></pre></td></tr></table></figure></li>
<li><p><strong>linux批处理有时候文件太多报错, 比如压缩文件zip</strong></p>
<figure class="highlight bash"><table><tr><td class="code"><pre><span class="line">/usr/bin/zip: Argument list too long</span><br></pre></td></tr></table></figure>
<p>解决方案：</p>
<figure class="highlight bash"><table><tr><td class="code"><pre><span class="line">ls | grep 关键字| xargs -n 10 zip zipfile.zip</span><br></pre></td></tr></table></figure>
<p>含义为：列出匹配关键字的文件，以10个分组，然后分批及逆行zip操作</p>
</li>
<li><p>** python中刷新输出语法 **：<br>如果是python 3</p>
<figure class="highlight bash"><table><tr><td class="code"><pre><span class="line"><span class="built_in">print</span>(<span class="string">&#x27;\r finished percent: %f&#x27;</span>%(counter/length * 100), end = <span class="string">&#x27;&#x27;</span>, flush = True)</span><br></pre></td></tr></table></figure>
<p>如果是pyrhon2</p>
<figure class="highlight bash"><table><tr><td class="code"><pre><span class="line">import sys</span><br><span class="line">sys.stdout.write(<span class="string">&#x27;\rfinished percent %f &#x27;</span>%(counter / length *100))</span><br><span class="line">Sys.flush()</span><br></pre></td></tr></table></figure>
<p>注意write后面的\r和下一句的flush</p>
</li>
<li><p><strong>pickle.load报错：</strong></p>
<figure class="highlight bash"><table><tr><td class="code"><pre><span class="line">unsupported pickle protocol: 3</span><br></pre></td></tr></table></figure>
<p>原因是pickle的保存格式版本不对，解决方案是重新保存pickle文件</p>
<figure class="highlight bash"><table><tr><td class="code"><pre><span class="line">pickle.dump(data, open(file_pth, <span class="string">&#x27;wb&#x27;</span>), protocol = 2)</span><br></pre></td></tr></table></figure></li>
<li><p>** python从url读取图片的三种方式**<br>第一种方式: opencv</p>
<figure class="highlight bash"><table><tr><td class="code"><pre><span class="line">import cv2</span><br><span class="line"></span><br><span class="line"><span class="built_in">cap</span> = cv2.VideoCapture(url_string)</span><br><span class="line">ret, img = cap.read()</span><br></pre></td></tr></table></figure>
<p>读取的图片为array数据</p>
</li>
</ul>
<p>第二种方式：skimage</p>
<figure class="highlight bash"><table><tr><td class="code"><pre><span class="line">from skimage import io</span><br><span class="line"></span><br><span class="line">image = io.imread(url)</span><br><span class="line"><span class="comment">#save the image</span></span><br><span class="line">io.imsave(<span class="string">&#x27;test.jpg&#x27;</span>, image)</span><br></pre></td></tr></table></figure>
<p>读取的图片也为array数据</p>
<p>第三种方式：urllib</p>
<figure class="highlight bash"><table><tr><td class="code"><pre><span class="line">import urllib</span><br><span class="line"></span><br><span class="line"><span class="comment"># 网络上图片的地址</span></span><br><span class="line">img_src = <span class="string">&#x27;https://img-my.csdn.net/uploads/201212/25/1356422284_1112.jpg&#x27;</span></span><br><span class="line"></span><br><span class="line"><span class="comment"># 将远程数据下载到本地，第二个参数就是要保存到本地的文件名</span></span><br><span class="line">urllib.urlretrieve(img_src,<span class="string">&#x27;D:/1.jpg&#x27;</span>)</span><br></pre></td></tr></table></figure>

<ul>
<li>** python中利用opencv在图片上写字： puttext函数**<figure class="highlight bash"><table><tr><td class="code"><pre><span class="line">import cv2</span><br><span class="line"></span><br><span class="line">cv2.puttext(image_data, text_string, position, fontFace, fontcolor, thickness, linetype)</span><br><span class="line"></span><br></pre></td></tr></table></figure>
其中：<br>image_data为图片，类型为ndarray</li>
</ul>
<p>text_string为想要放置的图片</p>
<p>position是textstring放置位置的左上角坐标</p>
<p>fontFace为字体设置，可以取的值为cv2.<br>FONT_HERSHEY_SIMPLEX,<br>FONT_HERSHEY_PLAIN,<br>FONT_HERSHEY_DUPLEX,<br>FONT_HERSHEY_COMPLEX,<br>FONT_HERSHEY_TRIPLEX,<br>FONT_HERSHEY_COMPLEX_SMALL,<br>FONT_HERSHEY_SCRIPT_SIMPLEX,<br>orFONT_HERSHEY_SCRIPT_COMPLEX</p>
<p>fontcolor为三元组（r, g, b）的值</p>
<p>thickness为线粗， 整数</p>
<p>linetype为线形，取值：<br>8 (or omitted) ： 8-connected line<br>4：4-connected line<br>CV_AA - antialiased line</p>
<ul>
<li><p>**pickle.dumps()的使用 **<br>pickle.dumps可以将一条数据序列化为一个字符串，并且以’.’作为一条数据的分隔符<br>在写入数据的时候，不用写换行, 存数据：</p>
<figure class="highlight bash"><table><tr><td class="code"><pre><span class="line">a_dict = &#123;<span class="string">&#x27;a&#x27;</span>:1, <span class="string">&#x27;b&#x27;</span>:2&#125;</span><br><span class="line">f = open(file_path, <span class="string">&#x27;w&#x27;</span>)</span><br><span class="line">dict_str = pickle.dumps(a_dict)</span><br><span class="line">f.write(<span class="string">&#x27;f&#x27;</span>)</span><br><span class="line">f.close()</span><br></pre></td></tr></table></figure>
<p>读取数据:</p>
<figure class="highlight bash"><table><tr><td class="code"><pre><span class="line">f = open(file_path , <span class="string">&#x27;r&#x27;</span>)</span><br><span class="line">content = f.read()</span><br><span class="line">content_list = content.split(<span class="string">&#x27;.&#x27;</span>)</span><br><span class="line">a_data = pickle.loads(content_list[0] + <span class="string">&#x27;.&#x27;</span>)</span><br><span class="line">f.close()</span><br></pre></td></tr></table></figure></li>
<li><p>** linux中文问题 **<br>linux在程序中不支持中文u’...’，解决方法：</p>
<figure class="highlight plaintext"><table><tr><td class="code"><pre><span class="line">string = u&#x27;\u...&#x27;</span><br><span class="line">str1 = string.encode(&#x27;utf-8&#x27;)</span><br></pre></td></tr></table></figure>
<p>有时程序中含有中文，报错， 例如：</p>
<figure class="highlight bash"><table><tr><td class="code"><pre><span class="line"><span class="keyword">if</span> <span class="string">&#x27;、&#x27;</span> <span class="keyword">in</span> string:</span><br><span class="line">		....</span><br></pre></td></tr></table></figure>
<p>解决方法， 在程序的开头加上：</p>
<figure class="highlight bash"><table><tr><td class="code"><pre><span class="line"><span class="comment">#-*-coding:utf8-*</span></span><br></pre></td></tr></table></figure></li>
<li><p>** linux下在图片上写汉字乱码，解决方法：将字符串解析为unicode并且将字体解码方式设置为默认</p>
<figure class="highlight bash"><table><tr><td class="code"><pre><span class="line">unicode_str = str.decode(<span class="string">&#x27;utf-8&#x27;</span>)</span><br><span class="line"><span class="comment"># PIL图片上打印汉字</span></span><br><span class="line">draw = ImageDraw.Draw(pilimg) <span class="comment"># 图片上打印</span></span><br><span class="line">font = ImageFont.truetype(<span class="string">&quot;*.ttf&quot;</span>, fontsize) <span class="comment"># 参数1：字体文件路径，参数2：字体大小</span></span><br><span class="line">draw.text((x, y), unicode_str, (0, 0, 0), font=font) 参数1：左上角坐标，参数2：字符串，参数3：字的颜色，参数4：字体</span><br></pre></td></tr></table></figure>
<p>字符串如果是乱码，可以尝试：</p>
<figure class="highlight bash"><table><tr><td class="code"><pre><span class="line">string = <span class="string">&#x27;??????&#x27;</span></span><br><span class="line">s = string.decode(<span class="string">&#x27;utf-8&#x27;</span>).encode(<span class="string">&#x27;utf-8&#x27;</span>)</span><br><span class="line"><span class="built_in">print</span>(s)<span class="comment">#将会是中文</span></span><br></pre></td></tr></table></figure></li>
<li><p>** macbook系统下进行hexo更新 **<br>进入的你的博客目录下（博客根目录下，不需要移动到博客右键所在的目录），运行hexo g -d即可</p>
</li>
</ul>
<p>** Linux查看内存使用情况的命令 **<br>free -h</p>
<p>** linux安装文件没有权限 **<br>输入–user 例如：<br>Pip install pytorch —user</p>
<ul>
<li>** vim打开的情况下在开另一个窗口显示另一个文件 **<figure class="highlight bash"><table><tr><td class="code"><pre><span class="line">:vsplit fiile_name.py</span><br><span class="line"><span class="comment">#多文件编辑</span></span><br><span class="line">Ctrl+^ 两者之间切换</span><br><span class="line">:bn/bp 切换到下一个/上一个</span><br><span class="line">:ls 查看所有打开的文件</span><br><span class="line">:close 关闭当前面板</span><br><span class="line"><span class="comment">#多标签操作</span></span><br><span class="line">:tabnew/tabe + 文件名 开起新tab打开文件</span><br><span class="line">:tabc/o 关闭当前tab/所有其他tab</span><br><span class="line">:qa 退出所有的tab，到bash中</span><br><span class="line">:tabn/p 下一个/上一个tab</span><br><span class="line">gt/gT 直接可以在tab之间切换</span><br><span class="line"></span><br><span class="line">:tabs 查看所有打开的tab</span><br><span class="line">ctrl+s 停止向屏幕输出</span><br><span class="line">ctrl+q 恢复向屏幕输出</span><br></pre></td></tr></table></figure></li>
</ul>
<p>在bash下打开之前使用vim -O file1 file2 会得到相同的结果<br>Split水平的效果</p>
<ul>
<li><p>**倒入tensorflow错误：ibcublas.so.10.0: cannot open shared object file: No such file or directory **<br>解决方法：</p>
<figure class="highlight bash"><table><tr><td class="code"><pre><span class="line">conda install cudatoolkit</span><br><span class="line">conda install cudnn</span><br><span class="line"></span><br></pre></td></tr></table></figure>
<p>如果上面的命令不奏效，运行时出错的话，终极解决办法：</p>
<figure class="highlight bash"><table><tr><td class="code"><pre><span class="line">conda install tensorflow-gpu</span><br></pre></td></tr></table></figure>
<p>Conda会自动安装版本相关的依赖</p>
</li>
<li><p>**读取xls文件的单元格内容及底色 **</p>
<figure class="highlight bash"><table><tr><td class="code"><pre><span class="line">def count():</span><br><span class="line">     path = <span class="string">&#x27;./510_loose_true.xls&#x27;</span></span><br><span class="line">     work_book = xlrd.open_workbook(path, formatting_info = 1)</span><br><span class="line">     sheets = work_book.sheet_names()</span><br><span class="line">     sheet = work_book.sheet_by_index(0)</span><br><span class="line"></span><br><span class="line">     rows, cols = sheet.nrows, sheet.ncols</span><br><span class="line"> </span><br><span class="line">    posi_count = 0</span><br><span class="line">    nega_count = 0</span><br><span class="line">    img_list = []</span><br><span class="line">    <span class="keyword">for</span> row <span class="keyword">in</span> range(rows):</span><br><span class="line">        <span class="keyword">for</span> col <span class="keyword">in</span> range(3, cols):</span><br><span class="line">            xfx = sheet.cell_xf_index(row, col)</span><br><span class="line">            xf = work_book.xf_list[xfx]</span><br><span class="line">            bgx = xf.background.pattern_colour_index</span><br><span class="line">            value = sheet.cell(row, col).value</span><br><span class="line">            <span class="keyword">if</span> bgx == 17:</span><br><span class="line">                posi_count += 1</span><br><span class="line">                img_list.append(value)</span><br><span class="line">            <span class="keyword">if</span> bgx == 10:</span><br><span class="line">                nega_count += 1</span><br><span class="line">                img_list.append(value)</span><br><span class="line"> </span><br><span class="line">    img_set = <span class="built_in">set</span>(img_list)</span><br><span class="line">    <span class="built_in">print</span>(<span class="string">&#x27;total image: %d&#x27;</span>%len(img_list))</span><br><span class="line">    <span class="built_in">print</span>(<span class="string">&#x27;different images: %d&#x27;</span>%len(img_set))</span><br><span class="line">    <span class="built_in">print</span>(<span class="string">&#x27;negative images: %d&#x27;</span>%nega_count)</span><br><span class="line">    <span class="built_in">print</span>(<span class="string">&#x27;positive iamges: %d&#x27;</span>%posi_count)</span><br><span class="line">    <span class="built_in">print</span>(<span class="string">&#x27;validate: %d&#x27;</span>%(nega_count + posi_count))</span><br></pre></td></tr></table></figure></li>
<li><p>**anaconda移除环境 **</p>
<figure class="highlight bash"><table><tr><td class="code"><pre><span class="line">conda remove -n tensorflow0 --all</span><br></pre></td></tr></table></figure></li>
<li><p><strong>Image.fromarray(an_array)报错cann’t haddle this type data</strong><br>解决方法，fromarrray的shape必须是(224,224,3)并转换为uint8格式</p>
<figure class="highlight bash"><table><tr><td class="code"><pre><span class="line">Array = np.random.rand(224,224,3)</span><br><span class="line">Image = Image.fromarray(array.astyep(np.uint8))</span><br><span class="line">``</span><br><span class="line"></span><br><span class="line">* **Image.open()错误：IOError：image file truncated **</span><br><span class="line">``` bash</span><br><span class="line">From PIL import ImageFile</span><br><span class="line">ImageFile.LOAD_TRUNCATED_IMAGES=True</span><br></pre></td></tr></table></figure></li>
<li><p><strong>声明inception_v3网络报错</strong><br>错误描述：ImportError: /lib64/libstdc++.so.6: version `CXXABI_1.3.9’ not found (required by /home/yaxiwang/Data/anaconda/envs/pytorch/lib/python2.7/site-packages/scipy/sparse/_sparsetools.so)<br>解决方法：</p>
<figure class="highlight bash"><table><tr><td class="code"><pre><span class="line">vim ~/.bash_profile</span><br><span class="line"><span class="comment">#在bash_profile加入</span></span><br><span class="line">LD_LIBRARY_PATH=/home/yaxiwang/anaconda/lib:<span class="variable">$LD_LIBRARY_PATH</span></span><br><span class="line"><span class="built_in">export</span> LD_LIBRARY_PATH两行</span><br><span class="line"><span class="comment">#最后要生效</span></span><br><span class="line"><span class="built_in">source</span> ~/.bash_profile</span><br></pre></td></tr></table></figure>
<p>一般在anaconda/lib的目录下会有和pytorch配套的libstdc++.so.6文件，上面两行将改路径加入到搜索路径中</p>
</li>
<li><p>**python中import scipy错误 **<br>Python自带的scipy不全，强制重新安装</p>
<figure class="highlight bash"><table><tr><td class="code"><pre><span class="line">pip install --force-reinstall scipy --ignore-installed</span><br></pre></td></tr></table></figure></li>
<li><p>**pip install安装错误：ERROR: Could not install packages due to an EnvironmentError: [Errno 39] Directory not empty: **<br>解决方案：后面加上选项 –ignore-installed</p>
<figure class="highlight bash"><table><tr><td class="code"><pre><span class="line">pip install --force-reinstall scipy --ignore-installed</span><br></pre></td></tr></table></figure></li>
<li><p>**anaconda安装之后无法识别conda命令 **</p>
<figure class="highlight bash"><table><tr><td class="code"><pre><span class="line">vim ~/.bash_profile</span><br><span class="line"><span class="comment">#在打开的文件中加入</span></span><br><span class="line"><span class="built_in">export</span> PATH=<span class="string">&quot;/users/wang/anaconda2/bin&quot;</span>:<span class="variable">$PATH</span></span><br></pre></td></tr></table></figure>
<p>保存退出之后，source ～/.bashrc即可</p>
</li>
<li><p><strong>tailf命令</strong><br>该命令可以实时检测文件的变动（最后一行），可以和nohup命令配合使用</p>
<figure class="highlight bash"><table><tr><td class="code"><pre><span class="line">tailf nohup.out</span><br></pre></td></tr></table></figure></li>
<li><p><strong>docker中导入torchvision错误：libcudart.so.10.0: cannot open shared object file: No such file or directory</strong><br>这是应为docker中没有读到cuda相关的文件，<br>解决办法：将cuda文件例如（/usr/local/cuda-10.0）复制到你的docker映射目录下例如（/home/yaxiwang/data/该目录在docker中的对应路径假设为：/data，<br>进入docker，打开文件：</p>
<figure class="highlight bash"><table><tr><td class="code"><pre><span class="line">vim /etc/profile插入</span><br><span class="line"><span class="built_in">export</span> PATH=<span class="variable">$PATH</span>:/data/cuda-8.0/bin</span><br><span class="line"><span class="built_in">export</span> LD_LIBRARY_PATH=<span class="variable">$LD_LIBRARY_PATH</span>:/data/cuda-10.0/lib64</span><br><span class="line"><span class="built_in">export</span> LIBRARY_PATH=<span class="variable">$LIBRARY_PATH</span>:/data/cuda-10.0/lib64</span><br><span class="line"><span class="comment">#最后,使之生效</span></span><br><span class="line"><span class="built_in">source</span> /etc/profile</span><br></pre></td></tr></table></figure></li>
<li><p>**一次清空所有gpu程序，释放显存 **</p>
<figure class="highlight bash"><table><tr><td class="code"><pre><span class="line"><span class="comment">#用clearndata来过滤指定的程序，ps ef 也可以换成ps ux</span></span><br><span class="line"><span class="keyword">for</span> i <span class="keyword">in</span> `ps -ef|grep cleardata |awk <span class="string">&#x27;&#123;print $2&#125;&#x27;</span> `; <span class="keyword">do</span> <span class="built_in">kill</span> -9 <span class="variable">$i</span> ; <span class="keyword">done</span>;</span><br></pre></td></tr></table></figure></li>
<li><p>**npy文件太大无法一次读入，以至于无法操作的问题 **<br>如果一个train.npy文件太大，以至于无法操作，解决方案，使用numpy.load 函数时设置mmap_model = ‘r’可以，其原理是将变量进行磁盘地址映射而不是载入内存中，语法：</p>
<figure class="highlight bash"><table><tr><td class="code"><pre><span class="line">import numpy as np</span><br><span class="line"></span><br><span class="line">Path = <span class="string">&#x27;train.npy&#x27;</span></span><br><span class="line">data = np.load(path, mmap_mode = <span class="string">&#x27;r&#x27;</span>)<span class="comment">#</span></span><br></pre></td></tr></table></figure></li>
<li><p>**rsync命令传输文件 **<br>使用rsync比scp更快，使用的一般模式：</p>
<figure class="highlight bash"><table><tr><td class="code"><pre><span class="line">Rsync -avP * wangyifan@:/home/wangyifan/part_images</span><br><span class="line"><span class="comment">#过滤文件夹</span></span><br><span class="line">rsync -avP ATTNGANW ATTNGAN_AUTO/ --exclude ATTNGANW/fea_save <span class="comment">#注意第一个源文件夹没有/ 第二个目的文件夹有/</span></span><br><span class="line"><span class="comment">#-a 表示拷贝本地文件</span></span><br><span class="line"><span class="comment">#-v verbose 详细模式输出</span></span><br><span class="line"><span class="comment">#-P 相当于--partial 保留那些没有传输完成的文件，可以加快后续的传输</span></span><br><span class="line"></span><br><span class="line"><span class="comment">#其他常见的命令如下：</span></span><br><span class="line">-v, --verbose 详细模式输出</span><br><span class="line">-q, --quiet 精简输出模式</span><br><span class="line">-c, --checksum 打开校验开关，强制对文件传输进行校验</span><br><span class="line">-a, --archive 归档模式，表示以递归方式传输文件，并保持所有文件属性，等于-rlptgoD</span><br><span class="line">-r, --recursive 对子目录以递归模式处理</span><br><span class="line">-R, --relative 使用相对路径信息</span><br><span class="line">-b, --backup 创建备份，也就是对于目的已经存在有同样的文件名时，将老的文件重新命名为~filename。可以使用--suffix选项来指定不同的备份文件前缀。</span><br><span class="line">--backup-dir 将备份文件(如~filename)存放在在目录下。</span><br><span class="line">-suffix=SUFFIX 定义备份文件前缀</span><br><span class="line">-u, --update 仅仅进行更新，也就是跳过所有已经存在于DST，并且文件时间晚于要备份的文件。(不覆盖更新的文件)</span><br><span class="line">-l, --links 保留软链结</span><br><span class="line">-L, --copy-links 想对待常规文件一样处理软链结</span><br><span class="line">--copy-unsafe-links 仅仅拷贝指向SRC路径目录树以外的链结</span><br><span class="line">--safe-links 忽略指向SRC路径目录树以外的链结</span><br><span class="line">-H, --hard-links 保留硬链结</span><br><span class="line">-p, --perms 保持文件权限</span><br><span class="line">-o, --owner 保持文件属主信息</span><br><span class="line">-g, --group 保持文件属组信息</span><br><span class="line">-D, --devices 保持设备文件信息</span><br><span class="line">-t, --<span class="built_in">times</span> 保持文件时间信息</span><br><span class="line">-S, --sparse 对稀疏文件进行特殊处理以节省DST的空间</span><br><span class="line">-n, --dry-run现实哪些文件将被传输</span><br><span class="line">-W, --whole-file 拷贝文件，不进行增量检测</span><br><span class="line">-x, --one-file-system 不要跨越文件系统边界</span><br><span class="line">-B, --block-size=SIZE 检验算法使用的块尺寸，默认是700字节</span><br><span class="line">-e, --rsh=COMMAND 指定使用rsh、ssh方式进行数据同步</span><br><span class="line">--rsync-path=PATH 指定远程服务器上的rsync命令所在路径信息</span><br><span class="line">-C, --cvs-exclude 使用和CVS一样的方法自动忽略文件，用来排除那些不希望传输的文件</span><br><span class="line">--existing 仅仅更新那些已经存在于DST的文件，而不备份那些新创建的文件</span><br><span class="line">--delete 删除那些DST中SRC没有的文件</span><br><span class="line">--delete-excluded 同样删除接收端那些被该选项指定排除的文件</span><br><span class="line">--delete-after 传输结束以后再删除</span><br><span class="line">--ignore-errors 及时出现IO错误也进行删除</span><br><span class="line">--max-delete=NUM 最多删除NUM个文件</span><br><span class="line">--partial 保留那些因故没有完全传输的文件，以是加快随后的再次传输</span><br><span class="line">--force 强制删除目录，即使不为空</span><br><span class="line">--numeric-ids 不将数字的用户和组ID匹配为用户名和组名</span><br><span class="line">--timeout=TIME IP超时时间，单位为秒</span><br><span class="line">-I, --ignore-times 不跳过那些有同样的时间和长度的文件</span><br><span class="line">--size-only 当决定是否要备份文件时，仅仅察看文件大小而不考虑文件时间</span><br><span class="line">--modify-window=NUM 决定文件是否时间相同时使用的时间戳窗口，默认为0</span><br><span class="line">-I 为所有更新输出一个更改摘要</span><br><span class="line">-T --temp-dir=DIR 在DIR中创建临时文件</span><br><span class="line">--compare-dest=DIR 同样比较DIR中的文件来决定是否需要备份</span><br><span class="line">-P 等同于 --partial</span><br><span class="line">--progress 显示备份过程</span><br><span class="line">-z, --compress 对备份的文件在传输时进行压缩处理</span><br><span class="line">--exclude=PATTERN 指定排除不需要传输的文件模式</span><br><span class="line">--include=PATTERN 指定不排除而需要传输的文件模式</span><br><span class="line">--exclude-from=FILE 排除FILE中指定模式的文件</span><br><span class="line">--include-from=FILE 不排除FILE指定模式匹配的文件</span><br><span class="line">--version 打印版本信息</span><br><span class="line">--address 绑定到特定的地址</span><br><span class="line">--config=FILE 指定其他的配置文件，不使用默认的rsyncd.conf文件</span><br><span class="line">--port=PORT 指定其他的rsync服务端口</span><br><span class="line">--blocking-io 对远程shell使用阻塞IO</span><br><span class="line">-stats 给出某些文件的传输状态</span><br><span class="line">--progress 在传输时现实传输过程</span><br><span class="line">--log-format=formAT 指定日志文件格式</span><br><span class="line">--password-file=FILE 从FILE中得到密码</span><br><span class="line">--bwlimit=KBPS 限制I/O带宽，KBytes per second</span><br><span class="line">-h, --<span class="built_in">help</span> 显示帮助信息</span><br><span class="line"></span><br><span class="line">* **tmux配置前缀为ctrl+a **</span><br><span class="line">退出tmux</span><br><span class="line">``` bash</span><br><span class="line">Step 1:</span><br><span class="line">Vim ~/.tmux.conf</span><br><span class="line">Step 2:在文件中输入</span><br><span class="line"><span class="built_in">set</span> -g prefix C-a</span><br><span class="line">Unbind C-b</span><br></pre></td></tr></table></figure>
<p>重新启动tmux即可生效,或者推出编辑之后，按ctrl+b-&gt;:进入命令行，再输入source-file ~/.tmux.conf</p>
</li>
<li><p>**tar解压与压缩文件 **<br>解压文件夹到指定文件夹</p>
<figure class="highlight bash"><table><tr><td class="code"><pre><span class="line">tar -zxvf name.tar -C ./your_dir</span><br></pre></td></tr></table></figure>
<p>压缩文件夹到指定文件</p>
<figure class="highlight bash"><table><tr><td class="code"><pre><span class="line">Tar -zcvf name.tar ./you_dir</span><br></pre></td></tr></table></figure></li>
<li><p><strong>查看内存或者cpu使用排行</strong></p>
<figure class="highlight bash"><table><tr><td class="code"><pre><span class="line">ps auxw --sort=%cpu  <span class="comment">#cpu使用排行</span></span><br><span class="line">ps auxw --sort=%MEM  <span class="comment">#内存使用排行</span></span><br></pre></td></tr></table></figure></li>
<li><p><strong>anaconda环境中不实用环境自带的python的问题</strong><br>原因：未知，猜测可能是在PATH搜索中没有优先搜索到环境中的python<br>解决办法：<br>在profile配置环境中的路径在系统python和anacda自带python的前边</p>
<figure class="highlight bash"><table><tr><td class="code"><pre><span class="line">PATH=<span class="string">&quot;/home/yxwang/anaconda3/envs/tensorflow/bin/&quot;</span>:<span class="variable">$PATH</span></span><br></pre></td></tr></table></figure>
<p>退出出并source ～/.profile</p>
</li>
<li><p>**top **<br>top命令中使用 shift + m按照内存使用进行排序, shift + c对cpu占用进行排序</p>
</li>
<li><p><strong>anaconda改变路径的一些问题</strong><br>1, anaconda创建环境的时候入指定路径，而不是默认在～/annaconda/envs下</p>
<figure class="highlight bash"><table><tr><td class="code"><pre><span class="line">Conda create -p /ssd/yuxiong/.conda/envs/tensorflow python=3.6</span><br><span class="line"><span class="comment">#激活</span></span><br><span class="line">Source activate /ssd/yuxiong/.conda/envs/tensorflow</span><br></pre></td></tr></table></figure></li>
</ul>
<p>2 conda install 软件的时候指定下载目录而不是下载到home目录下</p>
<figure class="highlight bash"><table><tr><td class="code"><pre><span class="line"><span class="comment">#设置环境变量</span></span><br><span class="line">``` bash</span><br><span class="line">Export CONDA_PKGS_DIRS=/ssd/yuxiong/conda/pags/</span><br></pre></td></tr></table></figure>
<p>后边安装可能会出现设定为文件夹访问权限问题，解决办法</p>
<figure class="highlight bash"><table><tr><td class="code"><pre><span class="line">Sudo chown 1002:1002 /ssd/yuxiong/conda/pkgs</span><br></pre></td></tr></table></figure>
<p>这样安装之后可能会有一些路径问题，如</p>
<figure class="highlight bash"><table><tr><td class="code"><pre><span class="line">Import numpy as np</span><br></pre></td></tr></table></figure>
<p>报错： cannot import name ‘_mklinit’ from ‘numpy’<br>这是路径的问题，解决方法是配置环境所在的路径<br>打开～/.profile,键入</p>
<figure class="highlight bash"><table><tr><td class="code"><pre><span class="line"><span class="built_in">export</span> PATH=<span class="string">&quot;/ssd/yuxiong/.conda/envs/tensorflow/bin&quot;</span>:<span class="variable">$PATH</span></span><br><span class="line">iexport PATH=<span class="string">&quot;/ssd/yuxiong/.conda/envs/tensorflow/lib/python3.6/site-packages&quot;</span>:<span class="variable">$PATH</span></span><br></pre></td></tr></table></figure>
<p>退出， source ～/.profile重新激活环境，进入即可</p>
<ul>
<li><p><strong>多版本cuda与cudnn安装</strong><br>如果你的tensorflow需要的版本是1.12.0,需要的cuda版本是9.0，但是机器上已经安装了cuda 10，这时候，只需要在<br>你的conda环境中，安装cudatoolkit和cudnn就可以了：/Users/wang/Documents/SMILES-近期工作进展报告_王亚雄_8_3.pdf</p>
<figure class="highlight bash"><table><tr><td class="code"><pre><span class="line">Conda install cudatoolkit=9.0</span><br><span class="line">Conda install cudnn=7.1.2</span><br></pre></td></tr></table></figure>
<p>之后就可以直接import tensorflow as tf</p>
</li>
<li><p><strong>linux系统中常用的配置/Users/wang/Hexo Blog/source/_posts/miscellanea.md文件与路径</strong><br>1, <em>配置文件：.bashrc .profile .bash_profile 的区别</em></p>
<figure class="highlight bash"><table><tr><td class="code"><pre><span class="line">.bashrc: 打开新的终端的时候回执行该文件中的命令和配置</span><br><span class="line">.bash_profile:登陆机器的时候执行，且只执行一次</span><br><span class="line">.profile: 该文件和.login,一起起到了bash_profile的作用,</span><br><span class="line">有时候在bash_profile配置的命令在登录时不起作用，这时可以在./profile中进行配置，例如nvidia-docker attach wangyx</span><br></pre></td></tr></table></figure>
<p>上述文件在用户目录和根目录下/etc都有，分别对当前用户和所有用户起作用，如果想理机起作用，只需source ～/.profile</p>
</li>
</ul>
<p>2, <em>三个路径变量：PATH， LD_LIBRARY_PATH和LIBRARY_PATH</em></p>
<figure class="highlight bash"><table><tr><td class="code"><pre><span class="line">PATH:可执行文件存放目录</span><br><span class="line">LIBRARY_PATH：程序编译期间查找动态链接库时，查找共享库的路径</span><br><span class="line">LD_LIBRARY_PATH：程序的加载运行期间查找动态链接库时，除了系统默认路径之外的其他路径</span><br></pre></td></tr></table></figure>

<p>3，<em>几个重要目录</em></p>
<figure class="highlight bash"><table><tr><td class="code"><pre><span class="line">/usr/<span class="built_in">local</span>:软件安装的系统级目录,相当于windows下的C:/Windows</span><br><span class="line">/opt: 用户级的附件安装目录，用于安装附件的软件</span><br><span class="line">/usr/bin:常用命令的目录</span><br><span class="line">/usr/<span class="built_in">local</span>/bin、/bin、/usr/<span class="built_in">local</span>/bin：常用的所有命令的文件</span><br><span class="line">/usr/sbin: 不是必要的系统管理目录</span><br><span class="line">/usr/include：C语言的头文件路径，</span><br><span class="line">/usr/lib：程序不变的数据文件，包括一些site-wide配置文件</span><br><span class="line">/var:系统一般运行时要改变的数据文件，每个系统都是特定的</span><br></pre></td></tr></table></figure>

<ul>
<li><p>**tqdm显示进度条 **</p>
<figure class="highlight bash"><table><tr><td class="code"><pre><span class="line">import tqdm </span><br><span class="line">from tqdm import trange</span><br><span class="line"></span><br><span class="line"><span class="comment">#索引便利</span></span><br><span class="line"><span class="keyword">for</span> I <span class="keyword">in</span> trange(100):</span><br><span class="line">   Pass</span><br><span class="line"></span><br><span class="line"><span class="comment">#职别便利列表</span></span><br><span class="line">For item <span class="keyword">in</span> tqdm.tqdm(a_list/iterator):</span><br><span class="line">   pass</span><br></pre></td></tr></table></figure></li>
<li><p><strong>linux服务器中添加新用户</strong><br>得有root权限</p>
<figure class="highlight bash"><table><tr><td class="code"><pre><span class="line">useradd wangyx <span class="comment">#添加用户名</span></span><br><span class="line">passwd wangyx <span class="comment">#为用户设置密码</span></span><br><span class="line">Mkdir /home/wangyx <span class="comment">#穿件home目录</span></span><br><span class="line">sudo chown -R linchao:linchao /home/linchao <span class="comment">#将目录权限付给用户</span></span><br><span class="line">useradd -d /home/wangyx wangyx <span class="comment">#给用户增加用户目录</span></span><br><span class="line">Userdel -r wangyx <span class="comment">#删除用户</span></span><br><span class="line"></span><br><span class="line"><span class="comment">#增加用户的sudo权限</span></span><br><span class="line">sudo usermod -aG sudo wangyx</span><br><span class="line"><span class="comment">#查看用户是否有sudo权限</span></span><br><span class="line">groups yaxiong</span><br><span class="line"><span class="comment">#列出所有具有sodu权限的用户</span></span><br><span class="line">getent group sudo</span><br></pre></td></tr></table></figure></li>
<li><p>**docker中git+url 错误：The SSL CA cert (path? access rights) **<br>解决方案：</p>
<figure class="highlight bash"><table><tr><td class="code"><pre><span class="line">apt-get install ca-certificates</span><br></pre></td></tr></table></figure>
<p>之后再次尝试即可。</p>
</li>
<li><p>**python format格式化控制输出精度 **</p>
<figure class="highlight bash"><table><tr><td class="code"><pre><span class="line"><span class="comment">#第一个表示多少位有效数字，第二个整数控制精度即小数点保留位数，前面的0， 1表示位置对应</span></span><br><span class="line"><span class="built_in">print</span>(<span class="string">&#x27;&#123;0:4.2f&#125;--&#123;1:8.4f&#125;&#x27;</span>.fomrat(prection, recall)) </span><br></pre></td></tr></table></figure></li>
<li><p><strong>python中f-string</strong><br>F-string中可以调用表达是或者函数,并且后面跟冒号可以控制精度(:.4f)</p>
<figure class="highlight bash"><table><tr><td class="code"><pre><span class="line">A = 2</span><br><span class="line">B = 3</span><br><span class="line"><span class="built_in">print</span>(f<span class="string">&#x27;A:&#123;A&#125;， B:&#123;B&#125;, A/B:&#123;A/B:.4f&#125;&#x27;</span>)</span><br><span class="line"><span class="comment">#注意括号内不能使用反斜杠\</span></span><br><span class="line"><span class="built_in">print</span>(f<span class="string">&#x27;His name is &#123;\&#x27;</span>Tom\<span class="string">&#x27;&#125;&#x27;</span>)<span class="comment">#错误</span></span><br><span class="line"><span class="built_in">print</span>(f<span class="string">&quot;His name is &#123;&#x27;Tom&#x27;&#125;&quot;</span>)<span class="comment">#使用不同的引号</span></span><br><span class="line">Str = f<span class="string">&#x27;&#123; &#123;A&#125; &#125;&#x27;</span><span class="comment">#插入大括号</span></span><br></pre></td></tr></table></figure></li>
<li><p><strong>docker中tmux error: Cann’t create soccer:permission denied</strong><br>解决方案1:</p>
<figure class="highlight bash"><table><tr><td class="code"><pre><span class="line"><span class="comment">#运行命令</span></span><br><span class="line">tmux -S ~/.tmsock new -ADsCyg</span><br><span class="line"><span class="comment">#tmux -a时任然会报错，解决方案在～/.bashrc中加入,并source</span></span><br><span class="line"><span class="built_in">alias</span> tmux=<span class="string">&#x27;tmux -S ~/.tmsock new -ADsCyg&#x27;</span></span><br></pre></td></tr></table></figure>
<p>解决方案2:</p>
<figure class="highlight bash"><table><tr><td class="code"><pre><span class="line"><span class="comment">#step1: add following command in ~/.bashrc</span></span><br><span class="line"><span class="built_in">export</span> TMUX_TMPDIR=~/.tmux/tmp</span><br><span class="line"></span><br><span class="line"><span class="comment">#step2:create folder &amp; change to write mode</span></span><br><span class="line">mkdir -p ~/.tmux/tmp</span><br><span class="line">Chmod 777 -R ~/.tmux/tmp</span><br><span class="line"></span><br><span class="line"><span class="comment">#step3: source/reload the bashrc file</span></span><br><span class="line"><span class="built_in">source</span> ~/.bashrc</span><br><span class="line"></span><br><span class="line"><span class="comment">#step4: re-run the tmux, and it would be work normally</span></span><br><span class="line">tmux</span><br></pre></td></tr></table></figure></li>
<li><p><strong>ubuntu更新显卡驱动</strong><br>Ubuntu 16.04, CUDA 10.2, NVIDA<br>Reference: <a href="https://askubuntu.com/questions/1077061/how-do-i-install-nvidia-and-cuda-drivers-into-ubuntu">https://askubuntu.com/questions/1077061/how-do-i-install-nvidia-and-cuda-drivers-into-ubuntu</a> (the second answer)</p>
<figure class="highlight bash"><table><tr><td class="code"><pre><span class="line">step 1: sudo rm /etc/apt/sources.list.d/cuda*</span><br><span class="line">sudo apt remove nvidia-cuda-toolkit</span><br><span class="line"></span><br><span class="line">step 2: sudo apt remove nvidia-*</span><br><span class="line"></span><br><span class="line">step 3: sudo apt update</span><br><span class="line"></span><br><span class="line">step 4: sudo apt-key adv --fetch-keys  http://developer.download.nvidia.com/compute/cuda/repos/ubuntu1604/x86_64/7fa2af80.pub</span><br><span class="line"></span><br><span class="line">step 5: sudo bash -c <span class="string">&#x27;echo &quot;deb http://developer.download.nvidia.com/compute/cuda/repos/ubuntu1604/x86_64 /&quot; &gt; /etc/apt/sources.list.d/cuda.list&#x27;</span></span><br><span class="line"></span><br><span class="line">step 6: sudo apt update</span><br><span class="line"></span><br><span class="line">step7: sudo apt install cuda-10-2 <span class="comment">#此步骤更新cuda和对应的driver</span></span><br></pre></td></tr></table></figure></li>
<li><p>**Submitted to arXiv **<br>1, Journal(reference: begin{item} in main.tex)</p>
<figure class="highlight bash"><table><tr><td class="code"><pre><span class="line">step 1: <span class="keyword">in</span> overleaf, choose the submit --&gt; arxiv-&gt;download the zip file (would auto deal with some dir problems)(texStudio打开文件并编译)</span><br><span class="line">step 2: <span class="keyword">in</span> main.tex, <span class="keyword">if</span> you use the hyperref like <span class="string">&#x27;\usepackage&#123;options ... &#125;[hyperref]&#x27;</span> change to</span><br><span class="line">\usepackage&#123;hyperref&#125;</span><br><span class="line">\hypersetup&#123;colorlinks, allcolors=black&#125; </span><br><span class="line">step 3: build a new file named ref.bbl(名称尽量和主.tex文件名相同，例如tmi_main.tex==&gt;tmi_main.bbl) and cut the content from \begin&#123;thebibliography&#125;[1] to \end&#123;thebibliograph&#125;(包含环境变量), ie, all references</span><br><span class="line">step 4: delete the content mentioned <span class="keyword">in</span> step3 and add the following contents <span class="keyword">in</span> its original place:</span><br><span class="line">\nocite&#123;*&#125; <span class="comment">#注意不是notice</span></span><br><span class="line">\input&#123;tmi_main.bbl&#125;</span><br><span class="line">step 5: notice the dir problems, <span class="keyword">if</span> your project under name <span class="string">&#x27;tcyb&#x27;</span>, the image path, <span class="string">&#x27;tcyb/first_show.png&#x27;</span> should change to <span class="string">&#x27;first_show.png&#x27;</span> </span><br><span class="line"></span><br><span class="line"><span class="comment">#会议版本</span></span><br><span class="line">step 2和step 4也是必须的</span><br></pre></td></tr></table></figure></li>
<li><p><strong>查找当前目录下是否包含某个字符串的所有文件</strong></p>
<figure class="highlight bash"><table><tr><td class="code"><pre><span class="line">grep -rn <span class="string">&quot;nibabel&quot;</span> * <span class="comment">#递归查找所有目录下包含“nibabel“的文件</span></span><br></pre></td></tr></table></figure></li>
<li><p>** matplotlib， opencv， Pillow, script.misc.imread读入的图片**</p>
<figure class="highlight bash"><table><tr><td class="code"><pre><span class="line">Opencv读取的图片的通道0-2对应的BGR颜色, 取值范围0-255， 数据类型numpy.ndarry</span><br><span class="line">matplotlib.image.imread读入的图片是RGB颜色,取之范围0-255， 数据类型numpy.ndarray</span><br><span class="line">scripy.misc.imread/scipy.ndimage.imread 读取的图像为RGB，范围0-255， 数据类型numpy.ndarray</span><br><span class="line">Pillow(PIL)读取图像对象，供后续处理</span><br><span class="line">skimage.io.imread通道顺序RGB，取值范围0-255， 数据类型numpy.ndarray</span><br></pre></td></tr></table></figure></li>
<li><p><strong>opencv保存与读取四通道图片</strong></p>
<figure class="highlight bash"><table><tr><td class="code"><pre><span class="line">import cv2</span><br><span class="line"><span class="comment">#save the image, img是一个四通道ndarray</span></span><br><span class="line">Cv2.imwrite(<span class="string">&#x27;test.png&#x27;</span>, img, [int(cv2.IMWRITE_PNG_COMPRESSION), 9])</span><br><span class="line"><span class="comment">#读取图片</span></span><br><span class="line">Im = cv2.imread(path, cv2.IMREAD_UNCHANGED)</span><br><span class="line"></span><br></pre></td></tr></table></figure></li>
<li><p><strong>tensorboard启动时error：Not a TBLoader or TBPlugin subclass: …</strong></p>
<figure class="highlight bash"><table><tr><td class="code"><pre><span class="line"><span class="comment">#解决方案</span></span><br><span class="line">Pip uninstall tensorboard-plugin-wit==1.6.0.post3</span><br></pre></td></tr></table></figure></li>
<li><p><strong>segmentation可视化的包</strong></p>
<figure class="highlight bash"><table><tr><td class="code"><pre><span class="line">From skimage.segmentation import mark_boundaries, find_boundaries</span><br><span class="line"><span class="comment">#mark_boundaries:根据区域的label画出边界</span></span><br><span class="line"><span class="comment">#image_array:image data 0-255</span></span><br><span class="line"><span class="comment">#sp_arry: label data int</span></span><br><span class="line">Out = mark_boundaries(image_array, sp_array)</span><br><span class="line">Cv2.imwrite(<span class="string">&#x27;vis.png&#x27;</span>, out*255)</span><br><span class="line"></span><br><span class="line"><span class="comment">#find_boundaries:根据label显示出边界（1表示边界）</span></span><br><span class="line"><span class="comment">#output the boundary pixel</span></span><br><span class="line">Out = find_boundaries(sp_arr)</span><br><span class="line"></span><br></pre></td></tr></table></figure></li>
<li><p><strong>pytorch分布式训练</strong><br>step1: 安装apex</p>
<figure class="highlight bash"><table><tr><td class="code"><pre><span class="line">git <span class="built_in">clone</span> --recursive https://github.com/NVIDIA/apex.git</span><br><span class="line"><span class="built_in">cd</span> apex</span><br><span class="line">pip install . <span class="comment">#注意点号</span></span><br><span class="line"><span class="comment">#最好的安装是，cuda11报错未安装成功,cuda10成功：</span></span><br><span class="line">pip install -v --no-cache-dir --global-option=<span class="string">&quot;--cpp_ext&quot;</span> --global-option=<span class="string">&quot;--cuda_ext&quot;</span> ./</span><br><span class="line"></span><br><span class="line"><span class="comment">#note:可能安装之后：import apex报错module not found, 解决</span></span><br><span class="line"><span class="comment">#</span></span><br><span class="line">Import sys</span><br><span class="line">sys.path.append(<span class="string">&#x27;/home/yaxiong/apex&#x27;</span>)</span><br></pre></td></tr></table></figure>
<p>step2: 分布式训练</p>
<figure class="highlight bash"><table><tr><td class="code"><pre><span class="line"><span class="comment">#代码示例</span></span><br><span class="line">import os</span><br><span class="line">from datetime import datetime</span><br><span class="line">import argparse</span><br><span class="line">import torch.multiprocessing as mp</span><br><span class="line">import torchvision</span><br><span class="line">import torchvision.transforms as transforms</span><br><span class="line">import torch</span><br><span class="line">import torch.nn as nn</span><br><span class="line">import torch.distributed as dist</span><br><span class="line">from apex.parallel import DistributedDataParallel as DDP</span><br><span class="line">from apex import amp</span><br><span class="line"></span><br><span class="line">class ConvNet(nn.Module):</span><br><span class="line">    def __init__(self, num_classes=10):</span><br><span class="line">        super(ConvNet, self).__init__()</span><br><span class="line">        self.layer1 = nn.Sequential(</span><br><span class="line">            nn.Conv2d(1, 16, kernel_size=5, stride=1, padding=2),</span><br><span class="line">            nn.BatchNorm2d(16),</span><br><span class="line">            nn.ReLU(),</span><br><span class="line">            nn.MaxPool2d(kernel_size=2, stride=2))</span><br><span class="line">                </span><br><span class="line">        self.layer2 = nn.Sequential(</span><br><span class="line">            nn.Conv2d(16, 32, kernel_size=5, stride=1, padding=2),</span><br><span class="line">            nn.BatchNorm2d(32),</span><br><span class="line">            nn.ReLU(),</span><br><span class="line">            nn.MaxPool2d(kernel_size=2, stride=2))</span><br><span class="line">        self.fc = nn.Linear(7*7*32, num_classes)</span><br><span class="line"></span><br><span class="line">    def forward(self, x):</span><br><span class="line">        out = self.layer1(x)</span><br><span class="line">        out = self.layer2(out)</span><br><span class="line">        out = out.reshape(out.size(0), -1)</span><br><span class="line">        out = self.fc(out)</span><br><span class="line">        <span class="built_in">return</span> out</span><br><span class="line"></span><br><span class="line"></span><br><span class="line">def train(gpu, args):</span><br><span class="line">    <span class="comment">#计算传入的gpu在总共gpu的顺序号，如果只是一个机器，就是gpu的id</span></span><br><span class="line">    <span class="comment">#******************************************************#</span></span><br><span class="line">    rank = args.nr * args.gpus + gpu                              </span><br><span class="line">    <span class="comment">#初始化方式为env://, 需要设置四个环境变量：MASTER_PORT, MASTER_ADDR, WORD_SIZE, RANK</span></span><br><span class="line">    <span class="comment">#前两个已设置，后两个作为参数传入</span></span><br><span class="line">    dist.init_process_group(                                   </span><br><span class="line">        backend=<span class="string">&#x27;nccl&#x27;</span>,                                         </span><br><span class="line">        init_method=<span class="string">&#x27;env://&#x27;</span>,                                   </span><br><span class="line">        world_size=args.world_size,                              </span><br><span class="line">        rank=rank                                               </span><br><span class="line">    )</span><br><span class="line">    <span class="comment">#******************************************************#</span></span><br><span class="line">    torch.manual_seed(0)</span><br><span class="line">    model = ConvNet()</span><br><span class="line">    torch.cuda.set_device(gpu)</span><br><span class="line">    model.cuda(gpu)</span><br><span class="line">    batch_size = 100</span><br><span class="line">    <span class="comment"># define loss function (criterion) and optimizer</span></span><br><span class="line">    criterion = nn.CrossEntropyLoss().cuda(gpu)</span><br><span class="line">    optimizer = torch.optim.SGD(model.parameters(), 1e-4)</span><br><span class="line"></span><br><span class="line">    <span class="comment">#******************************************************#	</span></span><br><span class="line">    <span class="comment">#如果模型中有batchnorm，使用同步batchnorm以获得更好的结果</span></span><br><span class="line">    model = torch.nn.SyncBatchNorm.convert_sync_batchnorm(model)      </span><br><span class="line"></span><br><span class="line">    <span class="comment">#包装模型，</span></span><br><span class="line">    model = nn.parallel.DistributedDataParallel(model,</span><br><span class="line">        device_ids=[gpu])</span><br><span class="line"></span><br><span class="line">    <span class="comment">#-------如果要同时分布式训练并且使用fp16，上面两句需要替换为（后续loss反向也需要改动)----------#</span></span><br><span class="line">    model = apex.parallel.convert_syncbn_model(model)</span><br><span class="line">    model, optimizer = amp.initialize(model, optimizer, opt_level=<span class="string">&#x27;O1&#x27;</span>)</span><br><span class="line">    model = DDP(model)</span><br><span class="line">    <span class="comment">#-------------------------------------------------------------#     </span></span><br><span class="line">    <span class="comment">#******************************************************#</span></span><br><span class="line">    </span><br><span class="line">    <span class="comment"># Data loading code</span></span><br><span class="line">    train_dataset = torchvision.datasets.MNIST(root=<span class="string">&#x27;./data&#x27;</span>,</span><br><span class="line">        train=True,</span><br><span class="line">        transform=transforms.ToTensor(),</span><br><span class="line">        download=True)</span><br><span class="line"></span><br><span class="line">   <span class="comment">#******************************************************#</span></span><br><span class="line">   <span class="comment">#负责给各个gpu切分数据</span></span><br><span class="line">    train_sampler = torch.utils.data.distributed.DistributedSampler(</span><br><span class="line">        train_dataset,</span><br><span class="line">        num_replicas=args.world_size,</span><br><span class="line">        rank=rank</span><br><span class="line">    )</span><br><span class="line">    <span class="comment">#******************************************************#</span></span><br><span class="line">    </span><br><span class="line">   <span class="comment">#dataloader中shuffle必须为false，指定sampler为分配的sampler</span></span><br><span class="line">   train_loader = torch.utils.data.DataLoader(dataset=train_dataset,</span><br><span class="line">        batch_size=batch_size,</span><br><span class="line">        shuffle=False,</span><br><span class="line">        num_workers=2,                                                                                                                                                                                 </span><br><span class="line">        pin_memory=True, sampler=train_sampler)</span><br><span class="line"></span><br><span class="line">    start = datetime.now()</span><br><span class="line">    total_step = len(train_loader)</span><br><span class="line">    <span class="keyword">for</span> epoch <span class="keyword">in</span> range(args.epochs):</span><br><span class="line">        <span class="keyword">for</span> i, (images, labels) <span class="keyword">in</span> enumerate(train_loader):</span><br><span class="line">            images = images.cuda(non_blocking=True)</span><br><span class="line">            labels = labels.cuda(non_blocking=True)</span><br><span class="line">            outputs = model(images)</span><br><span class="line">            loss = criterion(outputs, labels)</span><br><span class="line"></span><br><span class="line">            optimizer.zero_grad()</span><br><span class="line">            loss.backward()</span><br><span class="line">	    <span class="comment">#-----如果使用fp16，需要loss.backward替换为：----------------#</span></span><br><span class="line">	    with amp.scale_loss(loss, optimizer) as scaled_loss:</span><br><span class="line">                scaled_loss.backward()</span><br><span class="line">            <span class="comment">#-------------------------------------------------------#</span></span><br><span class="line"></span><br><span class="line">            optimizer.step()</span><br><span class="line">            <span class="keyword">if</span> (i + 1) % 100 == 0 and gpu == 0:</span><br><span class="line">                <span class="built_in">print</span>(<span class="string">&#x27;Epoch [&#123;&#125;/&#123;&#125;], Step [&#123;&#125;/&#123;&#125;], Loss: &#123;:.4f&#125;&#x27;</span>.format(</span><br><span class="line">                epoch + 1,</span><br><span class="line">                args.epochs,</span><br><span class="line">                i + 1,</span><br><span class="line">                total_step,</span><br><span class="line">                loss.item())</span><br><span class="line">                )</span><br><span class="line"></span><br><span class="line">    <span class="keyword">if</span> gpu == 0:</span><br><span class="line">        <span class="built_in">print</span>(<span class="string">&quot;Training complete in: &quot;</span> + str(datetime.now() - start))</span><br><span class="line"></span><br><span class="line">def main():</span><br><span class="line">    parser = argparse.ArgumentParser()</span><br><span class="line">    parser.add_argument(<span class="string">&#x27;-n&#x27;</span>, <span class="string">&#x27;--nodes&#x27;</span>, default=1, <span class="built_in">type</span>=int, metavar=<span class="string">&#x27;N&#x27;</span>)</span><br><span class="line">    parser.add_argument(<span class="string">&#x27;-g&#x27;</span>, <span class="string">&#x27;--gpus&#x27;</span>, default=1, <span class="built_in">type</span>=int,</span><br><span class="line">                       <span class="built_in">help</span>=<span class="string">&#x27;number of gpus per node&#x27;</span>)</span><br><span class="line">    parser.add_argument(<span class="string">&#x27;-nr&#x27;</span>, <span class="string">&#x27;--nr&#x27;</span>, default=0, <span class="built_in">type</span>=int,</span><br><span class="line">                       <span class="built_in">help</span>=<span class="string">&#x27;ranking within the nodes&#x27;</span>)</span><br><span class="line">    parser.add_argument(<span class="string">&#x27;--epochs&#x27;</span>, default=200, <span class="built_in">type</span>=int, metavar=<span class="string">&#x27;N&#x27;</span>,</span><br><span class="line">                       <span class="built_in">help</span>=<span class="string">&#x27;number of total epochs to run&#x27;</span>)</span><br><span class="line">    args = parser.parse_args()</span><br><span class="line">    </span><br><span class="line">    <span class="comment">#step 1:设置控制主机的地址，如果为本地主机，即设置为127.0.0.1，并设置端口</span></span><br><span class="line">    args.world_size = args.gpus * args.nodes</span><br><span class="line">    <span class="comment">#******************************************************#</span></span><br><span class="line">    os.environ[<span class="string">&#x27;MASTER_ADDR&#x27;</span>] = <span class="string">&#x27;127.0.0.1&#x27;</span></span><br><span class="line">    os.environ[<span class="string">&#x27;MASTER_PORT&#x27;</span>] = <span class="string">&#x27;8888&#x27;</span> </span><br><span class="line">    <span class="comment">#mp的作用是启动该节点的所有进程，每个节点都会运行train函数，其中i从0-args.gpus-1(节点机器的总共可用的gpu数量）</span></span><br><span class="line">    <span class="comment">#即函数是将程序按照GPU id进行部署到每一个gpu上</span></span><br><span class="line">    mp.spawn(train, nprocs=args.gpus, args=(args,))</span><br><span class="line">    <span class="comment">#******************************************************#</span></span><br><span class="line">    train(0, args)</span><br><span class="line"></span><br><span class="line"><span class="keyword">if</span> __name__ == <span class="string">&#x27;__main__&#x27;</span>:</span><br><span class="line">    main()</span><br><span class="line"></span><br><span class="line"><span class="comment">#===============命令行中输入命令开始训练======================#</span></span><br><span class="line">CUDA_VISIBLE_DEVICES=1,2 python pytorch_dist.py -n 1 -g 2 -nr 0</span><br></pre></td></tr></table></figure>
<p>注意，在validation的时候，需要统计各个进程的结果</p>
<figure class="highlight bash"><table><tr><td class="code"><pre><span class="line">validate_loss = loss(input, target)</span><br><span class="line">validate_loss = reduce_tensor(validate_loss)</span><br><span class="line"></span><br><span class="line">def reduce_tensor(tensor, world_size=2):</span><br><span class="line">    dist.barrier()</span><br><span class="line">    dist.all_reduce(tensor, op=torch.distributed.ReduceOp.SUM)<span class="comment">#或者op=dist.reduce_op.SUM</span></span><br><span class="line">    tensor =  tensor / world_size</span><br><span class="line">    </span><br><span class="line">    <span class="built_in">return</span> tensor</span><br></pre></td></tr></table></figure></li>
<li><p><strong>使用pudb进行多线程调试</strong></p>
<figure class="highlight bash"><table><tr><td class="code"><pre><span class="line"><span class="comment">#step 1:首先安装pudb</span></span><br><span class="line">pip install pudb</span><br><span class="line"></span><br><span class="line"><span class="comment">#step 2:在程序中</span></span><br><span class="line">Import pub.remote import set_trace</span><br><span class="line"><span class="comment">#在想要走停住的地方插入</span></span><br><span class="line">set_trace() <span class="comment">#或者设置窗口大小set_trace(term_size=(80, 24))</span></span><br><span class="line"></span><br><span class="line"><span class="comment">#step 3:打开另一个终端窗口输入</span></span><br><span class="line">telnet into 127.0.0.1 6899.</span><br><span class="line"></span><br><span class="line"><span class="comment">#重要的快捷键</span></span><br><span class="line">ctrl + x 进入交互模式，类似pdb，但是上下箭头被占用, 再次ctrl +x 推出交互模式</span><br><span class="line">Ctrl + p 进行设置，包括设置主题</span><br><span class="line">？ 帮助</span><br><span class="line"></span><br></pre></td></tr></table></figure></li>
<li><p>** caffe 安装的小技巧**<br>1 如果cmake出错/或者cmake之后make出错，报变量找不到的错误，一长串, 试试设置c++版本为11： </p>
<figure class="highlight bash"><table><tr><td class="code"><pre><span class="line">cmake -DCMAKE_CXX_FLAGS=<span class="string">&#x27;-std=c++11&#x27;</span> ..</span><br></pre></td></tr></table></figure>
<p>之后再make</p>
</li>
</ul>
<p>2 Error： cudnn.hpp(114): error : too few arguments in function call</p>
<figure class="highlight bash"><table><tr><td class="code"><pre><span class="line"><span class="comment">#修改cudnn.h中的函数</span></span><br><span class="line"></span><br><span class="line">template &lt;typename Dtype&gt;</span><br><span class="line">inline void setConvolutionDesc(cudnnConvolutionDescriptor_t* conv,</span><br><span class="line">    cudnnTensorDescriptor_t bottom, cudnnFilterDescriptor_t filter,</span><br><span class="line">    int pad_h, int pad_w, int stride_h, int stride_w) &#123;</span><br><span class="line">  CUDNN_CHECK(cudnnSetConvolution2dDescriptor(*conv,</span><br><span class="line">      pad_h, pad_w, stride_h, stride_w, 1, 1, CUDNN_CROSS_CORRELATION));</span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line"><span class="comment">#改为</span></span><br><span class="line">template &lt;typename Dtype&gt;</span><br><span class="line">inline void setConvolutionDesc(cudnnConvolutionDescriptor_t* conv,</span><br><span class="line">    cudnnTensorDescriptor_t bottom, cudnnFilterDescriptor_t filter,</span><br><span class="line">    int pad_h, int pad_w, int stride_h, int stride_w) &#123;</span><br><span class="line"> </span><br><span class="line"><span class="comment">#if CUDNN_VERSION_MIN(6, 0, 0)</span></span><br><span class="line">	CUDNN_CHECK(cudnnSetConvolution2dDescriptor(*conv,</span><br><span class="line">		pad_h, pad_w, stride_h, stride_w, 1, 1, CUDNN_CROSS_CORRELATION,</span><br><span class="line">		dataType&lt;Dtype&gt;::<span class="built_in">type</span>));</span><br><span class="line"><span class="comment">#else</span></span><br><span class="line">	CUDNN_CHECK(cudnnSetConvolution2dDescriptor(*conv,</span><br><span class="line">		pad_h, pad_w, stride_h, stride_w, 1, 1, CUDNN_CROSS_CORRELATION));</span><br><span class="line"><span class="comment">#endif</span></span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<p>3 如果python3安装不成功试试python2.7</p>
<p>4, 安装一些自定义的层<br>安装一些项目自定义的层时，找这样的目录，该目录下有CMakeList.txt和src/caffe文件夹（或者类似名称），在该目录下创建build，并进行cmake .. ==&gt; make -j 命令进行安装</p>
<ul>
<li><p>** pip/apt搜索包**</p>
<figure class="highlight bash"><table><tr><td class="code"><pre><span class="line"><span class="comment">#在库中搜索安装包</span></span><br><span class="line">Pip/apt search opencv-python</span><br><span class="line"></span><br><span class="line"><span class="comment">#列出已经安装的包</span></span><br><span class="line">Pip list | grep opencv-python</span><br><span class="line"></span><br><span class="line"><span class="comment">#显示已经安装包的信息, 包括版本号,安装位置</span></span><br><span class="line">pip/apt show  opencv-python</span><br><span class="line"></span><br></pre></td></tr></table></figure></li>
<li><p><strong>python递归遍历文件夹得到所有文件的绝对路径</strong></p>
<figure class="highlight bash"><table><tr><td class="code"><pre><span class="line">Import os</span><br><span class="line"></span><br><span class="line">all_pathes = []</span><br><span class="line"><span class="keyword">for</span> root, <span class="built_in">dirs</span>, files <span class="keyword">in</span> os.walk(os.path.abspath(split)):</span><br><span class="line">   <span class="keyword">for</span> file <span class="keyword">in</span> files:</span><br><span class="line">   all_pathes.append(os.path.join(root, file))</span><br><span class="line"></span><br><span class="line">all_pathes =[item <span class="keyword">for</span> item <span class="keyword">in</span> all_pathes <span class="keyword">if</span> <span class="string">&#x27;.jpg&#x27;</span> <span class="keyword">in</span> item]</span><br></pre></td></tr></table></figure></li>
<li><p>** 整体迁移anaconda目录**<br>参考链接： <a href="https://my.oschina.net/u/4387680/blog/4318430">https://my.oschina.net/u/4387680/blog/4318430</a><br>注意：如果按照修改后source activate pytorch不行，请尝试conda activate pytorch</p>
</li>
<li><p><strong>vim配置NERDTRee</strong></p>
<figure class="highlight bash"><table><tr><td class="code"><pre><span class="line">1，安装Vundle</span><br><span class="line">mkdir -p ~/.vim/bundle</span><br><span class="line">git <span class="built_in">clone</span> https://github.com/VundleVim/Vundle.vim.git ~/.vim/bundle/Vundle.vim</span><br><span class="line"></span><br><span class="line">2，打开.vimrc添加如下内容</span><br><span class="line"><span class="built_in">set</span> nocompatible       <span class="string">&quot; be iMproved, required</span></span><br><span class="line"><span class="string">filetype off         &quot;</span> required</span><br><span class="line"> </span><br><span class="line"><span class="string">&quot; set the runtime path to include Vundle and initialize</span></span><br><span class="line"><span class="string">set rtp+=~/.vim/bundle/Vundle.vim</span></span><br><span class="line"><span class="string">call vundle#begin()</span></span><br><span class="line"><span class="string">&quot;</span> <span class="built_in">let</span> Vundle manage Vundle, required</span><br><span class="line">Plugin <span class="string">&#x27;VundleVim/Vundle.vim&#x27;</span></span><br><span class="line"> </span><br><span class="line"><span class="string">&quot; Keep Plugin commands between vundle#begin/end.</span></span><br><span class="line"><span class="string">&quot;</span> 安装其它插件</span><br><span class="line"> </span><br><span class="line"><span class="string">&quot; All of your Plugins must be added before the following line</span></span><br><span class="line"><span class="string">call vundle#end()      &quot;</span> required</span><br><span class="line">filetype plugin indent on  <span class="string">&quot; required</span></span><br><span class="line"><span class="string"></span></span><br><span class="line"><span class="string">3，安装NERDTree</span></span><br><span class="line"><span class="string">git clone https://github.com/preservim/nerdtree.git ~/.vim/bundle/nerdtree</span></span><br><span class="line"><span class="string"></span></span><br><span class="line"><span class="string">4，打开vimrc在call vundle#end()语句之前添加内容：</span></span><br><span class="line"><span class="string">Plugin &#x27;preservim/nerdtree&#x27;</span></span><br><span class="line"><span class="string"></span></span><br><span class="line"><span class="string">Vim打开文件，命令行下输入：NERDTree即可使用</span></span><br><span class="line"><span class="string"></span></span><br></pre></td></tr></table></figure></li>
<li><p>** Vim配置自动代码补全YouCompleteMe（docker环境中）**<br>假设已经安装好了Vundle</p>
<figure class="highlight bash"><table><tr><td class="code"><pre><span class="line">1，首先安装依赖库</span><br><span class="line">sudo apt-get install build-essential cmake python-dev python3-dev</span><br><span class="line"></span><br><span class="line">2，安装YouCompeleteMe，首先下载源码到指定的目录</span><br><span class="line">git <span class="built_in">clone</span> https://github.com/Valloric/YouCompleteMe.git ~/.vim/bundle/YCM</span><br><span class="line"></span><br><span class="line">3，获取相关依赖</span><br><span class="line">git submodule update --init --recursive</span><br><span class="line"></span><br><span class="line">4，安装YCM的语言依赖</span><br><span class="line">apt install mono-complete golang nodejs default-jdk npm</span><br><span class="line"></span><br><span class="line">5，编译,在YCM目录下，运行,all表示为所有的语言安装依赖</span><br><span class="line">./install.py --all</span><br><span class="line">如果安装总是不成功，尝试：</span><br><span class="line">/usr/bin/python3 install.py</span><br><span class="line"></span><br><span class="line">6，配置vimrc文件，打开vimrc文件</span><br><span class="line">在call vundle<span class="comment">#begin() 和call vundle#end()之间插入：</span></span><br><span class="line">Plugin <span class="string">&#x27;Valloric/YCM&#x27;</span></span><br><span class="line"></span><br><span class="line">7，在filetype plugin indent on  <span class="string">&quot;&quot;</span> required之后输入</span><br><span class="line"><span class="built_in">let</span> g:ycm_global_ycm_extra_conf = <span class="string">&#x27;/root/.vim/bundle/YCM/.ycm_extra_conf.py&#x27;</span></span><br><span class="line"><span class="string">&quot;&quot;</span>开启基于tag的补全，可以在这之后添加需要的标签路径</span><br><span class="line"><span class="string">&quot;&quot;</span><span class="built_in">let</span> g:ycm_collect_identifiers_from_tags_files = 1</span><br><span class="line"><span class="string">&quot;&quot;</span>开启语义补全</span><br><span class="line"><span class="built_in">let</span> g:ycm_seed_identifiers_with_syntax = 1</span><br><span class="line"><span class="string">&quot;&quot;</span>在接受补全后不分裂出一个窗口显示接受的项</span><br><span class="line"><span class="string">&quot;&quot;</span><span class="built_in">set</span> completeopt-=preview</span><br><span class="line"><span class="string">&quot;&quot;</span>不显示开启vim时检查ycm_extra_conf文件的信息</span><br><span class="line"><span class="built_in">let</span> g:ycm_confirm_extra_conf=0</span><br><span class="line"><span class="string">&quot;&quot;</span>每次重新生成匹配项，禁止缓存匹配项</span><br><span class="line"><span class="string">&quot;&quot;</span><span class="built_in">let</span> g:ycm_cache_omnifunc=0</span><br><span class="line"><span class="string">&quot;&quot;</span>在注释中也可以补全</span><br><span class="line"><span class="built_in">let</span> g:ycm_complete_in_comments=1</span><br><span class="line"><span class="string">&quot;&quot;</span>输入第一个字符就开始补全</span><br><span class="line"><span class="string">&quot;&quot;</span><span class="built_in">let</span> g:ycm_min_num_of_chars_for_completion=1</span><br><span class="line"><span class="string">&quot;&quot;</span>不查询ultisnips提供的代码模板补全，如果需要，设置成1即可</span><br><span class="line"><span class="built_in">let</span> g:ycm_use_ultisnips_completer=0</span><br><span class="line"></span><br><span class="line"><span class="string">&quot;&quot;</span>下面代码加上感觉最开的打开文件的时候，补全会变慢，不知道具体的功能</span><br><span class="line"><span class="built_in">let</span> g:ycm_show_diagnostics_ui = 0</span><br><span class="line"><span class="built_in">let</span> g:ycm_server_log_level = <span class="string">&#x27;info&#x27;</span></span><br><span class="line"><span class="built_in">let</span> g:ycm_min_num_identifier_candidate_chars = 2</span><br><span class="line"><span class="built_in">let</span> g:ycm_collect_identifiers_from_comments_and_strings = 1</span><br><span class="line"><span class="built_in">let</span> g:ycm_complete_in_strings=1</span><br><span class="line"><span class="built_in">let</span> g:ycm_key_invoke_completion = <span class="string">&#x27;&lt;c-z&gt;&#x27;</span></span><br><span class="line"></span><br><span class="line">noremap &lt;c-z&gt; &lt;NOP&gt;</span><br><span class="line"></span><br><span class="line"><span class="string">&quot;&quot;</span>导致变慢的原因是这个命令</span><br><span class="line"><span class="built_in">let</span> g:ycm_semantic_triggers =  &#123;</span><br><span class="line">                \ <span class="string">&#x27;c,cpp,python,java,go,erlang,perl&#x27;</span>: [<span class="string">&#x27;re!\w&#123;2&#125;&#x27;</span>],</span><br><span class="line">            \ <span class="string">&#x27;cs,lua,javascript&#x27;</span>: [<span class="string">&#x27;re!\w&#123;2&#125;&#x27;</span>],</span><br><span class="line">            \ &#125;</span><br><span class="line"></span><br><span class="line"></span><br><span class="line"><span class="comment">##====== 可能遇到的错误==================</span></span><br><span class="line">1，执行以上步骤之后，打开vim，报错</span><br><span class="line">The ycmd server SHUT DOWN (restart with :YcmRestartServer). Unexpected <span class="built_in">exit</span> code 1. Type <span class="string">&#x27;:YcmToggleLogs ycmd_57346_stderr_2hrqlh43.log&#x27;</span> to check the logs.</span><br><span class="line"></span><br><span class="line">解决方案：</span><br><span class="line">a 在YCM目录下，输入</span><br><span class="line">cat PYTHON_USED_DURING_BUILDING</span><br><span class="line">b,上述命令输出一个python路径，为YouCompleteMe所执行的python环境，假设输出为/usr/bin/python3</span><br><span class="line">/usr/bin/python3 install.py</span><br><span class="line"></span><br><span class="line">2，提示vim版本过低，不支持YouCompleteMe</span><br><span class="line">YouCompleteMe unavailable: requires Vim 8.1.2269+.</span><br><span class="line"></span><br><span class="line">解决方案：安装最新版的vim，依次执行</span><br><span class="line">add-apt-repository ppa:jonathonf/vim </span><br><span class="line">apt update </span><br><span class="line">apt install vim </span><br><span class="line"></span><br><span class="line">3，提示gcc版本过低： Your C++ compiler does NOT fully support C++17</span><br><span class="line"></span><br><span class="line">解决方案：升级gcc，依次执行</span><br><span class="line">apt-get install g++-8</span><br><span class="line">update-alternatives --install /usr/bin/gcc gcc /usr/bin/gcc-7 700 --slave /usr/bin/g++ g++ /usr/bin/g++-7</span><br><span class="line">update-alternatives --install /usr/bin/gcc gcc /usr/bin/gcc-8 800 --slave /usr/bin/g++ g++ /usr/bin/g++-8</span><br><span class="line"></span><br></pre></td></tr></table></figure></li>
<li><p>**vim配置AsyncRun命令 **<br>Async可以在不退出vim的状态下，执行各种bash命令<br>1 安装： 假设已经安装好了Vundle了</p>
<figure class="highlight bash"><table><tr><td class="code"><pre><span class="line"><span class="built_in">cd</span> ~/.vim/bundle</span><br><span class="line">git <span class="built_in">clone</span> https://github.com/skywind3000/asyncrun.vim asyncrun</span><br></pre></td></tr></table></figure>
<p>2 配置vimrc文件<br>在call vundle#begin() 和call vundle#end()之间插入：</p>
<figure class="highlight bash"><table><tr><td class="code"><pre><span class="line">Plugin <span class="string">&#x27;skywind3000/asyncrun&#x27;</span> (较老版本的Plugin==&gt;Bundle)</span><br></pre></td></tr></table></figure></li>
</ul>
<p>3 配置vimrc让asyncrun变得更好用</p>
<figure class="highlight bash"><table><tr><td class="code"><pre><span class="line">map &lt;F1&gt; :copen&lt;cr&gt; <span class="comment">#打开quick fix窗口</span></span><br><span class="line">map &lt;F2&gt; :AsyncRun  <span class="comment">#注意最后有一个空格</span></span><br></pre></td></tr></table></figure>

<ul>
<li>**vim安装ZoomWin实现最大最小化窗口 **<br>假设已经安装Vundle<br>1，安装<figure class="highlight bash"><table><tr><td class="code"><pre><span class="line"><span class="built_in">cd</span> ~/.vimrc/bundle</span><br><span class="line">git <span class="built_in">clone</span> https://github.com/vim-scripts/ZoomWin zoomwin</span><br></pre></td></tr></table></figure></li>
</ul>
<p>2, 配置vimrc<br>在call vundle#begin() 和call vundle#end()之间插入：</p>
<figure class="highlight bash"><table><tr><td class="code"><pre><span class="line">Plugin <span class="string">&#x27;vim-scripts/zoomwin&#x27;</span></span><br></pre></td></tr></table></figure>

<p>3, 映射快捷键，在vimrc文件中</p>
<figure class="highlight bash"><table><tr><td class="code"><pre><span class="line">map &lt;F4&gt; :ZoomWin&lt;cr&gt; <span class="comment">#利用F4在最大化当前窗口与返回</span></span><br></pre></td></tr></table></figure>

<ul>
<li><p>**命令行通过.torrent下载文件 **</p>
<figure class="highlight bash"><table><tr><td class="code"><pre><span class="line">step 1: 安装rtorrent</span><br><span class="line">sudo apt-get install rtorrent</span><br><span class="line"></span><br><span class="line">step 2：进入环境</span><br><span class="line">rtorrent</span><br><span class="line"></span><br><span class="line">step 3: 会车即可，load.normal&gt; 并粘贴.torrent文件的 url</span><br><span class="line"></span><br><span class="line">step 4: Down/Up 键按住选择要下载的文件（* 符号指示）</span><br><span class="line"></span><br><span class="line">step 5: ctrl +S开始下载</span><br><span class="line">   ctrl + D 停止下载</span><br><span class="line">   ctrl + Q 退出rtorrent</span><br></pre></td></tr></table></figure></li>
<li><p><strong>linux/Mac 测试制定端口的ip地址是否畅通</strong></p>
<figure class="highlight bash"><table><tr><td class="code"><pre><span class="line"><span class="comment">#linux: telnet</span></span><br><span class="line">telnet 192.168.1.102 3306</span><br><span class="line"></span><br><span class="line"><span class="comment">#Mac</span></span><br><span class="line">nc -vz -w 2 192.168.1.104 3306</span><br><span class="line"></span><br><span class="line">windows下得下载tcping: https://blog.csdn.net/qq_35487165/article/details/89458003</span><br></pre></td></tr></table></figure></li>
<li><p>**安装ninja包 **<br>参考<a href="https://blog.csdn.net/qiuguolu1108/article/details/103842556">https://blog.csdn.net/qiuguolu1108/article/details/103842556</a></p>
</li>
</ul>
]]></content>
  </entry>
  <entry>
    <title>program_tips</title>
    <url>/2018/10/24/program-tips/</url>
    <content><![CDATA[<h1 id="this-file-record-some-program-errors-and-corresponding-solution"><a href="#this-file-record-some-program-errors-and-corresponding-solution" class="headerlink" title="this file record some program errors and corresponding solution"></a>this file record some program errors and corresponding solution</h1><hr>
<ul>
<li><strong>error:</strong> python read the chinese file, open(filepath,encoding=’UTF-8’), however, there are still errors occuring: “UnicodeDecodeError: ‘gbk’ codec can’t decode byte 0x80 in position 205: illegal multibyte sequence”</li>
<li><em>solution</em>*: don’t open the file with “UTF-8” mode, and open(filepath, ‘rb’), read the content line by line. for example: line=f.readline(), line=line.decode(‘UTF-8’, <strong>‘ignore’</strong>), since the default parameter in decode function is strict, change it as “ignore” is ok.</li>
</ul>
<hr>
<ul>
<li>** pytorch changes the network detail in squence while maintain the pretrained parameters**<br>for example, we want the change the output class number for vgg16, while retrain the pretrained parameters. Since the vgg16 classifier consist of a squence, it is troublesome to change it. we can do by .children() API:<figure class="highlight bash"><table><tr><td class="code"><pre><span class="line">vgg=models.vgg16(pretrained=True)</span><br><span class="line">children=vgg.classifie.children()</span><br><span class="line">*obtain the layer that want to be saved *</span><br><span class="line">removed=list(vgg.classifier.children())[:-1]</span><br><span class="line">model.classifier=torch.nn.Sequential(*removed)</span><br><span class="line">*<span class="keyword">then</span>, add the new <span class="built_in">fc</span> layer*</span><br><span class="line">vgg.add_module(<span class="string">&#x27;fc&#x27;</span>, torch.nn.Linear(4096, out_num))</span><br></pre></td></tr></table></figure>
by this way, we can change the class number, while retained the pretrained parameters</li>
</ul>
<hr>
<ul>
<li><p><strong>pytorch error: forward() missing 1 required positional argument: ‘input’</strong><br>we want to declare a net using the vgg16’s feature part as a component of our network:</p>
<figure class="highlight bash"><table><tr><td class="code"><pre><span class="line">vgg16=models.vgg16(pretrained=True)</span><br><span class="line">class MyNET(nn.Module):</span><br><span class="line">    def __init__(self):</span><br><span class="line">        super(NET, self).__init__()</span><br><span class="line">        self.features=vgg16.features()</span><br><span class="line">        self.fc=vgg16.classifier()</span><br><span class="line">    def forward(self, x):</span><br><span class="line">        x=self.features(x)</span><br><span class="line">        x=self.fc(x)</span><br><span class="line">        <span class="built_in">return</span> x</span><br></pre></td></tr></table></figure>
<p>if we declare the network like above, the error will occur. Because self.feature = vgg16.features() means forward the data by vgg.features, however,we just want to declare the network.</p>
</li>
<li><ul>
<li>suolution: ** self.features = vgg16.features, self.fc = vgg16.classifier</li>
</ul>
</li>
<li><p>** About converting the Ascii code (like ‘\xe8\x87\xaa\xe5\xb7\xb1’) to Chinese **</p>
<figure class="highlight bash"><table><tr><td class="code"><pre><span class="line">f = open(file, <span class="string">&#x27;r&#x27;</span>)</span><br><span class="line">line = f.readline()</span><br><span class="line">we can direcyly <span class="built_in">print</span> the string, <span class="keyword">if</span> line is encoded by Ascii, the output will be chinese</span><br><span class="line"><span class="built_in">print</span>(line)</span><br></pre></td></tr></table></figure></li>
<li><p>** convert the pytorch data type **:</p>
<figure class="highlight bash"><table><tr><td class="code"><pre><span class="line">t1=torch.eye(3)</span><br><span class="line">convert t1 to ByteTensor</span><br><span class="line">t1_byte = t1.type(tf.ByteTensor)</span><br></pre></td></tr></table></figure></li>
<li><p>** pytorch Error: save_for_backward can only save input or output tensors, **<br>this error occurs since some medium tensor is not packed by Variable, pack it by Variable will be ok</p>
</li>
<li><p>**pytorch Error: when use the nn.embedding to embedding the word(in rnn like LSTM), the id tensor type is LongTensor **</p>
</li>
<li><p>**pytorch tips: when you utilize the rnn.pack_padded_sequence() to pack the input sentence, the sen_len parameter should be a LongTensor type, instead of a Variable **<br>for example: rnn.pack_padded_sequence(word_embedding, list(torch.squeeze(sentent_len_tensor)), batch_first=True), where the sentence_len_tensor is a LongTensor type rather a Variable, if its a Variable, some error will occur,</p>
</li>
<li><p>** about the output for LSTM **:<br>assume the <em>packed</em> is a output by rnn.pack_padded_sequence(), forward the LSTM</p>
<figure class="highlight bash"><table><tr><td class="code"><pre><span class="line">output_packed = model.lstm(packed)</span><br><span class="line">```  </span><br><span class="line">the output_packed is a tuple, the first element is an packedSequence, the second element contains two components: they are *hn* output and *cn* respectively</span><br><span class="line"></span><br><span class="line"><span class="keyword">then</span> unpacked the output by rnn.pad_packed_squeeze()</span><br><span class="line">``` bash</span><br><span class="line">output_unpacked = rnn.pad_packed_squeeze(output_packed[0])</span><br></pre></td></tr></table></figure>
<p>the output_unpacked is also a tuple, the first element <em>output_unpacked[0]</em> is the requried output Variable with size batch_size x sentence_len x lstm_hidden_size, the second element is a list containing the word number of each sentence.</p>
</li>
<li><p><strong>pytorch报错</strong></p>
<figure class="highlight plaintext"><table><tr><td class="code"><pre><span class="line">Cublas runtime error : library not initialized</span><br></pre></td></tr></table></figure>
<p>一般是显存不够</p>
</li>
<li><p>** pytorch错误**</p>
<figure class="highlight bash"><table><tr><td class="code"><pre><span class="line">device-side assert triggered</span><br></pre></td></tr></table></figure>
<p>查询网络中用到的索引是否越界（word embedding的index，以及分类类别号的index）</p>
</li>
<li><p>** windows 中错误 BrokenPipeError: [Errno 32] Broken pipe **<br>这是windows多线程的问题，在dataloader中将num_work参数设置为0即可解决</p>
</li>
<li><p>** pytorch中多标记分类的损失函数可以使用 **</p>
<figure class="highlight bash"><table><tr><td class="code"><pre><span class="line">Loss = torch.nn.BCEWithLogitsLoss()</span><br><span class="line"><span class="comment">#或者</span></span><br><span class="line">loss = torch.nn.BCELoss()</span><br></pre></td></tr></table></figure>
<p>注意网络输出的最后一层不要使用softmax，因为这不是单标签分类，使用sigmoid结合BCELoss或者直接输出结合BCEWithLogitsLoss进行训练</p>
</li>
<li><p>** pytorc将gpu变量移动到cpu中并且存起来，超出显存**<br>场景如下：一系列书序需要通过网络，网络和输入数据在GPU上计算，在循环中想要将模型的输出移动道内存中，并且存储起来，直接运行gpu会累加并最后超出： </p>
<figure class="highlight bash"><table><tr><td class="code"><pre><span class="line"><span class="comment">#错误做法：</span></span><br><span class="line">out = model(input)</span><br><span class="line">out = out.cpu()</span><br><span class="line">save_list.append(out)</span><br><span class="line"><span class="comment">#正确做法</span></span><br><span class="line">out = model(input)</span><br><span class="line">out = out.cpu().detach().numpy()/out.data.cpu().numpy()</span><br><span class="line">save_list.append(out)</span><br></pre></td></tr></table></figure>
<p>需要使用detach函数先将变量从图中剪下来，之后移动到cpu上进行存储。</p>
</li>
<li><p>**pytorch网络两次输出不一样 **<br>在pytorch中，如果没有申明model.eval(), 即使是相同模型参数，相同输出，两次的输出结果也不一样，请注意。</p>
</li>
<li><p><strong>python 多线程</strong><br>一个简单例子</p>
<figure class="highlight bash"><table><tr><td class="code"><pre><span class="line">import multiprocess</span><br><span class="line"></span><br><span class="line">P1 = multiprocess.Process(target=Function1, arg s=(input,))</span><br><span class="line">P2 = multiprocess.Process(target = Function2, args=(input2,))</span><br><span class="line"></span><br><span class="line">p1.start()</span><br><span class="line">p2.start()</span><br><span class="line"></span><br><span class="line">p1.join()</span><br><span class="line">p2.join()</span><br></pre></td></tr></table></figure></li>
<li><p>**使用多线程进行图片下载 **</p>
<figure class="highlight bash"><table><tr><td class="code"><pre><span class="line">import json</span><br><span class="line">import urllib<span class="comment">#python 3使用urllib.request</span></span><br><span class="line">import os</span><br><span class="line">import sys</span><br><span class="line">import multiprocessing as multiprocess</span><br><span class="line">from threading import Thread</span><br><span class="line">import threading</span><br><span class="line"></span><br><span class="line">image_save_pth = <span class="string">&#x27;./images/&#x27;</span></span><br><span class="line"><span class="keyword">if</span> not os.path.exists(image_save_pth):</span><br><span class="line">    os.mkdir(image_save_pth)</span><br><span class="line">def download_image(data_lines, start_index, end_index, split):</span><br><span class="line">    <span class="comment">#if split.startswith(&#x27;test&#x27;):</span></span><br><span class="line">    <span class="comment">#    split = split+&#x27;all&#x27;</span></span><br><span class="line">     </span><br><span class="line">    all_data_lines = data_lines[start_index:end_index]</span><br><span class="line">    count = 0.0</span><br><span class="line">    thread_name = threading.current_thread().name</span><br><span class="line">    <span class="built_in">print</span>(thread_name + <span class="string">&#x27; is downloading images...&#x27;</span>)</span><br><span class="line">    length = len(all_data_lines)</span><br><span class="line">    <span class="keyword">for</span> i <span class="keyword">in</span> range(0, length):</span><br><span class="line">        line = all_data_lines[i]</span><br><span class="line">        tmp_dict = json.loads(line)</span><br><span class="line">        url = tmp_dict[<span class="string">&#x27;url&#x27;</span>]</span><br><span class="line">        img_id = tmp_dict[<span class="string">&#x27;img_id&#x27;</span>]</span><br><span class="line">        <span class="keyword">if</span> os.path.exists(image_save_pth + str(img_id) + <span class="string">&#x27;.jpg&#x27;</span>):</span><br><span class="line">            <span class="built_in">continue</span></span><br><span class="line"></span><br><span class="line">        flag = True</span><br><span class="line">        <span class="keyword">while</span> flag:</span><br><span class="line">            try:</span><br><span class="line">		<span class="comment">#python 3使用urllib.request.urlretrieve(~,~)</span></span><br><span class="line">                out = urllib.urlretrieve(url, image_save_pth + str(img_id) + <span class="string">&#x27;.jpg&#x27;</span>)</span><br><span class="line">                flag = False</span><br><span class="line">            except:</span><br><span class="line">                flag = True</span><br><span class="line">        <span class="comment">#sys.stdout.write(&#x27;[%d/%d]-th in [%s], [%f] percent is finished!\r&#x27;%(i+1, length, split, (i*1.0 + 1) / length *100))</span></span><br><span class="line">        <span class="comment">#sys.stdout.flush()</span></span><br><span class="line">    </span><br><span class="line">def main():</span><br><span class="line">    <span class="comment">#data_split = [&#x27;train&#x27;, &#x27;dev&#x27;, &#x27;test&#x27;]</span></span><br><span class="line">    <span class="comment">#for item in data_split:</span></span><br><span class="line">    <span class="comment">#download_image(item)</span></span><br><span class="line">    <span class="comment">#p = multiprocess.Process(target = download_image, args = (&#x27;train&#x27;,))</span></span><br><span class="line">    <span class="comment">#p.start()</span></span><br><span class="line">    <span class="comment">#p.join()</span></span><br><span class="line">    split = <span class="string">&#x27;train&#x27;</span></span><br><span class="line">    path = <span class="string">&#x27;/Users/wang/SCAN/data/coco_precomp/&#x27;</span> + split + <span class="string">&#x27;_data_coco.json&#x27;</span></span><br><span class="line">    data_lines = open(path).readlines()</span><br><span class="line"></span><br><span class="line">    num_thread = 100</span><br><span class="line">    all_length = len(data_lines)</span><br><span class="line">    step = all_length // num_thread</span><br><span class="line">    start_index = 0</span><br><span class="line">    end_index = step</span><br><span class="line">    t = []</span><br><span class="line">    count = 1</span><br><span class="line">    <span class="keyword">while</span> end_index != all_length:</span><br><span class="line">        thread = Thread(target = download_image, args = (data_lines,start_index, end_index, split), name = <span class="string">&#x27;Thread &#x27;</span> + str(count))</span><br><span class="line">        t.append(thread)</span><br><span class="line">        start_index=end_index</span><br><span class="line">        end_index = min(start_index + step, all_length)</span><br><span class="line">        count += 1</span><br><span class="line"></span><br><span class="line">    <span class="keyword">for</span> th <span class="keyword">in</span> t:</span><br><span class="line">        th.start()</span><br><span class="line">    <span class="keyword">for</span> th <span class="keyword">in</span> t:</span><br><span class="line">        th.join()</span><br><span class="line"></span><br><span class="line"></span><br><span class="line"><span class="keyword">if</span> __name__ == <span class="string">&#x27;__main__&#x27;</span>:</span><br><span class="line">    main()</span><br></pre></td></tr></table></figure></li>
</ul>
<p> 关于更多python多线程的使用参照：<a href="https://blog.csdn.net/u012969412/article/details/82768882">https://blog.csdn.net/u012969412/article/details/82768882</a><br> 示例：<a href="https://blog.csdn.net/SeeTheWorld518/article/details/49639651">https://blog.csdn.net/SeeTheWorld518/article/details/49639651</a><br> 基础用法：<a href="https://thief.one/2016/11/23/Python-multiprocessing/">https://thief.one/2016/11/23/Python-multiprocessing/</a><br> mutliprocess multiprecess.dummy和Thread的区别参照：<a href="https://www.zhihu.com/question/44738028">https://www.zhihu.com/question/44738028</a></p>
<ul>
<li><p>**python并行简单例子 **</p>
<figure class="highlight bash"><table><tr><td class="code"><pre><span class="line">from joblib import Parallel, delayed</span><br><span class="line">import math</span><br><span class="line"></span><br><span class="line">def my_fun(i):</span><br><span class="line"></span><br><span class="line">    <span class="built_in">return</span> math.sqrt(ii*2)</span><br><span class="line"></span><br><span class="line"><span class="comment">#Parallel和delayed配合使用进行多线程</span></span><br><span class="line"><span class="comment">#n_jobs设置多少个并行线程</span></span><br><span class="line">Parallel(n_jobs=2)(delayed(my_fun)(i) <span class="keyword">for</span> i <span class="keyword">in</span> range(num))</span><br></pre></td></tr></table></figure></li>
<li><p><strong>numpy从字符串中读取array</strong></p>
<figure class="highlight bash"><table><tr><td class="code"><pre><span class="line">import numpy as np</span><br><span class="line">string = <span class="string">&#x27;1 2 3 6&#x27;</span></span><br><span class="line">arr = np.fromstring(string, dtype = int, sep = <span class="string">&#x27; &#x27;</span>)</span><br><span class="line"></span><br></pre></td></tr></table></figure></li>
<li><p>**pytorch runtimeError: DataLoader worker (pid13)is killed by signal : buss Error **<br>解决方法：在DataLoader去除num_workers</p>
<figure class="highlight bash"><table><tr><td class="code"><pre><span class="line">dataloader_train = data.DataLoader(train_dataset, batch_size=64, shuffle=True, num_workers=2)</span><br><span class="line">改为：</span><br><span class="line">dataloader_train = data.DataLoader(train_dataset, batch_size=64, shuffle=True)</span><br><span class="line"></span><br></pre></td></tr></table></figure></li>
<li><p>**pytorch错误：cannot assign module before Module.<strong>init</strong>() call **<br>定义的networ中的init没有使用super(network, self).<strong>init</strong>()</p>
</li>
<li><p><strong>pytorch错误：CUDNN_STATUS_BAD_PARAM</strong><br>输入网络的图片的size不对<br>一些奇怪的错误可以参考这个网址：<a href="https://blog.csdn.net/VictoriaW/article/details/72636375">https://blog.csdn.net/VictoriaW/article/details/72636375</a></p>
</li>
<li><p>**tensorboard的使用 **<br>如果是在pytorch中使用tensorboardX，使用tensorboard命令还需要安装tensorflow，如果是tensorlow中使用tensorboard则不需要（浏览器中输入：localhost:<br>6006）</p>
</li>
<li><p>**python Error：bad file descriptor **<br>Nohup运行程序的时候代码中有 pdb.set_trace()没有注释掉</p>
</li>
<li><p>**docker中pytorch错误：Unexpected bus error encountered in worker. This might be caused by insufficient shared memory (shm) **<br>docker中运行docker把batch size设置过大，shared memory不够<br>解决办法：将Dataloader的num——workers设置为0</p>
</li>
<li><p>**使用pytorch多GPU注意的问题 **<br>1，第一种方式</p>
<figure class="highlight bash"><table><tr><td class="code"><pre><span class="line"><span class="comment">#使用多gpu训练</span></span><br><span class="line">net = my_model()</span><br><span class="line">net = torch.nn.DataParallel(net, device_ids=[0,1,2,3])</span><br><span class="line">net = net.cuda()</span><br><span class="line"></span><br><span class="line"><span class="comment">#模型的保存与load--方法1:保存的时候做处理</span></span><br><span class="line">由于被DataParallel是一个新的对象，所以保存的时候需要，直接提取出state_dict保存</span><br><span class="line">torch.save(net.module.state_dict(), path)<span class="comment">#load的时候直接按照单gpu的来训练</span></span><br><span class="line"></span><br><span class="line"><span class="comment">#方法2:load的时候做处理</span></span><br><span class="line">1), 利用torch.nn.DataParallel包起来进行训练的模型在，载入的时候也要使用DataParallel封装，因为经过封装之后的参数字典中的key多出了一个module前缀：</span><br><span class="line">``` bash</span><br><span class="line">net = my_model()</span><br><span class="line">net = torch.nn.DataParallel(net)</span><br><span class="line">net.load_state_dict(torch.load(path))</span><br></pre></td></tr></table></figure></li>
</ul>
<p>2)，如果模型被DataParallel封装，访问模型的某一个模块必须加上module，例如myNet.module.fc</p>
<figure class="highlight plaintext"><table><tr><td class="code"><pre><span class="line">2，第二种方式 torch.nn.parallel.DistributedDataPrallel</span><br><span class="line">推荐使用,使用步骤为：</span><br><span class="line">``` bash</span><br><span class="line">#1， 加入命令行参数并初始化</span><br><span class="line">parser = argparser.ArgumentParser()</span><br><span class="line">Parser.add_argument(&#x27;--local_rank&#x27;m type=int, default=0)</span><br><span class="line">opt = parser.parser_args()</span><br><span class="line"></span><br><span class="line">torch.distributed.init_process_group(backend=&#x27;nccl&#x27;, world_size=1, rank=0)</span><br><span class="line"></span><br><span class="line">#2, 使用DistributionSampler进行数据分享</span><br><span class="line">dataset = DataSet(...) #your datasets</span><br><span class="line">Sampler = torch.utils.data.distributed.DistributedSampler(dataset)</span><br><span class="line">Data loader = DataLoader(dataset, batch, shuffle=False, num_workers=num, pin_memoty=True, drop_last=False, sampler=Sampler) #其中pin_memory=True是必要的</span><br><span class="line"></span><br><span class="line">#3接下来部署模型进行分布式训练</span><br><span class="line">model=torch.nn.parallel.DistributedDataParallel(model)</span><br><span class="line"></span><br><span class="line">#最后，命令行中需要加torch.distributed.launch</span><br><span class="line">CUDA_VISIBLE_DEVICES=0,1 python -m torch.distributed.launch --nproc_per_node=2 train.py</span><br><span class="line"></span><br><span class="line">note: a_tensor.type(Tensor)会引起inplace error错误;</span><br><span class="line">Dataloader的参数shuffle 要设为False</span><br></pre></td></tr></table></figure>


<ul>
<li><p><strong>使用多gpu训练网络 segmentation Error（段错误）</strong><br>主要是因为docker中的共享内存不够</p>
</li>
<li><p><strong>torch.load模型错误：UnicodeDecodeError: ‘ascii’ codec can’t decode byte 0xe9 in position 6</strong><br>原因：如果是在python 2中的保存的模型，python3环境中load就会出现上述的错误<br>解决方法：</p>
<figure class="highlight bash"><table><tr><td class="code"><pre><span class="line">from functools import partial</span><br><span class="line">import pickle</span><br><span class="line">pickle.load = partial(pickle.load, encoding=<span class="string">&quot;latin1&quot;</span>)</span><br><span class="line">pickle.Unpickler = partial(pickle.Unpickler, encoding=<span class="string">&quot;latin1&quot;</span>)</span><br></pre></td></tr></table></figure></li>
<li><p><strong>ImageNet上的均值方差</strong></p>
<figure class="highlight bash"><table><tr><td class="code"><pre><span class="line">normalize = transforms.Normalize(mean=[0.485, 0.456, 0.406],</span><br><span class="line">                                  std=[0.229, 0.224, 0.225])</span><br></pre></td></tr></table></figure></li>
<li><p>**pytorch输出损失或者梯度为nan **<br>检查：1， loss计算<br>2，检查梯度计算,例如sqrt(0), log(0)等均会导致nan<br>可以使用torch.autograd.detect_anormaly</p>
<figure class="highlight bash"><table><tr><td class="code"><pre><span class="line">with torch.autograd.detect_anomaly():                                              │</span><br><span class="line">	out = model(input)</span><br><span class="line">	optizer.zero_grad()</span><br><span class="line">	loss. =Loss(out,target)</span><br><span class="line">        Loss.backward()</span><br><span class="line">        optimizer.step()</span><br></pre></td></tr></table></figure>
<p>该模块自动检测grad计算异常的地方</p>
</li>
<li><p>**import cv2错误:mportError: libSM.so.6: cannot open shared object file: No such file or directory” **<br>解决方法：</p>
<figure class="highlight bash"><table><tr><td class="code"><pre><span class="line">Apt-get install update</span><br><span class="line">apt-get install -y libsm6 libxext6 libxrender-dev</span><br><span class="line">Pip install opencv-python</span><br></pre></td></tr></table></figure>
<p>如果没有sudo权限，可以暂时安装无头依赖版本</p>
<figure class="highlight bash"><table><tr><td class="code"><pre><span class="line">Pip install opencv-python-headless</span><br><span class="line"><span class="comment">#if you need, also install the contrib</span></span><br><span class="line">Pip install opencv-contrib-python-headless</span><br></pre></td></tr></table></figure></li>
<li><p><strong>c++版本opencv &amp; boost库安装</strong></p>
<figure class="highlight bash"><table><tr><td class="code"><pre><span class="line"><span class="comment">#step 1</span></span><br><span class="line">1. sudo apt-get install cmake</span><br><span class="line">2. sudo apt-get install python-devel numpy</span><br><span class="line">3. sudo apt-get install gcc gcc-c++</span><br><span class="line"></span><br><span class="line"><span class="comment">#Step 2</span></span><br><span class="line">1.sudo apt-get install gtk2-devel</span><br><span class="line">2. sudo apt-get install libv4l-devel</span><br><span class="line">3. sudo apt-get install ffmpeg-devel</span><br><span class="line">4. sudo apt-get install gstreamer-plugins-base-devel</span><br><span class="line"></span><br><span class="line"><span class="comment">#step 3</span></span><br><span class="line">git <span class="built_in">clone</span> https://github.com/opencv/opencv.git</span><br><span class="line"></span><br><span class="line"><span class="comment">#step 4</span></span><br><span class="line">1. <span class="built_in">cd</span> ~/opencv</span><br><span class="line">2. mkdir build</span><br><span class="line">3. <span class="built_in">cd</span> build</span><br><span class="line"></span><br><span class="line"><span class="comment">#step 5</span></span><br><span class="line">1. cmake ../</span><br><span class="line">2. make</span><br><span class="line">3. sudo make install</span><br><span class="line"></span><br><span class="line"><span class="comment">#======安装boost==========</span></span><br><span class="line">sudo apt-get install libboost-all-dev</span><br><span class="line"></span><br></pre></td></tr></table></figure></li>
<li><p>**pytorch使用fp16节省显存 **</p>
</li>
<li><p>方法1: 利用apex<br>主要三部分需要改动：初始化模型，反传损失，梯度裁剪，使用方法：</p>
<figure class="highlight bash"><table><tr><td class="code"><pre><span class="line">from apex import amp</span><br><span class="line"><span class="comment">#declare the model</span></span><br><span class="line">model = MyModel()<span class="comment">#nn.Module子类</span></span><br><span class="line"><span class="comment">#declare the optimiser</span></span><br><span class="line">optimizer = torch.optim.Adam(model.paramerters(), lr= 0.0001)</span><br><span class="line"><span class="comment">#initlize the model using the amp</span></span><br><span class="line">model, optimiser = amp.initialize(model, optimiser, opt_level=<span class="string">&#x27;O1&#x27;</span>,</span><br><span class="line">keep_batchnorm_fp32=True, loss_scale=<span class="string">&#x27;dynamic&#x27;</span>)<span class="comment">#优化级别一般使用‘O1’</span></span><br><span class="line"><span class="comment">#反传loss的时候</span></span><br><span class="line">loss = loss_function(model_out, ground_truth)</span><br><span class="line">with amp.scale_loss(loss, optimizer) as scaled_loss:</span><br><span class="line">    scaled_loss.backward()</span><br><span class="line"><span class="comment">#如果模型中有梯度裁剪的部分需要将普通的</span></span><br><span class="line">clip_grad_norm(model.paramters(), clip_grad)</span><br><span class="line"><span class="comment">#改为：</span></span><br><span class="line">clip_grad_norm(amp.master_params(optimizer), clip_grad)</span><br></pre></td></tr></table></figure>
<p>如果模型不是nn.Module,例如model为object，包含两个子类img_enc， txt_enc为nn.Module，那么模型初始化改为</p>
<figure class="highlight bash"><table><tr><td class="code"><pre><span class="line">model = MyModel()</span><br><span class="line">optimizer = torch.optim.Adam(model.paramerters(), lr= 0.0001)</span><br><span class="line"><span class="comment">#initialise the model using the amp</span></span><br><span class="line">model.img_enc, model.txt_enc, optimiser = amp.initialize([model.img_enc, model_txt], </span><br><span class="line">optimiser, opt_level=<span class="string">&#x27;O1&#x27;</span>, keep_batchnorm_fp32=True, loss_scale=<span class="string">&#x27;dynamic&#x27;</span>)</span><br></pre></td></tr></table></figure></li>
<li><p>方法2: 利用pytorch(&gt;1.6)的封装</p>
<figure class="highlight plaintext"><table><tr><td class="code"><pre><span class="line">import torch</span><br><span class="line"></span><br><span class="line">#训练开始之前</span><br><span class="line">Scaler = torch.cuda.amp.GradScaler()</span><br><span class="line"></span><br><span class="line">#训练时</span><br><span class="line">for epoch in range(total_epoch):</span><br><span class="line">    for data in enumerate(train_loader):</span><br><span class="line">        </span><br><span class="line">	optimizer.zero_grad()        </span><br><span class="line">        #模型inference</span><br><span class="line">	with torch.cuda.amp.autocast():</span><br><span class="line">              y_pred = model(data)</span><br><span class="line">              loss = loss_fn(y_pred, gt)</span><br><span class="line"></span><br><span class="line">        scaler.scale(loss).backward()</span><br><span class="line"></span><br><span class="line">        scaler.step(optimizer)</span><br><span class="line">        scaler.update()</span><br><span class="line"></span><br></pre></td></tr></table></figure></li>
</ul>
<p>From apex import amp报错：TypeError: Class advice impossible in Python3<br>Uninstall the old apex</p>
<figure class="highlight bash"><table><tr><td class="code"><pre><span class="line">pip uninstall apex</span><br><span class="line">git <span class="built_in">clone</span> https://www.github.com/nvidia/apex</span><br><span class="line"><span class="built_in">cd</span> apex</span><br><span class="line">python setup.py install</span><br></pre></td></tr></table></figure>


<ul>
<li><p><strong>pytorch设置学习率衰减</strong></p>
<figure class="highlight bash"><table><tr><td class="code"><pre><span class="line">from torch.optim import lr_scheduler</span><br><span class="line"></span><br><span class="line">optimiser = torch.optim.Adam(net.parameters(), lr = 0.005)</span><br><span class="line">Scheduler = lr_scheduler.StepLR(optimizer, 10, 0.1)<span class="comment">#没10轮学习率乘以1</span></span><br><span class="line"></span><br><span class="line"><span class="keyword">for</span> n <span class="keyword">in</span> range(epochs):</span><br><span class="line">    scheduler.step()</span><br><span class="line">    .....</span><br></pre></td></tr></table></figure>
<p>也可以手动调整learning rate</p>
<figure class="highlight bash"><table><tr><td class="code"><pre><span class="line">def adjust_learning_rate(opt, optimizer, epoch):</span><br><span class="line">     <span class="string">&quot;&quot;</span><span class="string">&quot;Sets the learning rate to the initial LR</span></span><br><span class="line"><span class="string">        decayed by 10 every 30 epochs&quot;</span><span class="string">&quot;&quot;</span></span><br><span class="line">     lr = opt.learning_rate * (0.1 ** (epoch // opt.lr_update))</span><br><span class="line">     <span class="keyword">for</span> param_group <span class="keyword">in</span> optimizer.param_groups:</span><br><span class="line">         param_group[<span class="string">&#x27;lr&#x27;</span>] = lr</span><br></pre></td></tr></table></figure></li>
<li><p>**tensorflow load checkpoint error: DataLoss Error **<br>解决方法：<br>如果你的checkpoint文件名称是model.ckpt.data-00000-data-00001,那么sess.restore中掺入的参数只需要是<br>model.ckpt而不是整个的文件model.ckpt.data-00000-of-00001，如果传入整个文件名称就会报以下错误：<br>Data loss: not an sstable (bad magic number): perhaps your file is in a different file format and you need to use a different restore operator?，</p>
</li>
<li><p>**pytorch设置随机种子 **</p>
<figure class="highlight bash"><table><tr><td class="code"><pre><span class="line">random.seed(seed)</span><br><span class="line">np.random.seed(seed)</span><br><span class="line">torch.manual_seed(seed)</span><br><span class="line">torch.cuda.manual_seed(seed)</span><br><span class="line">torch.backends.cudnn.deterministic=True <span class="comment">#使用确定性卷积,会极大的降低速度</span></span><br><span class="line">torch.backends.cudnn.benchmark =True <span class="comment">#自动搜索最快的操作，用于提速</span></span><br></pre></td></tr></table></figure>
<p>tensorflow随机种子设置：tf.set_random_seed(seed)</p>
</li>
<li><p><strong>tensoflow一次奇怪的bug记录：libdevice.compute_30.10.bd not found</strong><br>当使用instanceNorm或者囤cated_normal_initializer()初始化的时候出现了报错。<br>解决方案：<br>找到该文件，并将它复制到你的程序运行目录下，但是只可以解决instancNorm的问题，无法解决truncated_normal_initializer的问题，<br>该文件啃可能的的位置：1 conda环境lib目录下例如： /home/yaxiong/anaconda3/envs/tensorflow/lib<br>2 cuda目录下：/usr/local/cuda8/</p>
</li>
<li><p><strong>tensorflow将tfrecords load进入内存中</strong></p>
<figure class="highlight bash"><table><tr><td class="code"><pre><span class="line">dataset = tf.data.TFRecordDataset(files=[your_data_path])</span><br><span class="line">dataset = dataset.cache()</span><br></pre></td></tr></table></figure></li>
<li><p><strong>tensorflow error： Loaded runtime CuDNN library: 7.1.4 but source was compiled with: 7.2.1.</strong><br>如果你没有操作过cudnn相关的目录和路径，该错误很有可能是tensorflow版本的问题，例如1.12.0和1.14.0，尝试将版本降级看看</p>
</li>
<li><p>**pytorch下载预训练参数输入：URL error **<br>可能是因为python版本原因，python2.7有这个错误，解决办法：</p>
<figure class="highlight bash"><table><tr><td class="code"><pre><span class="line">Import torchvision.models as models</span><br><span class="line">From torch vision.models.resnet import model_urls</span><br><span class="line"></span><br><span class="line">model_urls[<span class="string">&#x27;resnet152&#x27;</span>] = model_urls[<span class="string">&#x27;resnet152&#x27;</span>].replace(<span class="string">&#x27;https://&#x27;</span>, <span class="string">&#x27;http://&#x27;</span>)</span><br><span class="line">Resnet152 = models.renset152(pertrained=True)</span><br></pre></td></tr></table></figure></li>
<li><p><strong>python中找到字符串中子串的位置</strong><br>Python中没有直接实现这个的函数，所以可以按照如下做法得到：</p>
<figure class="highlight bash"><table><tr><td class="code"><pre><span class="line">Import re</span><br><span class="line"><span class="comment">#返回在super_string中sub_string 的下标</span></span><br><span class="line">Index = [m.start() <span class="keyword">for</span> m inre.finditer(sub_string, super_string)]</span><br></pre></td></tr></table></figure></li>
<li><p>**python得到路径下所有的满足条件的文件 **<br>例如文件夹A下存放了10个文件夹，每个文件夹中有一些图片，一次性可以读到所有的文件：</p>
<figure class="highlight bash"><table><tr><td class="code"><pre><span class="line">Import glob</span><br><span class="line">all_images = glob.glob(r<span class="string">&#x27;.A/*/*.jpg&#x27;</span>)<span class="comment">#得到所有文件的路径</span></span><br></pre></td></tr></table></figure></li>
<li><p><strong>python中的map函数，filter函数</strong></p>
</li>
<li><p>1， map函数：map(function, iterable) *<br>map函数会更具参数序列中的每一个元素调用function函数,例子：</p>
<figure class="highlight bash"><table><tr><td class="code"><pre><span class="line">Out = map(lambda x:x*2, [1,3,5,7])<span class="comment">#每个元素翻倍</span></span><br><span class="line">Out = map(lambda x, y: x+y, [1,3,5], [2,4,6])<span class="comment">#对应元素相加</span></span><br></pre></td></tr></table></figure></li>
<li><p>2，filter函数:filter(function, iterable) *<br>Filter函数用于过滤序列，过滤掉不符合条件的元素，其中function的返回值必须是true或者false</p>
<figure class="highlight bash"><table><tr><td class="code"><pre><span class="line">Out = filter(lambda x:x.startswith(<span class="string">&#x27;wang&#x27;</span>), [<span class="string">&#x27;wang&#x27;</span>, <span class="string">&#x27;wangyx&#x27;</span>, <span class="string">&#x27;yaxiong&#x27;</span>])</span><br></pre></td></tr></table></figure></li>
<li><p><strong>python使用seaborn画热力图</strong></p>
<figure class="highlight bash"><table><tr><td class="code"><pre><span class="line">Import matplotlib.pyplot as plt</span><br><span class="line">Import senborn as sns</span><br><span class="line">Import numpy as np</span><br><span class="line"></span><br><span class="line">mat = np.random.rand(16,16)</span><br><span class="line">ax = sns.heatmap(mat, camp =<span class="string">&#x27;rainbow&#x27;</span>)<span class="comment">#cmap设置色系，可以参考https://blog.csdn.net/weixin_39777626/article/details/95892284</span></span><br><span class="line"><span class="comment">#保存热力图</span></span><br><span class="line">ax.figure.savefig(<span class="string">&#x27;hmx.png&#x27;</span>, transparent=True, bbox_inches=<span class="string">&#x27;tight&#x27;</span>)</span><br><span class="line"></span><br><span class="line"><span class="comment">#只保存图，去除边框</span></span><br><span class="line"></span><br><span class="line">plt.imshow()</span><br><span class="line">plt.axis(<span class="string">&#x27;off&#x27;</span>)<span class="comment">#去掉坐标系</span></span><br><span class="line">plt.savefig(<span class="string">&#x27;test.png&#x27;</span>, bbox_inches=<span class="string">&#x27;tight&#x27;</span>, pad_inches=0.0)</span><br></pre></td></tr></table></figure></li>
<li><p><strong>python中matplotlib中的画图常用语法</strong></p>
<figure class="highlight bash"><table><tr><td class="code"><pre><span class="line">Import matplotlib.pyplot as pat</span><br><span class="line">styles = pet.style.available <span class="comment">#所有的画图风格，其中第22， 24比较好看，是一个列表，</span></span><br><span class="line">plt.style.use(styles[args.style])<span class="comment">#应用风格，对所有的绘图</span></span><br><span class="line">fig = plt.figure()</span><br><span class="line">ax1 = plt.subplot(2,1,1)</span><br><span class="line"><span class="comment">#设置不显示上方和右边的边界</span></span><br><span class="line"><span class="comment">#ax1.spines[&#x27;right&#x27;].set_visible(False)</span></span><br><span class="line"><span class="comment">#ax1.spines[&#x27;left&#x27;].set_visible(False)</span></span><br><span class="line">ax1.set_title(<span class="string">&#x27;Discriminator Loss&#x27;</span>)<span class="comment">#设置子图的标题</span></span><br><span class="line">posi_d, = plt.plot(np.arange(len(loss_d)), np.array(posi_loss_d), label=<span class="string">&#x27;Dis-NRSC&#x27;</span>, color=<span class="string">&#x27;r    &#x27;</span>)</span><br><span class="line">our_d, = plt.plot(np.arange(len(loss_d)), np.array(loss_d), label=<span class="string">&#x27;Dis-RSC&#x27;</span>,color = <span class="string">&#x27;g&#x27;</span>)</span><br><span class="line">plt.legend(handles =[posi_d, our_d],loc=<span class="string">&#x27;best&#x27;</span>,facecolor=<span class="string">&#x27;blue&#x27;</span>)<span class="comment">#加图例</span></span><br><span class="line">plt.grid()</span><br><span class="line"><span class="comment">#plt.xlabel(&#x27;epoch&#x27;)</span></span><br><span class="line"><span class="comment">#plt.ylabel(&#x27;loss&#x27;)</span></span><br><span class="line">ax1.set_xlabel(<span class="string">&#x27;epochs&#x27;</span>,fontsize=12)<span class="comment">#设置坐标轴的名称，字体大小</span></span><br><span class="line">ax1.set_ylable(<span class="string">&#x27;Gen loss&#x27;</span>,fontsize=12)<span class="comment">#设置坐标轴的名称，字体大小</span></span><br><span class="line">plt.savefig(<span class="string">&#x27;test.png&#x27;</span>)<span class="comment">#保存图片</span></span><br><span class="line"></span><br><span class="line"><span class="comment">#绘制第二个子图，类似</span></span><br><span class="line">ax2=plt.subplot(2,1,2)</span><br><span class="line">ax2.set_title(<span class="string">&#x27;Generator Loss&#x27;</span>)</span><br><span class="line">posi_g, = plt.plot(np.arange(len(loss_d)), np.array(posi_loss_g),label = <span class="string">&#x27;Gen-NRSC&#x27;</span>, color=<span class="string">&#x27;    r&#x27;</span>)</span><br><span class="line">our_g, = plt.plot(np.arange(len(loss_d)), np.array(loss_g), label = <span class="string">&#x27;GEN_RSC&#x27;</span>, color = <span class="string">&#x27;g&#x27;</span>)</span><br><span class="line">plt.legend(handles =[posi_g, our_g],loc=<span class="string">&#x27;best&#x27;</span>,facecolor=<span class="string">&#x27;blue&#x27;</span>)</span><br><span class="line">plt.grid()</span><br><span class="line">ax2.set_xlabel(<span class="string">&#x27;epochs&#x27;</span>, fontsize=12)</span><br><span class="line">ax2.set_ylabel(<span class="string">&#x27;Gen loss&#x27;</span>, fontsize=12)</span><br></pre></td></tr></table></figure>
<p>Pyplot绘图剔除白边</p>
<figure class="highlight bash"><table><tr><td class="code"><pre><span class="line"><span class="comment">#bbox_inches=&#x27;tight&#x27;去除坐标轴所占用的空间</span></span><br><span class="line"><span class="comment">#dpi用来控制分辨率，600是很高的分辨率</span></span><br><span class="line"><span class="comment">#pad_inches=0.0 重点，去除占用的白边</span></span><br><span class="line">plt.savefig(<span class="string">&#x27;test.png&#x27;</span>, bbox_inches=<span class="string">&#x27;tight&#x27;</span>,dpi=600,pad_inches=0.0)</span><br><span class="line"></span><br></pre></td></tr></table></figure></li>
<li><p>**python图片显示几秒后再关闭 **</p>
<figure class="highlight plaintext"><table><tr><td class="code"><pre><span class="line"></span><br><span class="line">for im in all_images:</span><br><span class="line">    Img = Image.open(im)</span><br><span class="line">    plt.figure()</span><br><span class="line">    plt.ion()</span><br><span class="line">    plt.axis(&#x27;off&#x27;)</span><br><span class="line">    plt.imhsow(img)</span><br><span class="line">    plt.pause(2)#停顿两秒</span><br><span class="line">    plt.ioff()</span><br><span class="line"></span><br><span class="line">plt.clf()</span><br><span class="line">plt.close()</span><br><span class="line"></span><br></pre></td></tr></table></figure></li>
<li><p><strong>python中使用pysnooper进行调试</strong><br>先说用pip install pysnooper<br>使用方式</p>
<figure class="highlight bash"><table><tr><td class="code"><pre><span class="line"><span class="comment">#作为函数的装饰器进行使用</span></span><br><span class="line">Import pysnooper</span><br><span class="line">@pysnooper.snoop()</span><br><span class="line">def func():</span><br><span class="line">   ...</span><br><span class="line"></span><br><span class="line">主要参数：</span><br><span class="line">输出到调试信息到文件中：pysnooper.snoop(<span class="string">&#x27;debug.log&#x27;</span>)</span><br><span class="line">Prefix参数给函数命名，让挑食信息更加清晰:pysnooper.snoop(prefix=<span class="string">&#x27;my_fun&#x27;</span>)</span><br><span class="line">watch/watch_explode:查看参数的值/展开所有的值：pysnooper.snoop(watch=<span class="string">&#x27;var1&#x27;</span>)，查看多个参数设置为watch=(<span class="string">&#x27;var1&#x27;</span>,<span class="string">&#x27;var2&#x27;</span>)</span><br><span class="line">Depth:设置出打印的代码行数，默认为1:pysnooper.snoop(depth=2)</span><br></pre></td></tr></table></figure></li>
<li><p>** 使用fire库进行命令行解析**<br>安装：pip install fire<br>使用实例</p>
<figure class="highlight bash"><table><tr><td class="code"><pre><span class="line">import fire</span><br><span class="line"></span><br><span class="line">def print_fun(path):</span><br><span class="line">    <span class="built_in">print</span>(path)</span><br><span class="line"></span><br><span class="line">def add_fun(a,b):</span><br><span class="line">    <span class="built_in">return</span> a+b</span><br><span class="line"></span><br><span class="line"><span class="comment">#默认将所有的函数都进行了打包</span></span><br><span class="line">fire.Fire()</span><br><span class="line"></span><br><span class="line"><span class="comment">#---------------------------------</span></span><br><span class="line">在命令行中运行时需要指定使用哪个函数：</span><br><span class="line">python test.py print_fun --path  <span class="string">&#x27;a path string&#x27;</span><span class="comment">#也可以不要参数--path</span></span><br><span class="line">或者 python test.py add_fun --a 2 --b 3 <span class="comment">#同样的也可以不要参数--a and --b 值按照顺序分配</span></span><br><span class="line"></span><br><span class="line"><span class="comment">#------------------------------</span></span><br><span class="line">也可以在py文件中指定打包哪个函数</span><br><span class="line">fire.Fire(print_fun)</span><br><span class="line"><span class="comment">#则在命令行中不需要传递函数参数</span></span><br><span class="line">python test.py --path <span class="string">&#x27;a path string&#x27;</span> </span><br></pre></td></tr></table></figure></li>
<li><p>**python setup.py install/make install错误：gcc: error: pycocotools/_mask.c: No such file **<br>缺少cython遇pycocotools包</p>
<figure class="highlight bash"><table><tr><td class="code"><pre><span class="line">pip install cython</span><br><span class="line">pip. Install pycocotools</span><br></pre></td></tr></table></figure></li>
<li><p>**python序列化模快json与pickle **</p>
<figure class="highlight bash"><table><tr><td class="code"><pre><span class="line"><span class="comment">#两种都是对象在前，文件在后</span></span><br><span class="line">json.dump(object, open(<span class="string">&#x27;files.json&#x27;</span>, <span class="string">&#x27;w&#x27;</span>))</span><br><span class="line">pickle.dump(object, open(<span class="string">&#x27;files.pkl&#x27;</span>, <span class="string">&#x27;wb&#x27;</span>))</span><br></pre></td></tr></table></figure></li>
<li><p><strong>git同步本地与github远端仓库</strong></p>
</li>
<li><p>本地与远程github链接配置请参见：<a href="https://www.cnblogs.com/sdcs/p/8270029.html">https://www.cnblogs.com/sdcs/p/8270029.html</a></p>
</li>
<li><p>同步的步骤</p>
<figure class="highlight bash"><table><tr><td class="code"><pre><span class="line">1, mkdir git_test <span class="comment">#创建文件夹，进入该文件夹</span></span><br><span class="line">2，git init <span class="comment">#文件夹初始化为可供管理的仓库，之后就可以创建文件</span></span><br><span class="line">3，touch test.txt <span class="comment">#创建一个文件</span></span><br><span class="line">4, git add test.txt/. <span class="comment">#将test.txt文件/所有文件添加到本地仓库</span></span><br><span class="line">5, git commit -m <span class="string">&#x27;this is a test file&#x27;</span> <span class="comment">#提交改动，并增加注释</span></span><br><span class="line">6, git remote add origin git@github.com:wangyxxjtu/JHR.git <span class="comment">#与github的仓库建立连接</span></span><br><span class="line">7, git push （-u） origin (+)master <span class="comment">#推送 -u 当连接的仓库JHR.Git为空时才使用， 否则不会使用，+表示强制推送 （git push -f -u origin master 另一种强制提交方式）</span></span><br></pre></td></tr></table></figure>
<p>特别注意，如果当前仓库不包含所有想要同步的远端仓库的文件，会报错：</p>
<figure class="highlight bash"><table><tr><td class="code"><pre><span class="line">! [rejected]        master -&gt; master (fetch first)</span><br><span class="line">hint: Updates were rejected because the remote contains work that you <span class="keyword">do</span></span><br><span class="line">hint: not have locally. This is usually caused by another repository pushing</span><br><span class="line">hint: to the same ref. You may want to first integrate the remote changes</span><br></pre></td></tr></table></figure></li>
<li><p>**将本地的一个文件夹下的代码作为一个项目上传到指定的仓库中 **</p>
</li>
</ul>
<figure class="highlight bash"><table><tr><td class="code"><pre><span class="line">1 在github上新建一个项目AINet</span><br><span class="line">2 在要操作的文件夹下输入如下的命令</span><br><span class="line">  a git init (初始化，令git管理改文件夹)</span><br><span class="line">  b git add .(将当前文件夹下的所有文件加入本地仓库，如果只是想加入某个文件，将.换成文件名称)</span><br><span class="line">  c git commit -m <span class="string">&#x27;AINet code-Official&#x27;</span></span><br><span class="line">  d git remote add origin https://github.com/wangyxxjtu/AINet.git (将本地仓库与远程仓库进行关联)</span><br><span class="line">  e git push -u origin master (上传项目到github)</span><br></pre></td></tr></table></figure>
<p>#解决方案是：想将两个仓库合并到本地，如果本地的文件github仓库没有，那么该文件会通过该命令删除，慎重使用<br>Git pull –rebase origin master</p>
<p>一般来说更新远端需要四步</p>
<figure class="highlight bash"><table><tr><td class="code"><pre><span class="line">1，建立远端连接，并改动文件，如果项目只从github上通过git <span class="built_in">clone</span> 命令下载下来的，第一步可以省略</span><br><span class="line">git remote add origin git@github.com:wangyxxjtu/JHR.git</span><br><span class="line"></span><br><span class="line">2, 添加改动的文件或者文件夹到仓库</span><br><span class="line">git add file_name(dir_name/)</span><br><span class="line"></span><br><span class="line">3, 提交改动</span><br><span class="line">git commit -m <span class="string">&#x27;提示信息&#x27;</span></span><br><span class="line"></span><br><span class="line">4, 执行提交的改动</span><br><span class="line">git push origin master</span><br></pre></td></tr></table></figure>

<ul>
<li><p><strong>git常用的需求以及命令</strong><br>移除文件</p>
<figure class="highlight bash"><table><tr><td class="code"><pre><span class="line">git rm file_name <span class="comment">#删除文件,文件夹加r</span></span><br><span class="line">git commit -m <span class="string">&#x27;...&#x27;</span> <span class="comment">#提交改动</span></span><br><span class="line">git push origin master <span class="comment">#执行改动</span></span><br></pre></td></tr></table></figure>
<p>查看状态信息</p>
<figure class="highlight bash"><table><tr><td class="code"><pre><span class="line">git status</span><br></pre></td></tr></table></figure>
<p>代码回退某个代码提交历史中，需要满足：在当前工作仓库并且没有提交到Github,</p>
<figure class="highlight bash"><table><tr><td class="code"><pre><span class="line">git reset --hard HEAD^ <span class="comment">#回退到上个版本</span></span><br><span class="line">git reset --hard HEAD^^ <span class="comment">#回退到上上个版本</span></span><br><span class="line">git reset --hard HEAD~10 <span class="comment">#回退到之前10个版本</span></span><br><span class="line">git reset --hard commit-id <span class="comment">#回到提交ID的版本</span></span><br><span class="line">git <span class="built_in">log</span> --graph / git <span class="built_in">log</span> --<span class="built_in">stat</span> <span class="comment">#查看提交历史和commit-id</span></span><br></pre></td></tr></table></figure>
<p>这里的回退，指的是更具git log –graph第一commit-id进行的回退，并不一定会回退到之前的改动版本，移动前务必调研清楚<br>特别注意，这里一reset，在这个日期之后的commit-ID使用git log –graph命令就看不到了，使用：</p>
<figure class="highlight bash"><table><tr><td class="code"><pre><span class="line">git reflog <span class="comment">#可以看到命令的log，从中可以查看进入的commit-id历史</span></span><br></pre></td></tr></table></figure>
<p>查看当前工作目录的文件和远程github上的不同</p>
<figure class="highlight bash"><table><tr><td class="code"><pre><span class="line">git diff HEAD -- main.py</span><br></pre></td></tr></table></figure>
<p>放弃这此所做的修改，回到最近一次commit的结果</p>
<figure class="highlight bash"><table><tr><td class="code"><pre><span class="line">git checkout main.py</span><br></pre></td></tr></table></figure>
<p>将文件从暂存区撤回来，即抵消git add main.py操作,撤销文件的修改详细操作可以<a href="https://blog.csdn.net/kikajack/article/details/79846098">参见这里</a></p>
<figure class="highlight bash"><table><tr><td class="code"><pre><span class="line">git reset main.py</span><br></pre></td></tr></table></figure>
<p>查看一些信息</p>
<figure class="highlight bash"><table><tr><td class="code"><pre><span class="line">git <span class="built_in">log</span> --follow main.oy <span class="comment">#查看文件的版本历史</span></span><br><span class="line">git whatchanged main.py <span class="comment">#查看文件每次提交的修改历史</span></span><br><span class="line">git <span class="built_in">log</span> -p main.py <span class="comment">#查看文件每次的不同</span></span><br><span class="line">git blame main.py <span class="comment">#显示文件什么时候被什么人修改过</span></span><br><span class="line">Git diff <span class="comment">#显示暂存区和工作区的差异</span></span><br><span class="line">git show --name-only commit-id <span class="comment">#显示某次提交发生变化的文件名</span></span><br><span class="line">git show commit-id <span class="comment">#显示某次提交数据和内容的变化</span></span><br><span class="line">git show commit-id1 commit-id2 <span class="comment">#显示两次提交内容的差异</span></span><br><span class="line">git diff HEAD <span class="comment">#显示工作区和最新一次提交的差异</span></span><br></pre></td></tr></table></figure></li>
<li><p><strong>让自己搭的django app公网可访问：holer使用</strong><br>一般来说,自己电脑上搭建的app，在外网无法访问，借助工具holer可以实现;<br>假设现在你已经开发好了一些django应用，并且启动了服务：<br>python manage.oy runserver 127.0.0.1:8080 #后边是网址，但是为局域网<br>为了让该应用，可以让所有人访问,得进行如下配置</p>
<figure class="highlight bash"><table><tr><td class="code"><pre><span class="line">step 1： 安装java 1.8或者更高版本</span><br><span class="line">step 2: 下载[holer软件](https://link.zhihu.com/?target=https%3A//github.com/wisdom-projects/holer/tree/master/Binary/Java)</span><br><span class="line">Step 3: 解压zip文件，并修改配置文件：hole-client/conf/holer.conf</span><br><span class="line">设置HOLER_ACCESS_KEY如下：</span><br><span class="line">HOLER_ACCESS_KEY=HOLER_CLIENT-2F8D8B78B3C2A0AE</span><br><span class="line">HOLER_SERVER_HOST=holer.org</span><br><span class="line">上面的holder_access_Key,参照下表设置：</span><br><span class="line">访问密钥	                             访问域名	           公网地址	 本地地址	    使用场景</span><br><span class="line">HOLER_CLIENT-2F8D8B78B3C2A0AE	holer65530.wdom.net	holer.org:65530	127.0.0.1:8080	WEB</span><br><span class="line">HOLER_CLIENT-3C07CDFD1BF99BF2	holer65531.wdom.net	holer.org:65531	127.0.0.1:8088	WEB</span><br><span class="line">HOLER_CLIENT-2A623FCB6E2A7D1D	holer65532.wdom.net	holer.org:65532	127.0.0.1:80	WEB</span><br><span class="line">HOLER_CLIENT-AF3E6391525F70E4	             N/A	holer.org:65533	127.0.0.1:3389	远程桌面</span><br><span class="line">HOLER_CLIENT-822404317F9D8ADD	             N/A	holer.org:65534	127.0.0.1:22	SSH</span><br><span class="line">HOLER_CLIENT-27DD1389DF1D4DBC	             N/A	holer.org:65535	127.0.0.1:3306	数据库</span><br><span class="line">我们启动的网址和端口，为127.0.0.1和8080所以使用第一个密钥</span><br><span class="line">step 4:启动服务</span><br><span class="line">Cd holer-client/bin</span><br><span class="line">bash startup.sh(停止为bash shutdown.sh)</span><br><span class="line">如果为windows直接点击startup.bat/shutdown.bat</span><br></pre></td></tr></table></figure>
<p>那么当前的网址<a href="http://127.0.0.1:8080/">http://127.0.0.1:8080</a> 等价于 <a href="http://holer.org:65530/">http://holer.org:65530</a>, 输入该网址就等于输入了<a href="http://127.0.0.1:8080/">http://127.0.0.1:8080</a><br>更加详细的教程可以看<a href="http://blog.wdom.net/article/28">这里</a></p>
</li>
<li><p><strong>pytorch使用data_prefetcher加速数据读取</strong><br>首先声明一个类</p>
<figure class="highlight bash"><table><tr><td class="code"><pre><span class="line">class Data_Prefetcher():</span><br><span class="line">     def __init__(self, loader):</span><br><span class="line">         self.loader = iter(loader)</span><br><span class="line">         self.dataloader = loader</span><br><span class="line">         self.stream = torch.cuda.Stream()</span><br><span class="line">         <span class="comment">#self.fetch_next()</span></span><br><span class="line"></span><br><span class="line">     def fetch_next(self):</span><br><span class="line">         try:</span><br><span class="line">             self.batch_data = next(self.loader) <span class="comment">#imgs是一个字典形式</span></span><br><span class="line">         except StopIteration:</span><br><span class="line">             self.batch_data = None</span><br><span class="line">             self.loader = iter(self.dataloader) <span class="comment">#如果迭代器到头了重新开始</span></span><br><span class="line">             <span class="built_in">return</span></span><br><span class="line"></span><br><span class="line">         with torch.cuda.stream(self.stream):</span><br><span class="line">             <span class="keyword">for</span> item <span class="keyword">in</span> self.imgs:</span><br><span class="line">                 self.batch_data[item] = self.batch_data[item].cuda(non_blocking=True)</span><br><span class="line"></span><br><span class="line">	      <span class="comment">#如果不是字典，直接是tensor，直接self.img = self.img.cuda(non_blocking=True)</span></span><br><span class="line">         <span class="built_in">return</span> self.batch_data</span><br></pre></td></tr></table></figure>
<p>训练的写法对应的更新为</p>
<figure class="highlight bash"><table><tr><td class="code"><pre><span class="line">dataloader = DataLoader(dataset= ...) <span class="comment">#定义你的dataloader</span></span><br><span class="line">data_prefetcher = Data_Prefetcher(dataloaader)</span><br><span class="line"><span class="keyword">for</span> epoch <span class="keyword">in</span> range(epochs):</span><br><span class="line">	batch_data = data_prefetcher.fetch_next()</span><br><span class="line">	<span class="keyword">while</span> batch_data is not None:</span><br><span class="line">		images = batch_data[<span class="string">&#x27;imgs&#x27;</span>]</span><br><span class="line"> 		labels = batch_data[<span class="string">&#x27;labels&#x27;</span>]</span><br><span class="line">		......</span><br><span class="line"></span><br><span class="line">		batch_data = data_prefetcher.fetch_next()</span><br><span class="line"></span><br></pre></td></tr></table></figure></li>
<li><p><strong>tensorflow使用预训练的网络作为网络的一部分</strong><br>step 1: 下载所需要的模型和对应的模型代码（tar.gz文件和code），并解压：<a href="https://github.com/tensorflow/models/tree/master/research/slim">https://github.com/tensorflow/models/tree/master/research/slim</a></p>
</li>
</ul>
<p>step 2: 载入参数–tf.1.12.0</p>
<figure class="highlight bash"><table><tr><td class="code"><pre><span class="line">from tensorflow.python import pywrap_tensorflow</span><br><span class="line">from resnet_v2 import resnet_v2_152, resnet_arg_scope</span><br><span class="line">slim = tf.contrib.slim</span><br><span class="line">def load_checkpoint(path=<span class="string">&#x27;./resnet_v2_152.ckpt&#x27;</span>, sess=None):</span><br><span class="line">    reader = pywrap_tensorflow.NewCheckpointReader(path)</span><br><span class="line">    saved_vars = reader.get_variable_to_shape_map()</span><br><span class="line">    variables = tf.trainable_variables()</span><br><span class="line">    count =0</span><br><span class="line">    hit_list = []</span><br><span class="line">    <span class="keyword">if</span> sess is None:</span><br><span class="line">        sess = tf.get_default_session()<span class="comment">#传入自己的sess，自己网络所在图</span></span><br><span class="line">    vars_to_restore = []</span><br><span class="line">    <span class="keyword">for</span> v <span class="keyword">in</span> variables:</span><br><span class="line">        v_name = v.name[:-2] <span class="keyword">if</span> v.name.endswith(<span class="string">&#x27;:0&#x27;</span>) <span class="keyword">else</span> v.name</span><br><span class="line">        <span class="keyword">for</span> k <span class="keyword">in</span> sorted(saved_vars):</span><br><span class="line">            k_name = k[:-2] <span class="keyword">if</span> k.endswith(<span class="string">&#x27;:0&#x27;</span>) <span class="keyword">else</span> k</span><br><span class="line">            <span class="keyword">if</span> v_name.replace(<span class="string">&#x27;cpu_variables/&#x27;</span>,<span class="string">&#x27;&#x27;</span>) == k_name:<span class="comment">#名称匹配来赋值</span></span><br><span class="line">                <span class="comment">#print(v_name, k_name)</span></span><br><span class="line">                count += 1</span><br><span class="line">                hit_list.append(v.name)</span><br><span class="line">                para = reader.get_tensor(k)</span><br><span class="line">                vars_to_restore.append(v.assign(para))</span><br><span class="line"></span><br><span class="line">    sess.run(vars_to_restore)</span><br><span class="line">    <span class="comment">#saved_names = sorted(list(saved_vars.keys()))</span></span><br><span class="line">    <span class="comment">#filter = [item for item in saved_names if &#x27;conv1&#x27; in item]</span></span><br><span class="line">    <span class="built_in">print</span>(count, len(hit_list))</span><br><span class="line"><span class="keyword">if</span> __name__ == <span class="string">&#x27;__main__&#x27;</span>:</span><br><span class="line">    input = tf.placeholder(tf.float32, [6, 128,128,3])</span><br><span class="line">    input_arr = np.random.rand(6,128,128,3)</span><br><span class="line">    <span class="comment">#使用该var_scope和类别数设置为1001是必须的</span></span><br><span class="line">    arg_scope = resnet_arg_scope()<span class="comment">#官方的设定的一些参数，包括卷积的参数配置    </span></span><br><span class="line">    with slim.arg_scope(arg_scope):</span><br><span class="line">      output, end_points = resnet_v2_152(input, 1001, is_training=False)</span><br><span class="line"></span><br><span class="line">    load_checkpoint()    </span><br><span class="line">    sess = tf.InteractiveSession()</span><br><span class="line"></span><br><span class="line">    sess.run(tf.global_variables_initializer())</span><br><span class="line">    out_arr = sess.run(output, feed_dict=&#123;input:input_arr&#125;)</span><br><span class="line">    <span class="built_in">print</span>(<span class="string">&#x27;Done&#x27;</span>)</span><br><span class="line"></span><br></pre></td></tr></table></figure>

<ul>
<li><p>** keras进行数据扩充，同时对图像和mask进行变换**</p>
<figure class="highlight bash"><table><tr><td class="code"><pre><span class="line"><span class="comment">#定义数据扩充的参数</span></span><br><span class="line">aug_args = dict(</span><br><span class="line">rotation_range=90.,</span><br><span class="line">width_shift_range=0.4,</span><br><span class="line">height_shift_range=0.4,</span><br><span class="line">zoom_range=0.5, horizontal_flip=True, vertical_flip=True)</span><br><span class="line"><span class="comment">#利用参数分别定义image_generator</span></span><br><span class="line">imDataGen = ImageDataGenerator(**aug_args)</span><br><span class="line">maskDataGen = ImageDataGenerator(**aug_args)</span><br><span class="line">maskDataGen1 = ImageDataGenerator(**aug_args)</span><br><span class="line">maskDataGen2 = ImageDataGenerator(**aug_args)</span><br><span class="line">maskDataGen3 = ImageDataGenerator(**aug_args)</span><br><span class="line"><span class="comment">#如果imageDataGenerator中制定了normalization或者白化参数，则需要进行fit</span></span><br><span class="line"><span class="comment">#imDataGen.fit(im_train, augment=True, seed=random_seed)</span></span><br><span class="line"><span class="comment">#maskDataGen.fit(mask_train, augment=True, seed=random_seed)</span></span><br><span class="line"></span><br><span class="line"><span class="comment">#接下来只需要进行在generator中制定seed就可以同步的变换</span></span><br><span class="line">image_generator = imDataGen.flow(x=im_train, seed=random_seed, shuffle=True, batch_size=batchnum)</span><br><span class="line">mask_generator = maskDataGen.flow(x=mask_train, seed=random_seed, shuffle=True, batch_size=batchnum)</span><br><span class="line">mask_generator1 = maskDataGen.flow(x=mask_train1, seed=random_seed, shuffle=True, batch_size=batchnum)</span><br><span class="line">mask_generator2 = maskDataGen.flow(x=mask_train2, seed=random_seed, shuffle=True, batch_size=batchnum)</span><br><span class="line">mask_generator3 = maskDataGen.flow(x=mask_train3, seed=random_seed, shuffle=True, batch_size=batchnum)</span><br><span class="line"></span><br><span class="line"><span class="comment">#如果网络有多个输出，也需要再次封装为Geneartor</span></span><br><span class="line">def multi_output_generator(image_generator, mask_generator1, mask_generator2, mask_generator3, mask_generator4):</span><br><span class="line">     <span class="keyword">while</span> True:</span><br><span class="line">         im = image_generator.next()</span><br><span class="line">         gt1 = mask_generator1.next()</span><br><span class="line">         gt2 = mask_generator2.next()</span><br><span class="line">         gt3 = mask_generator3.next()</span><br><span class="line">         gt4 = mask_generator4.next()</span><br><span class="line"></span><br><span class="line">         yield im, [gt1, gt2, gt3, gt4]</span><br><span class="line"></span><br><span class="line">final_train_generator = multi_output_generator(image_generator, mask_generator1, mask_generator2, mask_generator3, mask_generator)</span><br><span class="line"><span class="comment">#将最后封装好的generator传入fit_generator函数中进行训练</span></span><br><span class="line">hist = model.fit_generator(final_train_generator, steps_per_epoch=len(train_index)//batchnum,</span><br><span class="line">shuffle=True, epochs=epochnum,validation_data=[im_val, [mask_val1, mask_val2, mask_val3, mask_val]], callbacks=[checkpoint])</span><br><span class="line">h = hist.history</span><br></pre></td></tr></table></figure></li>
<li><p><strong>keras中自定义损失函数</strong></p>
<figure class="highlight bash"><table><tr><td class="code"><pre><span class="line">import keras.layers as KL</span><br><span class="line">From losses import My_Loss</span><br><span class="line">From keras import Model</span><br><span class="line"><span class="comment">#step1:写好损失函数并利用Lamdab进行封装</span></span><br><span class="line">my_loss = KL.Lambda(lambda x:My_Loss(*x), name=<span class="string">&#x27;my_loss&#x27;</span>)([input1, input2, input3])</span><br><span class="line"><span class="comment">#step2:正常定义模型，output中需要输出my_loss</span></span><br><span class="line">model= Model(input=[input1, ....], output=[output1, ...., my_loss])</span><br><span class="line"><span class="comment">#step3:得到loss的输出并加入到模型的loss之中去</span></span><br><span class="line">loss = model.get_layer(<span class="string">&#x27;my_loss&#x27;</span>).output</span><br><span class="line">model.add_loss(loss)</span><br><span class="line"><span class="comment">#step4:loss的对应输出定义为None</span></span><br><span class="line">losses=&#123;<span class="string">&#x27;out1_name&#x27;</span>:loss_fun, ...., <span class="string">&#x27;my_loss&#x27;</span>:None&#125;</span><br><span class="line">weights=&#123;<span class="string">&#x27;out1_name&#x27;</span>:1, ..., <span class="string">&#x27;my_loss&#x27;</span>:1&#125;</span><br><span class="line">最后进行模型compile即可完成</span><br><span class="line">model.compile(optimizer=opt, loss=loss, loss_weights=loss_weights,metrics=[losses.dsc])</span><br></pre></td></tr></table></figure></li>
<li><p>**pytorch对不同的图像进行同样的数据增广操作 **<br>利用设置随机种子来实现</p>
<figure class="highlight bash"><table><tr><td class="code"><pre><span class="line">import random</span><br><span class="line">import numpy as np</span><br><span class="line">...</span><br><span class="line">def getitem():</span><br><span class="line">    img = ....</span><br><span class="line">    Mask = ...</span><br><span class="line">    </span><br><span class="line">    seed = np.random.randint(123456)</span><br><span class="line">    random.seed(seed)</span><br><span class="line">    img = self.transform(img)</span><br><span class="line">    </span><br><span class="line">    <span class="comment">#再次设置transofrm</span></span><br><span class="line">    random.seed(seed)</span><br><span class="line">    Mask = self.transform(mask)</span><br><span class="line">...</span><br><span class="line"></span><br></pre></td></tr></table></figure></li>
<li><p><strong>keras打印不需要训练的参数</strong></p>
<figure class="highlight bash"><table><tr><td class="code"><pre><span class="line"><span class="comment">#model.compile之后</span></span><br><span class="line"><span class="built_in">print</span>(model.non_trainable_weights)</span><br></pre></td></tr></table></figure></li>
<li><p>** pytorch可视化相关**<br>首先需要安装tensor board: pip install tensorboard<br>1， from torch.utils.tensorboard import SummaryWriter错误： No module named “past”,解决方法：</p>
<figure class="highlight bash"><table><tr><td class="code"><pre><span class="line">Pip install -r tb-nightly</span><br><span class="line">Pip install -r future</span><br></pre></td></tr></table></figure></li>
</ul>
<p>2, 启动tensorboard（tensorboard –logdir=./logs）报错： Tensorboard: ValueError: Duplicate plugins for name projector</p>
<figure class="highlight bash"><table><tr><td class="code"><pre><span class="line">A, 下载该文件</span><br><span class="line">wget https://raw.githubusercontent.com/tensorflow/tensorboard/master/tensorboard/tools/diagnose_tensorboard.py </span><br><span class="line"></span><br><span class="line">B, 运行</span><br><span class="line">python diagnose_tensorboard.py</span><br><span class="line"></span><br><span class="line">C, 遵照指示进行fix，一般得卸载一些东西并安装tensorflow</span><br></pre></td></tr></table></figure>

<p>3 将服务器上写的log文件在本地显示 </p>
<figure class="highlight bash"><table><tr><td class="code"><pre><span class="line">A, 在服务器上启动tensorboard</span><br><span class="line">tensorboard --logdir=./logs --port 6006</span><br><span class="line"></span><br><span class="line">B, 在本地进行端口映射(可以加f选项后台运行)</span><br><span class="line">ssh -NL localhost:6006:localhost:6006 user@remote</span><br><span class="line"></span><br><span class="line">N: no remote commands</span><br><span class="line">L machine1:portA : machine2:portB: forward machine2:portB to machine1:portA</span><br><span class="line"></span><br><span class="line"><span class="comment">#查看哪些进程在使用指定端口:</span></span><br><span class="line">lsof  -ti:6006</span><br><span class="line"></span><br><span class="line"><span class="comment">#释放指定端口</span></span><br><span class="line">lsof -ti:6006 | xargs <span class="built_in">kill</span> 9</span><br></pre></td></tr></table></figure>

<ul>
<li><p><strong>linux查看资源占用</strong></p>
<figure class="highlight bash"><table><tr><td class="code"><pre><span class="line"><span class="comment">#磁盘占用</span></span><br><span class="line">sudo iotop</span><br><span class="line"></span><br></pre></td></tr></table></figure></li>
<li><p><strong>tensorflow/keras对一个tensor安装行进行shuffle</strong></p>
<figure class="highlight bash"><table><tr><td class="code"><pre><span class="line"><span class="comment"># 不能直接tf.random.shuffle(tensor) shape: bs x height x width x channel</span></span><br><span class="line">Out = tf.gather(tensor, tf.random.shuffle(tf.range(bs)))</span><br></pre></td></tr></table></figure></li>
<li><p><strong>利用imutils图像翻转</strong></p>
<figure class="highlight bash"><table><tr><td class="code"><pre><span class="line"><span class="comment">#pip install imutils</span></span><br><span class="line"><span class="comment">#翻转30度并且超过边界的进行裁剪</span></span><br><span class="line">im = cv2,imread(im_path) <span class="comment">#im shape:256x256x3</span></span><br><span class="line">im_rotate = imutils.rotate(im,30)</span><br><span class="line"></span><br><span class="line"><span class="comment">#翻转30度并且保留全图的内容，不进行裁剪</span></span><br><span class="line">im_rotate_bound = imutils.im_rotate(im, 30)</span><br></pre></td></tr></table></figure></li>
<li><p><strong>nii.gz图像的读取和显示</strong></p>
<figure class="highlight bash"><table><tr><td class="code"><pre><span class="line">Import nibble as nib</span><br><span class="line">From nilearn import plotting</span><br><span class="line"><span class="comment">#1读取数据</span></span><br><span class="line">data = nib.load(nii_path)</span><br><span class="line">data_arr = np.array(data.get_fdata(), np.uint8)</span><br><span class="line"></span><br><span class="line"><span class="comment">#2显示图像直接传送路径即可</span></span><br><span class="line">plotting.plot_img(path)</span><br><span class="line"></span><br></pre></td></tr></table></figure></li>
<li><p><strong>可视化segmentation label</strong></p>
<figure class="highlight bash"><table><tr><td class="code"><pre><span class="line">from PIL import Image</span><br><span class="line"><span class="comment">#open and image or generate an image from array</span></span><br><span class="line">im=Image.fromarray(label_array, mode=<span class="string">&#x27;P&#x27;</span>) <span class="comment">#mode可以为&#x27;P&#x27;或者&#x27;L&#x27;</span></span><br><span class="line"></span><br><span class="line"><span class="comment">#定义colormap,数据有四个不同的label，则需定义4x3长度的list，每连续三个元素定义为一个颜色</span></span><br><span class="line"><span class="comment">#也可以从一个具有调色板的图像中得到color_map:</span></span><br><span class="line"><span class="comment">#color_map = im.getpalette()</span></span><br><span class="line">color_map = [255,0,0,  0,255,0,  0,0,255,  128,0,128]</span><br><span class="line">im.putpalette(color_map)</span><br><span class="line"></span><br><span class="line">im.save(<span class="string">&#x27;test.png&#x27;</span>)</span><br></pre></td></tr></table></figure></li>
<li><p><strong>可视化disparity map</strong></p>
<figure class="highlight plaintext"><table><tr><td class="code"><pre><span class="line">from PIL import Image</span><br><span class="line"></span><br><span class="line">image = cv2.imread(disparity_path, cv2.IMREAD_GRAYSCALE)</span><br><span class="line">plt.imshow(image, cmap=&#x27;viridis&#x27;) #virisdis为default setting</span><br><span class="line">plt.axis(&#x27;off&#x27;)</span><br><span class="line">plt.show()</span><br><span class="line"></span><br><span class="line">#cmap是调色板参数，内置调色板参数较为好看的有：rainbow, plasma, cool, viridis</span><br><span class="line">#参考https://matplotlib.org/stable/tutorials/colors/colormaps.html </span><br></pre></td></tr></table></figure></li>
<li><p><strong>pytorch中调用kl散度</strong></p>
<figure class="highlight bash"><table><tr><td class="code"><pre><span class="line">Import torch.nn.Functional as F</span><br><span class="line">Input = [0.2,0.3,0.4,0.2]</span><br><span class="line">Target = [0.3,0.4,0.1,0.2]</span><br><span class="line"></span><br><span class="line">Kl = F.kl_div(input.log(), target， reduction=<span class="string">&#x27;mean&#x27;</span>) <span class="comment">#input需要log，而不是直接传进去</span></span><br><span class="line"></span><br><span class="line"><span class="comment"># reduction=&#x27;mean&#x27;，所有元素求平均，等价于：</span></span><br><span class="line">kl = torch.mean(input * torch.log(input / (target+1e-10) + 1e-10))</span><br><span class="line"><span class="comment">#reduction=&#x27;batchmean&#x27;,bachsize求平均，为KL散度的严格定义，等价于</span></span><br><span class="line">Kl = torch.sum(input * torch.log(input / (target+1e-10) + 1e-10)) / batch_size </span><br><span class="line"></span><br></pre></td></tr></table></figure></li>
<li><p><strong>指数函数exp导致训练过程出现NaN</strong><br>指数函数torch.exp作用的数字过大/小，都会造成数值的溢出，从而导致训练的过程中出现NaN，<br>解决方法是进行数值裁剪</p>
<figure class="highlight bash"><table><tr><td class="code"><pre><span class="line">torch.exp(value).clamp(min=1e-8,max=1e15),</span><br></pre></td></tr></table></figure></li>
<li><p><strong>保存uint16的图片</strong></p>
<figure class="highlight bash"><table><tr><td class="code"><pre><span class="line">from PIL import Image</span><br><span class="line">Im = Image.fromarray(data.astype(np.uint16)).save(<span class="string">&#x27;16bit.png&#x27;</span>)</span><br><span class="line"><span class="comment">#读取时使用</span></span><br><span class="line">im_r = cv2.imread(<span class="string">&#x27;16bit.png&#x27;</span>, cv2.IMREAD_ANYDEPTH) <span class="comment">#后面的参数很重要</span></span><br></pre></td></tr></table></figure></li>
<li><p>** 将numpy中元素进行从小到大排列**</p>
<figure class="highlight bash"><table><tr><td class="code"><pre><span class="line">Import numpy as np</span><br><span class="line">Data = np.random.rand(3,4)</span><br><span class="line">Ele, count = np.unique(Data, return_counts=True)</span><br><span class="line">bd_index = np.argsort(count)</span><br><span class="line"><span class="comment">#the maximum ele</span></span><br><span class="line">Ele[bd_index[-1]]</span><br></pre></td></tr></table></figure></li>
<li><p>**pytorch (0.4.1) conda install error **<br>When. Install the pytorch 0.4.1 via conda: conda install pytorch=0.4.1 cuda90 -c pytorch<br>got error: </p>
<figure class="highlight bash"><table><tr><td class="code"><pre><span class="line">PackageNotFoundError: Package not found: <span class="string">&#x27;&#x27;</span> Dependencies missing <span class="keyword">in</span> current linux-64 channels:</span><br><span class="line">  - pytorch 0.4.1* -&gt; mkl &gt;=2018</span><br><span class="line">  - pytorch 0.4.1* -&gt; python &gt;=3.7,&lt;3.8.0a0</span><br><span class="line">  - pytorch 0.4.1* -&gt; cudatoolkit 9.*</span><br></pre></td></tr></table></figure></li>
</ul>
<p>Solution:</p>
<figure class="highlight bash"><table><tr><td class="code"><pre><span class="line">conda config --add channels conda-forge</span><br></pre></td></tr></table></figure>

<ul>
<li><p>** 得到numpy中矩阵中出现次数最大的元素**</p>
<figure class="highlight bash"><table><tr><td class="code"><pre><span class="line">Mat = np.random.rand(3,4)</span><br><span class="line">most_freq_ele = max(<span class="built_in">set</span>(mat.flatten().tolist()), key=seg_labels.count)</span><br></pre></td></tr></table></figure></li>
<li><p>** 利用pandas对csv文件进行操作 **</p>
<figure class="highlight plaintext"><table><tr><td class="code"><pre><span class="line">import pandas as pd</span><br><span class="line"></span><br><span class="line">csv_path = &#x27;csv_file.csv&#x27;</span><br><span class="line">#读入数据的几种常用操作</span><br><span class="line">#最简单形式，csv_data时DataFrame类型数据</span><br><span class="line">csv_data = pd.read_csv(csv_path, sep=&#x27;\t&#x27;)  </span><br><span class="line"></span><br><span class="line">#可以设置列名</span><br><span class="line">#设置header=None使用默认列名</span><br><span class="line">csv_data = pd.read_csv(csv_path, sep=&#x27;\t&#x27;, names=[&#x27;str1&#x27;, &#x27;str2&#x27;, &#x27;str3&#x27;]) #</span><br><span class="line"></span><br><span class="line">#读入时设置某列的值作为行名（索引）</span><br><span class="line">#将会每行以该列作为索引</span><br><span class="line">csv_data = pd.read_csv(csv_path, sep=&#x27;\t&#x27;, index_col=&#x27;str1&#x27;,  names=[&#x27;str1&#x27;, &#x27;str2&#x27;, &#x27;str3&#x27;])</span><br><span class="line"></span><br><span class="line">更多参数</span><br><span class="line">skip_rows=[0,1]  读入时跳过指定的行</span><br><span class="line">na_values=[&#x27;male&#x27;]. 将缺失的值（读取进来时NaN）填充为设定的值</span><br><span class="line">nrows=3. 只读取前3行数据</span><br><span class="line">chunksize=10. 分批读取，设置之后每次读取10个数据, 返回一个迭代器， csv_data.get_chunk(10)得到第11个块</span><br><span class="line"></span><br><span class="line">#pandas设置分批去读，例如设置显示前10行</span><br><span class="line">pd.options.display.max_rows=10</span><br><span class="line"></span><br><span class="line">#写入csv文件</span><br><span class="line">csv_data.to_csv(&#x27;./my.csv&#x27;, sep= &#x27;,&#x27;, na_rep= &#x27;WA&#x27;, columns= [&#x27;sex&#x27;,&#x27;age&#x27;,&#x27;name&#x27;,&#x27;a&#x27;])</span><br><span class="line"></span><br></pre></td></tr></table></figure></li>
<li><p><strong>csv读入之后，对DataFrame数据的操作</strong></p>
<figure class="highlight bash"><table><tr><td class="code"><pre><span class="line">import pandas as pd</span><br><span class="line">csv_data = pd.read_csv(path1, sep=<span class="string">&#x27;\t&#x27;</span>)</span><br><span class="line"><span class="comment">#csv_data的基础属性包括values, index, columns和dtypes</span></span><br><span class="line">csv_data.</span><br><span class="line">Values: 获取元素</span><br><span class="line">Index: 获取属性</span><br><span class="line">column:获取列名</span><br><span class="line">Types: 获取类型</span><br><span class="line">Ndim： 获取维度</span><br><span class="line">shape: 数据形状</span><br><span class="line"></span><br><span class="line"><span class="comment">#对数据的访问</span></span><br><span class="line"><span class="comment">#取出某列</span></span><br><span class="line">Age = csv_data[<span class="string">&#x27;Age&#x27;</span>]</span><br><span class="line"></span><br><span class="line"><span class="comment">#取出某行</span></span><br><span class="line">Out = csv_data[:][I:i+1]</span><br><span class="line"></span><br><span class="line"><span class="comment">#取出多行多列</span></span><br><span class="line">Out = csv_data[<span class="string">&#x27;age&#x27;</span>,<span class="string">&#x27;year&#x27;</span>][:3]</span><br><span class="line"></span><br><span class="line"><span class="comment">#更方便的是利用iloc/loc函数进行数据读取,</span></span><br><span class="line"><span class="comment"># iloc第二维度中填列索引，loc中填列名称</span></span><br><span class="line">DataFrame.loc[行索引条件/名称，列索引名称]</span><br><span class="line">DataFrame.iloc[行索引位置，列索引位置]</span><br><span class="line"><span class="comment">#例子</span></span><br><span class="line">Out = csv_data.iloc[:10, 2] <span class="comment">#前10行，2列数据</span></span><br><span class="line">Out2 = csv_data.loc[:10, <span class="string">&#x27;age&#x27;</span>] <span class="comment">#钱10行，age列数据</span></span><br><span class="line"><span class="comment">#根据条件过滤</span></span><br><span class="line">Out = csv_data.loc[csv_data[<span class="string">&#x27;age&#x27;</span>]&gt;10, <span class="string">&#x27;age&#x27;</span>]</span><br><span class="line"></span><br><span class="line"><span class="comment">#访问任意数据</span></span><br><span class="line">Out = csv_data.loc[:10, [<span class="string">&#x27;age&#x27;</span>,<span class="string">&#x27;year&#x27;</span>]]</span><br><span class="line"></span><br><span class="line"><span class="comment">#取出制定行制定列数据</span></span><br><span class="line">Out = csv.loc[2, [<span class="string">&#x27;age&#x27;</span>]]</span><br><span class="line">Out = csv.iloc[2, [0]]</span><br><span class="line"></span><br><span class="line"><span class="comment">#删除列数据</span></span><br><span class="line">Out = csv_data.drop([<span class="string">&#x27;age&#x27;</span>], axis=1)</span><br><span class="line"><span class="comment">#删除全部为nan的行</span></span><br><span class="line">Out = csv_data.dropna(axis=0, how=<span class="string">&#x27;all&#x27;</span>) <span class="comment">#how=&#x27;any&#x27; 删除某一元素为nan的行</span></span><br><span class="line"></span><br><span class="line"></span><br><span class="line"><span class="comment">#这里</span></span><br><span class="line">out = csv.iloc[2, [1]]索引出来的是序列化的数据，而</span><br><span class="line">csv.iloc[2,1]直接是对应的位置的值，等于out[0]</span><br><span class="line"></span><br><span class="line"><span class="comment">#DataFrame对某列进行操作</span></span><br><span class="line">Data1 = csv_data.age.apply(lambda x:int(x))</span><br><span class="line"></span><br><span class="line"><span class="comment">#匹配字典</span></span><br><span class="line"><span class="comment">#dict中村的事age:year, 映射过后age列和自动中对应age对应起来</span></span><br><span class="line">csv_data[<span class="string">&#x27;age&#x27;</span>] = csv_data[<span class="string">&#x27;year&#x27;</span>].map(a_dict)</span><br><span class="line"></span><br><span class="line"><span class="comment">#一般读如的age是字符串，我们需要转换为数字</span></span><br><span class="line"><span class="comment">#首先处理‘uknown’数字（或者其他类型）</span></span><br><span class="line">csv_data = csv_data.replace(&#123;<span class="string">&#x27;unknown&#x27;</span>:<span class="string">&#x27;0.0&#x27;</span>&#125;)</span><br><span class="line"><span class="comment">#应用函数</span></span><br><span class="line">csv_data1 = csv_data.age.apply(lambda x:int(x))</span><br><span class="line">csv_data[<span class="string">&#x27;age&#x27;</span>] = csv_data1</span><br><span class="line"></span><br><span class="line"><span class="comment">#按照列进行排序</span></span><br><span class="line">out. = csv_data.sort_values(by=[<span class="string">&#x27;age&#x27;</span>], ascending=False)</span><br><span class="line"><span class="comment">#也可以按照双列进行排序</span></span><br><span class="line">Out = csv_dta.sort_values(by=[<span class="string">&#x27;age&#x27;</span>,<span class="string">&#x27;height&#x27;</span>], ascending=[False, True])</span><br><span class="line"><span class="comment">#排序完还想调整索引号为升序，</span></span><br><span class="line">csv_data.reset_index()</span><br><span class="line"><span class="comment">#恢复原来的排序</span></span><br><span class="line">Out = out.sord_index()</span><br><span class="line"><span class="comment">#如果不想删除原索引号，值增加一列新索引，则:</span></span><br><span class="line">csv_data.reset_index()</span><br></pre></td></tr></table></figure></li>
</ul>
]]></content>
  </entry>
</search>
